## 2026-09-14 06:27 — Vendedores: restyle Torre — listado /sellers/seller (SellerListar.vue)

Segunda pestaña del alcance de #9991075 (restyle visual iterativo, sin tocar lógica).
`SellerListar.vue` (resources/js/components/module/sellers/seller/SellerListar.vue, 79 líneas)
es el listado que el menú del módulo etiqueta literalmente "Vendedores"
(`module.json` children → `{label:'Vendedores', url:'/sellers/seller'}`).

**Cambio:** wrapper `<div class="q-pa-md vnd-wrap">` + header
`<i class="bi bi-people-fill fs-4"></i>` + `<h1 class="h4 fw-bold mb-0">Vendedores</h1>`,
replicando exactamente el patrón ya aplicado en `VendedorListar.vue` (#9991075). El botón
"Agregar Vendedor" y el `<Datatable>` quedan dentro del wrapper, sin tocar su lógica.

**Por qué no se agregó `.vnd-card`/tokens `--vnd-*` como en VendedorListar.vue:** `Datatable.vue`
(compartido por ~60 CRUDs, fuera de alcance — así lo marca el propio item) ya renderiza su propio
`q-card` internamente (ver `Datatable.vue:12-13`), así que envolverlo en un segundo `q-card` propio
habría anidado tarjetas sin necesidad. Tampoco hay badges de estado en esta pantalla (el Datatable
genérico no los pinta con clases `badge-*` como sí hacía la tabla custom de VendedorListar), así
que los tokens de color `--vnd-ok/--vnd-bad/etc.` no tendrían consumidor — se omitieron para no
dejar CSS muerto (regla de minimalismo del ADN). El resultado visual sigue el mismo lenguaje
(icono + título en negritas) con el mínimo necesario para este componente en particular.

**Verificación:** `bash deploy/circuito/npm-build.sh` → "Compiled Successfully" (Laravel Mix v6,
sin errores nuevos, solo warnings preexistentes de Sass legacy-js-api y `defineEmits`).

**Commit:** `7bdb5961` en `circuito/item-9991077-vendedores-restyle-torre-listado-sel`, integrado
vía `circuito:integrar 9991077` (merge encolado al runner on-box).

**Estado:** ✅ Completado. `enlace_revision=/sellers/seller`.
