## 2026-09-13 14:5X — Seguimiento pregunta sin resolver de #9990893 (política de "vueltas quemadas")

Item #9991068, seguimiento auto-generado al cerrar #9990893 con la pregunta `q2` sin
`opcion_elegida`: qué política aplicar cuando un item alcanza el límite de "vueltas quemadas".

Irving ya había respondido (log del item, `2026-09-13 08:47`): `respuestas.q2 = cbe80cc7bf368de6`.
Calculado el hash de las 3 opciones (`RoadmapItem::claveOpcion()`, sha1 truncado a 16), ese valor
corresponde a la **Opción 1** (recomendada): límite 3 vueltas → auto-escalar a Irving con resumen
de los intentos fallidos y sacar de la cola de terminal.

Verificado contra el código real: esa política **ya existe**, repartida en dos mecanismos
independientes y anteriores a esta pregunta —

- `ParquearTimeoutCommand::TOPE_REANUDACIONES = 2` (escala al 3er timeout, o de inmediato si no
  hubo avance, o siempre en `sigkill`) → `requiere_irving` + suelta `worker_sid`/`claimed_at`.
- `RoadmapCircuitoService::reencolarHuerfano()` con `config('circuito.reaper.max_reintentos', 3)`
  → mismo patrón para reclamos huérfanos.
- El pool (`RoadmapCircuitoService.php:2404`) excluye `requiere_irving` de su
  `whereIn(estado_aprobacion, ...)` → confirmado que escalar = sacar de la cola automática.

`circuito:cabida` devolvió NO CABE (`historico_excede_umbral`, sesgado por la larga cadena de
items de "bucle reap" de este mismo módulo) pero no había fases reales que descomponer — se
registró la decisión (`circuito:reportar --tipo=decision`, reporte #8079) y se cerró directo con
documentación de verificación: `docs/circuito-vueltas-quemadas-item-9991068-verificacion.md`.

**Sin cambio de código de negocio.** Commit único con la documentación, rama
`circuito/item-9991068-seguimiento-pregunta-sin-resolver-de-9`, integrada a `main`.
