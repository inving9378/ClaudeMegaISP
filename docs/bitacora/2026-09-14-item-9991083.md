## 2026-09-14 10:15 — #9991083 Autorrenovación TLS dev: el banner sigue en 24 días

**Sesión directa con Irving.** Diagnóstico read-only, sin sudo (la sesión no tiene contraseña).

### Escenario (una línea)
**E2** — el cert SÍ se renovó (`cert2.pem` emitido el 2026-09-11 16:08 por webroot) pero
**nginx nunca recargó** y `:443` sigue sirviendo `cert1.pem` (Jul 10 → **2026-10-08**); el
banner de la Torre lee el cert servido en vivo, así que decía la verdad.

### Evidencia
- **A)** `renewal/dev.meganett.com.mx.conf` (09-11 16:08): `authenticator = webroot`, webroot
  `/var/www/megaisp/public`, `key_type = ecdsa`; **sin** `manual_*_hook`, `deploy_hook` ni
  `renew_hook`; `renewal-hooks/{pre,post,deploy}/` vacíos. `live/dev.meganett.com.mx/*.pem` →
  `cert2/chain2/fullchain2/privkey2` (symlinks del 09-11 16:08; CSR `0006` 16:08:23).
  `cert2.pem` no legible (`archive/` 0700 root); expiración esperada ≈ 2026-12-10.
- **B)** `openssl s_client` a `:443` (SNI): `notBefore=Jul 10 2026`, `notAfter=Oct 8 2026`,
  issuer Let's Encrypt YE2 → cert1. **Es nginx** (`apache2` inactive); worker PID 903 desde el
  2026-09-01 12:15 → sin reload desde entonces.
- **C)** `certbot.timer` enabled/active (2×/día); `certbot.service` última corrida 09-14
  09:02:01, `exit 0` en 2 s (cert2 no está por vencer → "no toca"). Sin cron propio. Journal y
  `/var/log/letsencrypt` no legibles sin root.
- **D)** `certbot renew --dry-run` **no corrido** (requiere root).
- **E)** Banner = `EnvironmentHealthService::certificado()` → `ssl://127.0.0.1:443` con SNI +
  `openssl_x509_parse`, caché 30 s (`RoadmapController:640`). Hoy: `dias_restantes 23`,
  `expira_at 2026-10-08T08:19:54-06:00`. No lee nada persistido.
- Reto HTTP-01: `http://…/.well-known/acme-challenge/test.txt` → 301 → https → 200 (LE sigue
  redirects; la emisión del 09-11 lo demuestra).

### Lo que faltaba: el deploy-hook
Versionado en `acc80ad3`: `deploy/letsencrypt/renewal-hooks/deploy/reload-nginx.sh` (con
`nginx -t` previo y `logger`) + sección "Renovación automática + recarga de nginx" en
`deploy/README-dev-tls.md` con instalación, reload, verificación y rollback. Merge a `main`:
`db808c02`.

### Pendiente (root, lo corre Irving) — item de respuesta **#9991085**
```bash
sudo install -m 0755 /var/www/megaisp/deploy/letsencrypt/renewal-hooks/deploy/reload-nginx.sh /etc/letsencrypt/renewal-hooks/deploy/reload-nginx.sh
sudo nginx -t && sudo systemctl reload nginx
echo | openssl s_client -connect dev.meganett.com.mx:443 -servername dev.meganett.com.mx 2>/dev/null | openssl x509 -noout -dates
sudo certbot renew --dry-run
```
V1–V4 dependen de esos comandos. Desviación a ratificar: el item decía "reload de Apache"; en
dev es nginx.
