# Item #9990934 — CIRC-09: Panorama en árbol (y baja de tres pestañas)

## 2026-09-12 02:19 — Descompuesto en 6 sub-items (uno por fase del propio prompt), cierre-intento completo

`circuito:cabida 9990934 --sid=wt-3` devolvió NO CABE (`ya_timeouteo_antes` — el item ya había
agotado `max_turns` dos veces sin commits en vueltas previas). Siguiendo el protocolo, no se picó
código: se descompuso directo.

**Hallazgo antes de descomponer:** la maqueta `docs/maquetas/panorama-arbol.html` que el propio
prompt exige abrir "ANTES DE ESCRIBIR CÓDIGO" (y que dice explícitamente "cuando este prompt y la
maqueta se contradigan, gana la maqueta") **no existe en el repo** — verificado en `main`, en los 6
worktrees del circuito y en todo el historial de git (`git log --all -- docs/maquetas/`, cero
resultados; el directorio `docs/maquetas/` no existe). Decisión registrada (reporte #7841): no
bloquear la descomposición por esto — cada sub-item de UI usa como especificación visual la tabla
de estados/marcas que el propio prompt trae en prosa, ya que no hay forma de producir la maqueta
ausente.

**Investigación previa a la Fase 1** (vía agente Explore, solo lectura): ubiqué con precisión las
tres pestañas a dar de baja — `AuditReport.vue` (Reporte), `TorreColaEjecutable.vue` (Cola),
`TorreSaludEntorno.vue` (Salud del entorno), todas en
`resources/js/components/module/releases/torre-control/`, montadas por tab en `ReleasesIndex.vue`;
backend en `AuditController.php` (Reporte), `RoadmapController::torreCola()`/`saludEntorno()`
(Cola/Salud) + `EnvironmentHealthService.php`; permisos `torre.cola.ver`/`torre.salud.manage`.

**Hallazgo importante — excepción del prompt ya resuelta, sin duplicar:** el prompt pide, antes de
borrar Reporte, crear un item nuevo SI el pendiente "Fix KpiController → usar tabla invoices" del
Plan de Acción sigue vivo. Verificado en BD: el `AuditPlanItem` id=1 sigue `completado=false`. Pero
ese pendiente **ya está trackeado** como parte de la épica invoices/client_invoices (items #159,
#632, #719-727, #748, #749, #796) — específicamente el item **#754** ("Fase 4 (real): migrar
KpiController/HomeController/AuditController a leer invoices") sigue abierto (`aprobado_irving`,
sus bloqueantes #719/#721/#722 ya `completado`). Documenté esto en el sub-item de la Fase 1 para
que NO se cree un duplicado exacto de #754 al ejecutarlo.

**Sub-items creados** (uno por cada una de las 6 fases del propio prompt, "cada una mergeable por
separado"):

| # | Título | Depende de |
|---|--------|-----------|
| #9990968 | Fase 1 — Baja completa de Cola/Reporte/Salud (con ubicación exacta de archivos ya investigada + excepción del KpiController resuelta sin duplicar) | — |
| #9990969 | Fase 2 — Endpoint de jerarquía Módulo→Épica→Item→Sub-item con lazy-load (resolución de q1 aprobada) | — |
| #9990970 | Fase 3 — Componente de árbol solo lectura (filtros/buscador/persistencia local, resolución de q5) | Fase 2 |
| #9990971 | Fase 4 — Paneles desplegables de lectura (Trabajando / Decidido sin ti) | Fase 3 |
| #9990973 | Fase 5 — Panel de decisión con placeholder (resolución de q4: no esperar a CIRC-02b/#9990858) | Fase 3 |
| #9990974 | Fase 6 — Acciones por estado + refresco en vivo sin perder scroll (resolución de q3) | Fase 4, Fase 5 |

Cada spec incluye las resoluciones ya aprobadas por Irving de las 5 preguntas del item (q1 lazy-load
por nivel, q2 feature-flag antes de borrado duro, q3 polling 5-10s con diff, q4 placeholder ahora
sin esperar CIRC-02b, q5 persistencia en localStorage), para que ningún ejecutor futuro tenga que
re-derivarlas.

**Cierre-intento ejecutado** (para no sumar un séptimo caso más a la familia de bugs "paraguas
descompuesto pero nunca cerrado" ya documentada extensamente en `CLAUDE.md` — #738/#745/#830/#816/
#818/#848/#852/#905/#878/#906/#907/#924 y otros): se intentó `estado_aprobacion='completado'` sobre
#9990934 vía tinker; el guard `(2b) PARAGUAS` de `RoadmapItem.php` lo reenrutó correctamente a
`aprobado_irving` + `excluir_pool_automatico=true` + liberó `worker_sid`/`claimed_at` (verificado
con el resultado real de la mutación). Queda fuera del pool/reaper hasta que el hook de cierre en
cascada lo complete solo cuando los 6 sub-items cierren.

Sin cambio de código de aplicación en esta vuelta — el trabajo real del rediseño del Panorama sigue
en los 6 sub-items, pendientes de que el revisor los tríe y una terminal los reclame.
