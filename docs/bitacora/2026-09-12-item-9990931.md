# Item #9990931 — CIRC-01: fix del escalado por timeout (cuarto duplicado de #9990855)

## 2026-09-12 01:13 — Cerrado como duplicado, sin cambio de código

`circuito:cabida 9990931 --sid=wt-2` devuelve CABE (`sin_senal_de_riesgo`, histórico ~2700s). El
título y el prompt (4 fases: diagnóstico → 3 guardas → saneamiento de los 4 items → candado de
regresión) son idénticos a los de `#9990855`, ya cerrado antes.

**Hallazgo:** `#9990855` resolvió las 4 fases completas y está mergeado a `main` (commit
`a4b200ac`, merge `ff72b2a3`). Verificado línea por línea en `ParquearTimeoutCommand.php`: guarda
de estado + guarda de paraguas + medición de `commits_rama` sobre `items.branch`, exactamente como
pedía el prompt. Reverificados en BD los 4 items atorados del ejemplo: `#9990801`/`#9990714`
`completado`, `#9990776`/`#9990810` `aprobado_irving`+`excluir_pool_automatico` correctamente
parqueados — ninguno sigue en `requiere_irving`. El candado de regresión (Fase 4) también ya
existe: `tests/Feature/Roadmap/ParquearTimeoutGuardsRegresionTest.php` (sub-item `#9990880`,
`completado`, merge `cb50405f`). Lo re-corrí en esta vuelta con el DB de pruebas forzado por env
(`DB_DATABASE=megaisp_test APP_ENV=testing`, porque el shell de este worktree trae `megaisp`/
`local` exportados y pisan el `<env>` de `phpunit.xml`) — **3 passed / 22 assertions**.

Es el **tercer** duplicado exacto del mismo bug detectado en el circuito, tras `#9990865` y
`#9990881` (mismo patrón de carrera: el documento origen genera el mismo hallazgo más de una vez
y el generador no comprueba si ya hay un item vivo cubriéndolo).

**Acción:** cierro `#9990931` directo como duplicado resuelto, sin crear sub-items nuevos (habría
triplicado — cuadruplicado — el trabajo de `#9990855`/`#9990880`). Detalle completo en
`docs/circuito-timeout-escalado-duplicado-item-9990931-verificacion.md`.

**Sin cambio de código de negocio** — el fix ya estaba en `main` antes de que este item se
reclamara.
