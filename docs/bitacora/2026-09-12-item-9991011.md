## 2026-09-12 — Item #9991011 — CIRC-04 PASO 4: propuesta de re-priorización de la cola viva

**Item:** "CIRC-04 PASO 4 — Propuesta de re-priorización de la cola viva (doc en docs/, NO aplicar
el cambio)". Sub-item de seguimiento de `#9990870`. Nivel de riesgo B, autorizado por el revisor
(documento en `docs/`, sin tocar código/BD/config, puramente aditivo).

### Contexto

El padre del circuito (#9990870) midió el sesgo hacia `priority='alta'` en la cola viva
(`estado_aprobacion NOT IN ('completado','cancelado')`) y pidió, como paso 4, construir una
propuesta de re-priorización objetiva — sin ejecutar el cambio, solo documentarlo para que Irving
lo revise. Un intento previo (`wt-6`) murió a media vuelta sin dejar código (`soltar-claim`).

### Qué se hizo

Medición fresca contra la BD real de dev (2026-09-12 ~03:15 UTC): **241 items vivos** (el número
de la descripción original, 244, ya había cambiado — la cola es dinámica). Se generó
`docs/circuito-repriorizacion-cola-2026-09-11.md` con:

- Tabla completa de los 241 items: id, título, prioridad actual, prioridad propuesta, motivo de
  una línea.
- Criterio aplicado tal cual lo definió el padre: **alta** = frente de cierre del mes
  (`modulo='Mapa de Red'` o título con patrón `CIRC-\d+`) **O** tiene otros items vivos
  apuntándole vía `depende_de`/`origen_item_id` (desbloquea a otros); **media** = vigente sin esas
  condiciones; **baja** = se conserva la actual (sin campo de fecha límite en el esquema para
  distinguir objetivamente "sin urgencia", no se demota nada nuevo a baja).
- Nota metodológica explícita sobre la ambigüedad de `origen_item_id` (un paraguas con hijos
  abiertos cuenta como "alta" por conectividad de grafo, no necesariamente por urgencia real —
  la familia de items "bucle reap sobre paraguas ya descompuesto" documentada en `CLAUDE.md`).
- Resumen ejecutivo con el cruce actual→propuesta: de 127 items en `alta`, 62 bajan a `media` (no
  cumplían ningún criterio objetivo) y 57 nuevos suben a `alta` (54 desde media, 3 desde baja, 11
  desde sin-prioridad) — el conteo neto de `alta` casi no cambia (127→133) pero la composición sí,
  que era el punto real del item.

**Sin UPDATE de `priority` en BD** — es exactamente lo que pidió el spec ("el cambio real de
prioridad se ejecuta en un item aparte, después de que Irving apruebe la tabla").

### Verificación

- Tabla markdown con columnas consistentes en las 241 filas (`awk` de sanity-check, 0 filas
  rotas).
- Sin caracteres `|` sueltos en ningún título que pudiera romper el render de la tabla.
- `circuito:estado-item` sin colisión antes de integrar.
- Commit `86ac8179` en la rama `circuito/item-9991011-...` → `circuito:integrar` → mergeado a
  `main` como `1cab7092`.

### Cómo queda

Nuevo doc `docs/circuito-repriorizacion-cola-2026-09-11.md` en `main`, listo para que Irving lo
revise y decida si aprueba la tabla (con o sin ajustes). El UPDATE real de `priority` queda
pendiente de un item aparte que Irving apruebe.
