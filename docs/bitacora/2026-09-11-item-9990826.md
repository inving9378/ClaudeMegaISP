## 2026-09-11 15:41 — Reapertura de acuses: cierre-intento faltante del paraguas #9990826

Item #9990826 ("Reapertura de acuses — implementar y correr el test E2E") llegó a esta vuelta
(wt-2) ya `en_progreso`. Al leerlo, el historial mostraba que una vuelta previa (wt-4,
2026-09-11 15:39) ya había hecho la parte difícil correctamente:

- Corrió `circuito:cabida` → NO CABE (`ya_timeouteo_antes`).
- En vez de picar código a ciegas, investigó a fondo el código real ya en `main`
  (`AcuseReopeningService::reopenForNewVersion()`, `TemplateVersionService::createVersion()`,
  `EmployeeDocumentPackageService::regenerateOne()`, `SignatureSlotStatus::describir()` y las
  migraciones reales de `talento_colaboradores` / `talento_employee_document_signatures`).
- Dejó la receta COMPLETA (setup exacto, asserts happy-path, caso negativo, 2 casos borde) en
  el sub-item **#9990834** ("Reapertura de acuses — implementar y pasar ReaperturaAcusesTest.php
  (happy path + negativo + 2 bordes)"), para que el próximo ejecutor no tenga que re-investigar.

Pero esa vuelta murió a media escritura del comentario final (texto cortado en
`comentarios_claude`) **sin intentar cerrar** al padre #9990826. El log solo registra
`soltar-claim` ("La vuelta de wt-4 terminó sin cerrar el item... se libera el reclamo y vuelve a
la cola como aprobado_revisor"). El pool lo repartió de nuevo (a mí, wt-2) sin que hubiera
trabajo propio pendiente — exactamente el mismo patrón "bucle reap sobre paraguas ya
descompuesto" documentado en CLAUDE.md para #738/#745/#830/#816/#818/#848/#852/#905/#878/#906/
#907/#924/#9990012/#917/#910/#936/#9990408/#962/#9990554/#9990549/#9990624/#9990650/#9990807 y
otros.

**Verificado antes de tocar nada:**
- `AcuseReopeningService.php` y `TemplateVersionService.php` siguen existiendo en
  `app/Modules/Addons/Talento/Services/` (la spec de #9990834 no quedó huérfana de un revert).
- #9990834 sigue intacto: `pendiente_revision`, `worker_sid=null`, sin reclamar — la
  descomposición original de wt-4 seguía siendo correcta, nadie más la tocó.
- Regla dura del prompt: "#341: tu item #9990826 ya fue RECLAMADO para ti. NO toques otros
  items" → no se implementó el contenido de #9990834 bajo el claim de #9990826 (sería tocar un
  item ajeno).

**Corrección aplicada:** se ejecutó el intento de cierre faltante
(`estado_aprobacion='completado'` sobre #9990826). El guard de paraguas del modelo
(`RoadmapItem.php` bloque "(2b) PARAGUAS") lo reenrutó automáticamente a `aprobado_irving` +
`excluir_pool_automatico=true`, sacándolo del pool/reaper hasta que el hook de cierre en
cascada (`RoadmapItem.php:459-491`) lo complete solo cuando #9990834 cierre.

**Sin cambio de código de negocio propio de esta vuelta.** El trabajo técnico real (implementar
y correr `tests/Feature/Modules/Talento/ReaperturaAcusesTest.php` con la receta ya documentada)
sigue en #9990834, pendiente de que el revisor lo tríe y una terminal lo reclame.

Detalle equivalente al de los casos hermanos: mismo mecanismo, mismo guard, misma causa raíz
(cierre-intento faltante tras una descomposición correcta).
