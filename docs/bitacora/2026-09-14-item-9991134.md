## 2026-09-14 13:13 — Aplicar la decisión de Irving sobre la urgencia de #9991116

Item #9991134 (sub-item de seguimiento de #9991122): "Decidir urgencia de #9991116 (12 agujeros
CRÍTICOS de rutas: leak password Mikrotik + toma de cuenta)". Frontera dura de seguridad —
Irving debía elegir, el circuito no decide por su cuenta. Irving ya había resuelto las 4
preguntas estructuradas (q1-q4, todas con la opción 1 recomendada, ver log del item:
`aprobado_irving` en 2026-09-14 12:30 y 13:08 con `respuestas` fijas):

- **q1** — HOTFIX inmediato: #9991116 salta al frente de la cola.
- **q2** — Rotar YA las passwords de Mikrotik potencialmente expuestas por el leak.
- **q3** — Invalidar sesiones/tokens de usuarios sensibles (admin/staff) tras aplicar el fix.
- **q4** — Auditar access logs de los 12 endpoints (30-90 días) antes de cerrar #9991116.

**Bloqueo de proceso encontrado:** `circuito:cabida 9991134` dio NO CABE (`ya_timeouteo_antes` —
un intento previo agotó max-turns sin commitear nada). El protocolo indica descomponer con
`circuito:sub-item`, pero éste rechazó crear hijos: `#9991134` ya está a profundidad máxima (3)
desde su raíz (`9990721 → 9991122 → 9991134`). Decisión registrada (`circuito:reportar
--tipo=decision`, reporte #8256): como la acción real no es código de la aplicación (solo datos
del roadmap, sin git/build), se ejecuta directo en esta vuelta en vez de dejar el item
bloqueado sin salida.

**Hecho (vía tinker, sin tocar archivos de la app):**
1. `RoadmapItem::find(9991116)`: `urgente=true`, `urgente_at=now()`, `urgente_by='circuito:automatico'`
   (mismo efecto que `RoadmapController::urgente()`, `priority` ya estaba en `alta`).
2. `RoadmapCircuitoService::requestDisparo('circuito:automatico','urgente',9991116)` — encoló una
   vuelta inmediata (`circuito_disparos`, origen `urgente`).
3. `RoadmapReportService::append(...)` sobre #9991116 (reporte #8257, tipo `nota`): deja
   registrados como **requisitos de cierre** los 3 puntos de q2/q3/q4 — **sin ejecutarlos aquí**,
   porque rotar credenciales Mikrotik reales o invalidar sesiones activas es frontera dura de
   credenciales/seguridad y requiere primero el detalle técnico exacto de qué se filtró (que sale
   de investigar los 2 endpoints de leak dentro de #9991116, no de este item de priorización).
4. #9991134 cerrado: `decision_resuelta=true`, `decision_resumen`, `decision_fuente='irving'`,
   `estado_aprobacion=completado`, `cierre_sin_codigo_motivo` (justifica la ausencia de
   rama/merge_commit propios: es una acción de gobernanza sobre datos del roadmap, no código de
   la app), `reporte_coloquial` y `enlace_revision`.

**Verificado:** `urgente=true` + fila nueva en `circuito_disparos` con `item_id=9991116`; reporte
#8257 visible en el historial de #9991116; #9991134 en `completado` con `decision_resuelta=true`
tras corregir el primer intento de cierre (lo frenó `JarvisService::verificarCierre` por falta de
`cierre_sin_codigo_motivo`).

Sin cambio de código de aplicación — es una acción de gobernanza del roadmap (marcar urgente +
disparar + dejar constancia de los requisitos de cierre en #9991116). Este archivo de bitácora es
el único artefacto versionado del item.
