# Item #9990877 — Identidad unificada Fase 2 continuación: colaborador_id en tablas legado sellers.id

## 2026-09-11 19:11 — colaborador_id en 6/8 tablas legado + backfill, descomposición del resto

Sub-item de seguimiento de #9990778 (que ya había resuelto `colaborador_id` en `client_main_information`
y `whatsapp_conversations`, las 2 tablas donde `seller_id` resultó ser `users.id`). Este item continúa
con las tablas donde `seller_id` SÍ es `sellers.id` — resolución por doble join.

Irving aprobó las 5 preguntas estructuradas del item, todas con la opción recomendada:
- **q1**: migración aditiva (nullable + FK + índice), sin tocar `seller_id`.
- **q2**: alcance = solo tablas de "impacto directo" en esta fase, dejando "reportes históricos"
  para fase siguiente.
- **q3**: la consolidación de duplicados la decide Irving caso por caso; el circuito solo ejecuta
  el merge ya mapeado.
- **q4**: preservar historial con tabla de auditoría `colaboradores_merges` (from_id, to_id, fecha,
  usuario, snapshot), sin borrar filas espejo.
- **q5**: generar un dry-run/reporte antes de aplicar cualquier consolidación.

### Trabajo ejecutado esta vuelta

Decisión de alcance (registrada con `circuito:reportar --tipo=decision`): de las 8 tablas listadas
en la descripción original (`commissions`, `commissions_rules_sellers`, `discounts`,
`history_sellers_rules`, `payment_by_rule`, `payments_sellers`, `sales`, `transactions_sellers`),
apliqué la migración a las **6 de "impacto directo"** (activamente consumidas por
`ComissionController`, `PaymentSellerController`, `SellerTransactionController`, `DiscountPayment`,
`CommissionRule`, `PaymentByRule`) y **deferí** `history_sellers_rules` (histórico/auditoría por
nombre) y `sales` (descontinuada, 0 filas y 0 consumidores reales de código, ya confirmado en
`docs/vendedores-sales-commissions-prospects-item-9990471-verificacion.md`) a un sub-item, tal como
pedía la q2 aprobada.

- Migración `database/migrations/2026_09_12_010000_add_colaborador_id_legacy_seller_tables_item_9990877.php`:
  agrega `colaborador_id` (nullable, FK `talento_colaboradores` con `onDelete('set null')`, índice)
  después de `seller_id` en las 6 tablas. `down()` reversible. Aplicada en dev.
- Comando `app/Console/Commands/Active/BackfillColaboradorIdSellersLegacyCommand.php`
  (`identidad:backfill-colaborador-id-sellers`): resuelve por doble join
  `sellers.id → sellers.user_id → talento_colaboradores.user_id`, idempotente (solo toca
  `colaborador_id IS NULL` + `seller_id IS NOT NULL`), reporta huérfanos sin adivinar.
- Corrido en dev: `commissions`(0)/`discounts`(0)/`payments_sellers`(0) sin filas;
  `commissions_rules_sellers` 12/12 resueltas, `payment_by_rule` 72/72, `transactions_sellers` 2/2 —
  **86 filas, 100% de cobertura, 0 huérfanas**. Verificado con conteos post-backfill.
- Integrado a `main` vía `circuito:integrar`.

### Descomposición (paraguas)

La parte (2) del item — consolidar duplicados (Guadalupe seller 12/48, cuentas espejo 23/25,
sellers `is_seller=0`/sin alta en Talento) — es frontera dura de dinero (reasignación de atribución
de comisiones/ventas) y el propio item exige NO ejecutarla sin visto bueno explícito caso por caso.
Irving solo aprobó la *metodología* (q3-q5), no el mapeo específico. Creé 2 sub-items:

- **#9990930** — aplicar el mismo patrón a `history_sellers_rules`/`sales` (fase siguiente, ya
  autorizada por q2, solo falta ejecutarla).
- **#9990932** — la consolidación real, con todo el contexto embebido (el dry-run que pedía q5 **ya
  existe**: `docs/identidad-vendedores-diagnostico-8-seller-id-item-9990802.md`, con los 10
  `seller_id` problemáticos en 3 bloques). Bloqueado hasta que Irving confirme el mapeo canónico.

Intenté cerrar el item a `completado`; el guard de paraguas (`RoadmapItem.php` bloque "(2b)
PARAGUAS") lo reenrutó correctamente a `aprobado_irving` + `excluir_pool_automatico=true` — queda
fuera del pool/reaper hasta que ambos sub-items cierren.

**Sin cambio de frontend** — solo migración + comando de consola, `sin_ui=true`.
