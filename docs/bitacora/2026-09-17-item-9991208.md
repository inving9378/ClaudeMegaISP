## 2026-09-17 10:50 — #9991208 Changelog en markdown: formato por columna, render saneado, legacy migrado

**Sesión directa con Irving** (Fase 3 de "cerrar el pipeline de releases"). Ajuste de Irving: nada
de heurística en cada render — columna `formato`.

- `25cd3e8e` — migración aditiva `release_descriptions.formato` (markdown|html, default markdown)
  con backfill de una vez: **48 html** (editor WYSIWYG `input-editor`, etiquetas de bloque sin
  marcadores) / **27 markdown** (15 legacy con `nl2br(e())`, 3 texto plano, 9 markdown limpios).
  `App\Services\Release\ReleaseNotesRenderer`: markdown → CommonMark GFM (`html_input=strip`,
  `allow_unsafe_links=false`); html → HTMLPurifier (lista blanca de texto). `ReleaseDescription::html`
  (apended) es lo único que se pinta con `v-html`. `limpiarGenerador()` quita `- -` y el
  `### Mejoras en esta versión` redundante.
- `67f9ea9d` — `ReleaseController::store` guarda markdown crudo (`formato=markdown`);
  `ReleaseDescriptionController` store/update → `formato=html`; `ReleasesDescription.vue:79`
  pinta `desc.html`; `normalizeReleaseNotes()` pública.
- `82515515` — `releases:migrar-descripciones-legacy {--dry-run}`: solo `formato=markdown` con
  `<br` o entidades; aplica **la misma** `normalizeReleaseNotes()` del paso github_release.
  Dry-run mostrado (15 filas) → aplicado: 15 migradas; segunda corrida: "Nada que migrar".

**Verificación:** XSS de prueba (`<script>`, `javascript:`, `onclick`) eliminado en ambos formatos;
V1.35 (id 87) renderiza `<ul><li><strong>Órdenes de Trabajo:</strong>…` sin `###` ni `<br />`;
una del editor (id 69) sale como `<p>…</p>` limpia; `show()` de la 94 incluye `html`.
Pendiente visual (Irving): `/releases/V1.35-17.09.2026` (detalle) y una legacy (p. ej. V1.32).
