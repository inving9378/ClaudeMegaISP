## 2026-09-11 19:43 — CIRC-02a: diagnóstico solo-lectura de respuestas de Irving perdidas

Item #9990866, sub-item de posición 2 de la descomposición de #9990854 (CIRC-01 en posición 1
verificado `completado`/mergeado antes de arrancar). Solo-lectura, tal como pide el spec: no se
tocó BD ni código de aplicación fuera de `docs/` (se usó un script temporal en `/tmp`, ya borrado).

**Resultado:** el "hoyo" que el item pedía medir está en **0** hoy. De 1,673 items en la tabla,
**209** llegaron alguna vez a `requiere_irving` y recibieron después una decisión humana (vía
`RoadmapController::decidir()`, único endpoint que fija `aprobado_irving`/etc., gateado por
`circuito.decidir`, cualquier login queda como `irving:<login>`). En el **100%** de los 209 casos
la MISMA llamada que trajo la decisión ya movió el estado fuera de `requiere_irving` — nunca hizo
falta un segundo paso manual de re-encolado. Explicación mecánica: solo la acción `comentar` deja
el estado sin tocar, y esa acción **nunca se ha usado** en toda la historia de la BD (0 entradas).
Grupo (b) = 0, sin antigüedad ni trabajo bloqueado que reportar.

Sobreescritura de comentarios (punto 4 del spec): auditados los 12 sitios de código que escriben
`comentarios_claude`; solo 2 reemplazan por completo (`decidir()`/`seguimiento()`, ambos humanos),
los otros 10 (revisor, merge runner, priorizador) siempre concatenan. Ningún camino automatizado
puede borrar un comentario humano. Verificado con el único caso real de 2+ comentarios humanos en
toda la BD (#9990335): el texto humano ("opcion 2") sigue intacto al día de hoy, con las notas
automáticas posteriores agregadas DESPUÉS, no encima.

**Punto ciego real, documentado para no dar el hoyo por cerrado a la ligera:** `elegirOpcion()`
(marcar la opción sin dar clic en Aprobar) no deja NINGUNA entrada en el `log` del item — es
invisible retroactivamente, solo detectable en el momento (item en `requiere_irving` con
`opcion_elegida` fijado). Se revisaron los 4 items que hoy están en `requiere_irving`: uno
(#9990890) tiene `opcion_elegida` puesto, pero por el **autopilot**, no por un humano (verificado
en su `log`) — no es un caso real. Los otros 3 no tienen opción elegida. 0 candidatos hoy, pero la
vía en sí sigue sin instrumentar — es la justificación de fondo para CIRC-02b (#9990867).

Pista falsa descartada y documentada: `decision_resuelta`/`decision_fecha` no sirven como proxy —
237 de 238 casos son del guard de nivel-C-sin-merge (semántica distinta, nada que ver con
`requiere_irving`), y el único con `decision_fecha` real (#753) lo escribió a mano una terminal del
circuito para su propia re-verificación, no el flujo de Irving.

Reporte completo en `docs/circuito-diagnostico-respuestas-perdidas-item-9990866.md`. Item cerrado
`completado` sin rama (solo-lectura, sin código de aplicación que integrar) — commit del propio
reporte en `main` vía `circuito:integrar`.
