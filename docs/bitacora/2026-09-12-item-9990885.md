## 2026-09-12 01:25 — Item #9990885 (CIRC-02c, cadena #9990874) — CANCELADO por duplicado exacto

**Item:** CIRC-02c: Visibilidad — hilo en la Torre + watchdog de respuestas sin consumir.
Sub-item de `#9990882` (CIRC-02 PARAGUAS, cadena raíz `#9990874`), depende de CIRC-02b `#9990884`.

### Qué se encontró

Al leer el item se confirmó que ya es un caso conocido y resuelto de gobierno del roadmap: el
mismo trabajo CIRC-02a/b/c está duplicado en **3 cadenas paralelas**, generadas por 3 items raíz
distintos que Irving aprobó por separado en una ventana de ~30 minutos sin que el circuito
detectara la duplicación semántica (hallazgo documentado en `#9990903`):

| Cadena raíz | CIRC-02a | CIRC-02b | CIRC-02c |
|---|---|---|---|
| `#9990856` (la viva) | `#9990857` completado, merge `2c23bad4` | `#9990858` → descompuesto en `#9990899` (migración+modelo, **YA MERGEADO**, commit `ce471c61`), `#9990900`/`#9990901`/`#9990902` | `#9990859` `aprobado_revisor`, **sin reclamar — vía canónica de este trabajo** |
| `#9990854` | `#9990866` duplicado | `#9990867` duplicado | `#9990868` duplicado |
| `#9990874` | `#9990883` completado (re-investigó lo mismo que `#9990857`) | `#9990884` cancelado (duplicado, merge `df6d8d2b`) | **`#9990885` (este item)** |

Irving ya aprobó explícitamente en `#9990903` (2026-09-11 19:01:08, opción recomendada del
DES-TRABE Opus, Opción A): *"Conservar cadena `#9990856` (la que ya avanzó) y cancelar las otras
dos"* — es decir, cancelar `#9990866/867/868` (cadena `#9990854`) y `#9990882/883/884/885` (cadena
`#9990874`), con nota de duplicado y referencia cruzada al item vivo. Mi hermano `#9990884`
(CIRC-02b de esta misma cadena) ya ejecutó exactamente esta misma cancelación para su equivalente.

### Qué se hizo

Ejecuté esa parte ya aprobada de la decisión para `#9990885` (mi item, "un item = un dueño" — no
toqué `#9990882` ni ningún otro item de las cadenas duplicadas):

- `estado_aprobacion = 'cancelado'`, `status = 'cancelled'` (vía tinker, replicando el patrón real
  de `RoadmapController::decidir(accion=cancelar)`: `cierreManualIrving=true` para que el guard de
  "cancelación con rama sin mergear" no interfiera — no aplicaba de todas formas, `branch` era
  `null`).
- `cierre_sin_codigo_motivo` + `sin_ui`/`sin_ui_motivo` poblados (cierre administrativo sin código
  ni pantalla — bookkeeping del propio roadmap).
- `comentarios_claude` con el detalle completo de la investigación y la cadena de autoridad de la
  decisión (Irving vía `#9990903`).
- Entrada de log `evento: cancelado_duplicado` con `decision_de: "irving:admin (via #9990903,
  2026-09-11 19:01:08)"`.
- `circuito:reportar --tipo=decision` (no decompongo, cancelo) + esta bitácora + `--tipo=cierre`.

### Qué NO se hizo (a propósito)

No cancelé `#9990882` (paraguas propio de esta cadena, ya parqueado por otra vuelta) ni ningún otro
item de las cadenas `#9990854`/`#9990874`. Tampoco toqué el bug de raíz (dedupe por
título/similitud al crear items, q2 de `#9990903`). El trabajo real de este item (hilo de
respuestas en la ficha de la Torre + watchdog de 60 min + hallazgo del eje 3 del auditor + contador
en el tablero) queda pendiente en `#9990859` — nadie lo ha reclamado todavía.

### Verificación

`tinker` confirma el estado final: `estado_aprobacion=cancelado`, `status=cancelled`,
`branch=null`, log con la entrada `cancelado_duplicado`. Sin cambio de código de negocio (no
aplica: es cierre administrativo, no una feature).
