## 2026-09-16 18:43 — Seguimiento de #9991178: q1/q2 ya implementadas, q3 no aplica (RESUELTO — sin cambio de código)

`#9991181` es el seguimiento auto-generado al cerrar `#9991178` («Fase 2 — Calibrar el tope de
40% de sesión de Carlos + config») con 3 preguntas sin `opcion_elegida`. Ya llegó con las 3
respuestas registradas por `irving:david_marsal`. Verificado por hash
(`RoadmapItem::claveOpcion()`, `sha1` del texto normalizado, 16 chars) cuál opción se eligió en
cada una:

- **q1** (valor del tope) → Opción 1, "Mantener 40% como tope duro".
- **q2** (dónde vive la config) → Opción 1, "Archivo config del circuito (versionado)".
- **q3** (acción al cruzar el tope) → Opción 1, "Auto-compact + continuar sesión".

q1 y q2 **ya están implementadas**: `#9991178` (mergeado, `31805a3f`) dejó exactamente eso en
`config/circuito.php` (`terminal.tope_sesion_carlos_pct=40`, sección versionada en git) +
`deploy/README-calibracion-tope-carlos.md`. Nada que tocar ahí.

q3 **no aplica**: la misma pregunta de fondo ("qué pasa cuando Carlos cruza el tope") ya tiene
una respuesta fijada y más autoritativa, más arriba en la cadena — `#9991175` (item raíz) q4, ya
aprobada por Irving: **"Bloqueo duro"** (rechaza la corrida, mensaje real de límite de Claude,
anti-bypass). El item de build todavía sin construir `#9991179` (Fase 3+4, bloqueado hasta que
Fase 1 tenga el paso root activado) repite esa misma decisión textual en su spec. Las opciones de
q3 en este seguimiento (auto-compact / cerrar-y-reabrir / alertar-y-decidir) tienen pinta de
plantilla genérica de "tope de sesión" adjuntada sin adaptar al hecho de que `#9991178` (su
padre inmediato) nunca pidió construir ninguna acción de enforcement — solo calibrar el número y
escribir el config.

Dado que implementar "auto-compact" aquí habría contradicho una decisión ya fijada y con más
detalle (mensaje real + anti-bypass) sobre el MISMO tema, se consultó a Thomas
(`circuito:consultar`) presentando las dos lecturas posibles: (A) tratar q3 como plantilla
genérica mal encajada, sin tocar nada — decisión vigente sigue siendo bloqueo duro; (B) tratar
q3 como una actualización real de política que reemplazaría el bloqueo duro en el spec de
`#9991179`. **Thomas: PROCEDE con la opción A** (recomendada, reversible, fuera del conjunto de
escalamiento).

**Cierre:** sin cambio de código — `#9991178` ya entregó su trabajo real; la decisión de
enforcement correcta (bloqueo duro) sigue viva en `#9991179`, sin tocarla. Detalle completo,
tabla de hashes y las 3 citas textuales relevantes (`#9991175`, `#9991178`, `#9991179`) en
`docs/circuito-carlos-tope-seguimiento-item-9991181-verificacion.md`.
