## 2026-09-11 19:20 — CIRC-03 Fase B: clasificar los 10 items conocidos con motivo_espera + barrido de candidatos

Item #9990905 (sub-item de seguimiento de #9990886, Fase B de CIRC-03). Depende de la Fase A
(#9990904, columna `motivo_espera` en `roadmap_items`) que ya estaba mergeada a `main` (commit
`a94fd956`) y migrada en dev antes de empezar.

**Comando nuevo** `circuito:clasificar-motivo-espera` (`app/Modules/Addons/Roadmap/Console/ClasificarMotivoEsperaCommand.php`,
registrado en `ModuleServiceProvider`), con dos pasos idempotentes:

1. **Conocidos** — aplica un mapeo fijo verificado contra el texto REAL de cada item (no el
   supuesto al escribir #9990886, que puede haber cambiado):
   - #683 → `credencial` (PAC CFDI 4.0, bloqueado por credenciales de Irving)
   - #692 → `sesion_presencial` (OLT Huawei Fase A, validar en lab con Irving presente)
   - #283 → `decision` (título literal "Decisión de negocio: ¿priorizar driver ZTE y/o V-SOL?" —
     prioricé 'decision' sobre 'hardware' porque la pregunta de negocio es el bloqueo primario, tal
     como pedía el propio spec del item ante la ambigüedad)
   - #671 → `hardware` (el #283 YA decidió — según su propia descripción, "Irving decidió (item
     #283, q2) conseguir hardware piloto…" — solo falta el piloto físico, no queda decisión abierta)
   - #9990075 → `credencial` (Hook DNS-01, falta credencial + delegación NS)
   - #691 → `autorizacion` (cuota Medussa, "BLOQUEADO sin autorización legal/CNBV")
   - #718 → `decision` (bloqueo primario: "Irving debe elegir explícitamente antes de construir
     nada" sobre la opción A/B/C de #661; el segundo bloqueo, acceso a megafamilia-rn, no tiene
     valor en la taxonomía y es secundario — la decisión es el gate real)
   - #661 → `decision` (mismo título lo dice: "decisión de alcance/plataforma pendiente con Irving")
   - #9990571 → `frontera_produccion` (anotación sobre RemoteDeployCommand/PROD, frontera dura ya
     resuelta por Irving en #9990491 pero el tema sigue siendo PROD)
   - #724 → `frontera_produccion` (ventana de monitoreo en prod con dual-write ON)

   Los 10 ya tenían `excluir_pool_automatico=true` de antes (no hacía falta tocarlo); solo
   `motivo_espera` estaba NULL. Corrido: 10/10 clasificados. Corrido una 2ª vez: 0 nuevos, 10
   "ya estaban" — confirma la idempotencia.

2. **Barrido** — decisión de Irving en el brief del propio item (q2, opción 1 recomendada): el
   barrido SOLO PROPONE, nunca escribe `motivo_espera` por su cuenta (aplicar requiere una pasada
   de confirmación aparte). Implementado así: el comando busca en `aprobado_irving`+`requiere_irving`
   con `motivo_espera` NULL por los patrones de texto que cita el propio spec ('bloqueado por',
   'requiere credencial', 'falta hardware', 'pendiente de autorización', + sinónimos evidentes:
   'decisión de irving', 'decisión pendiente', 'decisión de negocio', 'sesión presencial', etc.) y
   solo los imprime en consola — cero escritura en BD.

   **Ajuste hecho en esta misma vuelta:** la primera corrida incluía también el patrón 'frontera
   dura' y dio 166 candidatos; medido patrón por patrón, 153 de esos 166 eran SOLO por esa
   etiqueta, que el propio circuito usa para marcar casi cualquier item escalado a Irving por
   tocar dinero/permisos/prod/seguridad — no es una señal de que ESE item esté esperando algo
   externo específico (ver CLAUDE.md, convención de frontera dura). Se quitó del arreglo de
   patrones; sin ella el barrido da **65 candidatos**, un universo mucho más confiable. Como de
   cualquier forma el barrido nunca escribe nada (decisión de Irving), no importa si el total es
   >15 o no para la acción de esta vuelta — pero se deja documentado para quien reclame la
   siguiente pasada.

**Hallazgo de duplicación (no se tocó, no es mi item):** existe una segunda cadena
(#9990860→#9990910→#9990911/#9990913) que llegó independientemente a la misma idea — columna
`motivo_espera` + backfill de los mismos 10 items + barrido de candidatos adicionales. #9990910 ya
se auto-documentó como paraguas parqueado (ver `docs/bitacora/2026-09-11-item-9990910.md`) tras
confirmar que la migración ya existía por duplicado (#9990904/#9990914). Con el trabajo de esta
vuelta, **#9990911 y #9990913 quedan ya satisfechos** (backfill de los 10 + barrido de candidatos,
ambos hechos aquí) — quien los reclame solo necesita verificarlo y cerrarlos como duplicado, sin
rehacer nada. De la otra cadena (#9990904→#9990914→#9990915), **#9990915** ("exponer motivo_espera
en el modelo + e2e") también está ya satisfecho: el `$fillable` con el comentario de los 6 valores
ya se agregó en el propio commit de #9990904 (`7c811799`).

No se creó un sub-item nuevo para "aplicar los 65 candidatos del barrido": #9990913 ya existe con
ese propósito exacto — crear otro hubiera sido más duplicación, no menos.

**Verificación:** `php -l` limpio en los 2 archivos tocados, `php artisan --version` bootea,
comando corrido 2 veces (idempotencia confirmada), conteo de patrones medido uno por uno antes de
decidir cuál quitar.

Commits en la rama `circuito/item-9990905-circ-03-fase-b-clasificar-los-8-items-c`:
- `feat(roadmap): comando circuito:clasificar-motivo-espera (CIRC-03 Fase B)`
- `fix(roadmap): quita 'frontera dura' del barrido de motivo_espera (demasiado ruidoso)`
