## 2026-09-12 00:27 — Identidad unificada, Fase 2 (parcial): colaborador_id en client_main_information y whatsapp_conversations

Item #9990778 (Ventas/Talento, nivel de riesgo B, `depende_de=[9990801]`). #9990801 (Fase 1,
diagnóstico solo-lectura) ya estaba `completado` al reclamar este item, con reporte en
`docs/identidad-vendedores-inventario-seller-id-item-9990801.md`: confirmó que
`client_main_information.seller_id` y `whatsapp_conversations.seller_id` son en realidad
`users.id` (no `sellers.id`), resuelto vía triangulación de 3 consumidores de código + datos
reales.

**Hecho esta vuelta:**
- Migración aditiva: `colaborador_id` (nullable, FK `talento_colaboradores.id`) en
  `client_main_information` y `whatsapp_conversations`, sin tocar `seller_id`.
- Comando idempotente `identidad:backfill-colaborador-id` — resuelve por JOIN determinista
  `talento_colaboradores.user_id = seller_id` (nunca por nombre ni por ID cruzado de entornos).
  Corrido en dev: 1856/4195 filas de `client_main_information` resueltas, 0 pendientes en
  `whatsapp_conversations`. 0 discrepancias verificadas.
- 2339 filas quedan `colaborador_id=NULL` a propósito (Irving 2127, TERE 75, kathya 113, otros
  36) — son `users.id` sin `talento_colaboradores` correspondiente, reportadas, no adivinadas.

**NO hecho (descompuesto en sub-items):**
- #9990877 — colaborador_id en las 8 tablas donde `seller_id` sí es `sellers.id` + decisión de
  consolidar duplicados (Guadalupe, cuentas espejo) — frontera dura de dinero/comisiones,
  requiere consulta explícita a Thomas/Irving antes de tocar esas filas.
- #9990878 — Fase 3 (doble escritura), depende de #9990877.
- #9990879 — Fase 4 (corte de lectura), depende de #9990878.

Detalle completo, plan de reversa y estado de las 4 preguntas originales del item en
`docs/identidad-vendedores-fase2-colaborador-id-item-9990778.md`.

**Commits:** `1e98365d` (migración) + `18cd10c2` (comando backfill), rama
`circuito/item-9990778-identidad-unificada-colaborador-unico`.
