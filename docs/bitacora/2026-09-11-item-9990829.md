# Item #9990829 — VoIP Fase 3b: runbook de despliegue

## 2026-09-11 21:14 — Creación de `docs/voip/RUNBOOK-fase3.md`

Sub-item de seguimiento de #9990825, depende de #9990718/#9990824 (ya mergeados). Tarea
de documentación pura: escribir el runbook operativo de cómo la actualización de MegaISP
provisiona Asterisk en un servidor sin instalar, tomando los pasos ya investigados y
verificados en `docs/bitacora/2026-09-10-item-9990718.md` (no se re-investigó nada desde
cero).

**Confirmado antes de escribir** (para no adivinar, mismo error que causó los 12 defectos
originales): se leyó el código real de `ProvisionadorAsterisk::PASOS` (la constante que "es
el contrato") para sacar el orden exacto de los 15 pasos, en vez de confiar solo en la
prosa de la bitácora fuente. Se confirmó también en `config/requisitos-voip.php` los
detalles de `esquema_realtime`, `transporte`, `espacio_minimo_mb` y el principio de
verificación en dos canales del sha256; y en `ProvisionarAsteriskCommand.php` las banderas
reales (`--descubrimiento`, `--nueva`, `--estado=`, `--desinstalar --confirmar=`).

**Ubicación:** `docs/voip/RUNBOOK-fase3.md` — decisión ya tomada por Irving en la pregunta
q5 del item padre #9990825 (Opción 1: `docs/voip/`), citada literalmente en la descripción
de este sub-item.

**Contenido del runbook** (232 líneas):
1. Qué hace la actualización (principio: la actualización lleva las instrucciones, un
   sitio de descarga lleva los archivos).
2. Pre-checks (espacio, conectividad, sudoers con su gotcha de bandera obligatoria,
   `esquema_realtime` ya fijado, warm-up previo).
3. Los 15 pasos en el orden REAL (tabla completa, con el porqué de cada orden que corrige
   uno de los 12 defectos: `crear_base` antes de `generar_config`, `credenciales` antes de
   `config`, `alembic_version_config` en vez de `alembic_version`, menuselect por
   categoría real, etc.) + rango de duración 5-90 min (sin inventar un número fijo, como
   pedía el item).
4. Tabla de los 16 criterios de aceptación, con estado y método de verificación.
5. Warm-up (`config:clear && route:clear && queue:restart`, nunca `config:cache`).
6. Verificación de endpoints VoIP: UI `/voip/extensiones`, `pjsip show endpoints` contra
   la central real, `verificar-llamada.sh` (tonos 440/880 Hz), segunda corrida idempotente.
7. Rollback: revertir el tag + `down()` de las migraciones aditivas de esta fase (todas
   confirmadas con `dropIfExists`/guard `hasTable`, ninguna destructiva) + la opción
   `--desinstalar --confirmar=SI-BORRAR-ASTERISK` para dejar el servidor limpio (marcada
   explícitamente como destructiva y solo para pruebas, nunca con extensiones reales).
8. Troubleshooting: tabla sintoma → dónde mirar, mapeada a los 12 defectos ya corregidos,
   para que si reaparece una variante no se re-investigue desde cero.

**Verificación de este sub-item:** el archivo existe en `docs/voip/RUNBOOK-fase3.md`, cubre
los pasos reales en el orden correcto (no el ingenuo), y la tabla de 16 criterios está
completa — tal como pedía el criterio de aceptación del propio item.

**Ejecución:** rama `circuito/item-9990829-voip-fase-3b-runbook-de-despliegue-de`, commit
`4a3aaa79` (solo el archivo nuevo, add selectivo). Sin código PHP/Vue tocado — verificación
fue `php artisan --version` (bootea limpio) al no haber sintaxis de aplicación que testear.
Integración encolada vía `circuito:integrar 9990829`. Item cerrado a `completado` con
`reporte_coloquial` y `enlace_revision` poblados. 0 decisiones de criterio no triviales
más allá de confirmar la ruta ya decidida por Irving (q5) y el alcance ya decidido (q4) —
ambas ya resueltas en el item padre, no hubo que decidir nada nuevo.
