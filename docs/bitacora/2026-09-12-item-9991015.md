## 2026-09-12 03:25 — CIRC-05 rate limit dedicado de creación (10/min + tope 60/hora)

Item #9991015, sub-item de seguimiento de #9990871/#9990888 (CIRC-05 PASO 2). Al reclamarlo
(wt-2) la rama `circuito/item-9991015-circ-05-rate-limit-dedicado-de-creacio` ya traía el
trabajo completo de una sesión previa (wt-5), pausado por colisión en vuelo con #9990971 sobre
`app/Modules/Addons/Roadmap/routes.php` (candado #438) — al llegar mi turno #9990971 ya estaba
mergeado a `main`, así que la colisión estaba resuelta sola.

**Cambio (3 commits ya en la rama, sin tocar código propio):**
- `config/roadmap_externo.php`: nuevas claves `rate_create_min` (default 10, env
  `ROADMAP_EXTERNAL_RATE_CREATE_MIN`) y `rate_create_hora` (default 60, env
  `ROADMAP_EXTERNAL_RATE_CREATE_HORA`).
- `app/Modules/Addons/Roadmap/routes.php`: las 3 rutas de CREACIÓN (`POST /{token}/item`,
  `GET /{token}/crear/{modulo}/{tituloB64}/{specB64?}`, `POST /{token}/item/{id}/reporte`) se
  movieron a su propio grupo con **doble throttle** (`roadmap-externo-crea-min` por minuto +
  `roadmap-externo-crea-hora` por hora, buckets de caché independientes por el 3er parámetro de
  `ThrottleRequests::handle()`). `updateItem`/`setItem`/`setItemPath*` **sin cambio**, siguen en
  `rate_write` (30/min compartido).
- `tests/Feature/Modules/Roadmap/RoadmapExternalCreateTest.php`: caso 6 actualizado para agotar
  la cuota real de `rate_create_min` (antes leía `rate_write`, que ya no aplica a estas rutas).

**Verificación de esta vuelta:** `php -l` limpio en los 3 archivos (via `git show` a temporales,
sin necesidad de checkout — la rama ya estaba activa en el worktree de wt-5); diff completo
confirmó que el grupo de `rate_write` quedó intacto (solo 3 rutas, ninguna de creación) y que las
3 rutas de creación quedaron en el grupo nuevo; `circuito:estado-item` confirmó "libre, sin
colisión". Encolado el merge con `circuito:integrar` → aplicado por el runner on-box, commit
`1affdcc42f8d1219928e2f3e21b6defea42851f6` en `main`.

**Cierre:** `estado_aprobacion=completado`, `reporte_coloquial` + `enlace_revision` poblados
(marcado `sin_ui=true` — es un endurecimiento de rate limit de un endpoint API interno del
circuito, sin pantalla asociada; se verifica con `RoadmapExternalCreateTest` + `route:list`, no
en el navegador).
