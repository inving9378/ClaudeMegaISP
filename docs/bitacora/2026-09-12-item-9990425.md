## 2026-09-12 19:24 — FASE 4a-2: autolimpieza en isPaused() + candado de regresión (3 casos) — RESUELTO sin código

Item #9990425 (sub-item de #9990417) pedía: (1) cambiar `RoadmapCircuitoService::isPaused()`
para que, con el freno activo, consulte `FrenoCircuito::expirado()` y lo quite si venció antes de
devolver `true`; (2) extender `FrenoFueraDeLaBaseTest.php` con 3 casos de comportamiento (sin
`expira_en` / con `expira_en` pasado / con `expira_en` futuro).

Verificado contra el código real de este worktree (ya sincronizado con `main`, `git diff main`
sin diferencias en ambos archivos): **todo el trabajo ya estaba hecho**.

- `RoadmapCircuitoService::isPaused()` (líneas 150-171) ya tiene exactamente el cambio pedido —
  commit `3b443dc5` ("FASE 4a (#9990417): isPaused() autolimpia el freno con expira_en vencido"),
  Irving MegaISP, 2026-09-06 17:04:56.
- `FrenoFueraDeLaBaseTest.php` ya tiene los 3 casos (`test_freno_sin_expira_en_nunca_se_autolimpia`,
  `test_freno_con_expira_en_pasado_se_autolimpia`, `test_freno_con_expira_en_futuro_sigue_activo`)
  — commit `ad65a898` ("FASE 4a (#9990417): 3 casos de test para expira_en del centinela"), mismo
  autor, mismo día.

Corrida real: `php -l` de los 3 archivos limpio; `php artisan test --filter=FrenoFueraDeLaBaseTest`
→ **7 passed (26 assertions)** — exactamente "4 viejos + 3 nuevos", el criterio de cierre literal
del item.

Causa: carrera de timing — una sesión previa de Irving hizo FASE 4a-1 y 4a-2 completas en un solo
tramo, commiteando bajo la referencia del item padre (#9990417) casi al mismo tiempo en que el
circuito generaba y aprobaba el sub-item específico (#9990425). Mismo patrón que #9990003,
#9990353, #9990658.

Detalle completo en `docs/circuito-fase4a2-ispaused-item-9990425-verificacion.md`.

**Sin cambio de código.** Item cerrado como `completado` con reporte coloquial + enlace de
revisión, sin rama ni merge (no hay diff que integrar).
