## 2026-09-12 13:31 — Fase 1c CRUD select Borrador/Publicada: ya estaba resuelto en main

Item #9990586 ("Fase 1c — CRUD: select Borrador/Publicada en form de plantilla + validación",
sub-item de seguimiento de #9990578) llegó a `wt-1` como trabajo nuevo, dependiendo de las Fases
1a (#9990584) y 1b (#9990585), ambas ya `completado`.

Verificado contra `main`: las 3 piezas que pedía el item ya estaban aplicadas desde el
2026-09-07 (commits `32551280` validación+controller, `3ecf6320` campo en el catálogo del form),
hechas de un tirón cuando se trabajó el padre `#9990578` antes de decomponerse en 1a/1b/1c.
Confirmado con `git merge-base --is-ancestor` que ambos commits ya son ancestros de la rama
actual.

- `DocumentTemplateCreateRequest`/`UpdateRequest` ya validan `status => nullable|in:borrador,publicada`.
- `DocumentTemplateController::store()`/`update()` ya respetan "default en alta, no pisar en
  edición" (`$request->filled('status')`).
- El módulo usa formulario 100% DB-driven (`TemplateManager.vue` + `Module::getfields()`) — la
  migración de Fase 1b/1c ya insertó la fila `status` (select-component, `include=1`) en el
  catálogo del módulo `DocumentTemplate` (id=61), así que el select ya se pinta solo, sin tocar
  ningún `.vue`.

Verificación funcional en tinker (con limpieza): crear sin `status` → `borrador` (default de
columna); editar con `status` → persiste; editar sin `status` → no se pisa.

Detalle completo en
`docs/document-template-status-select-item-9990586-verificacion.md`. **Sin cambio de código de
aplicación** — solo se documenta el hallazgo (mismo patrón de carrera de timing que #9990585,
#9990353, #9990658, etc.).
