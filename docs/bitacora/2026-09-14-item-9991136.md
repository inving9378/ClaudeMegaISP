## 2026-09-14 12:05 — #9991136 nginx dev: gzip para JSON/texto de la API

**Sesión directa con Irving.** Nivel B (config de host, todo el sitio de dev). Decisión de
Irving: gzip primero, deuda #9991129 después.

### Antes (medido)
- `/etc/nginx/nginx.conf:46` `gzip on;` pero `gzip_types` comentado (`:53`) → solo `text/html`.
- `GET /api/roadmap/items` (sesión admin, en proceso): crudo **2,269,129 bytes (2.16 MB)**; gzip
  nivel 1 = 0.51 MB · nivel 4 = **0.44 MB** (40 ms) · nivel 6 = 0.42 MB (91 ms) · nivel 9 = 0.41 MB.
- `curl` con y sin `Accept-Encoding: gzip`: `/js/app.js` **14,147,309 bytes en ambos casos**, sin
  `Content-Encoding` — el bundle de 14.1 MB también viaja crudo (JS comprime ~4×: la ganancia
  mayor es esta). `/api/roadmap/items` sin sesión = 401 de 30 bytes (por debajo de
  `gzip_min_length`, no aplica).

### Cambio versionado (`c96cc886`, merge `ddb3a806`)
- `deploy/nginx-gzip-dev.conf` — drop-in para `/etc/nginx/conf.d/`: `gzip_types` (json, js,
  plain, css, xml, rss, svg — lista blanca por MIME, sin imágenes/pdf/zip), `gzip_min_length
  1024`, `gzip_comp_level 4`, `gzip_vary on`, `gzip_proxied any`.
- `deploy/README-nginx-gzip-dev.md` — instalación (V1 `nginx -t` antes del reload), validación
  (V2/V3 curl, V5 `roadmap:medir-hoja-de-ruta`) y reversión (`rm` + reload, sin downtime).

### Pendiente (root — lo corre Irving)
```bash
sudo install -m 0644 /var/www/megaisp/deploy/nginx-gzip-dev.conf /etc/nginx/conf.d/gzip-dev.conf
sudo nginx -t && sudo systemctl reload nginx
```
Después: V2/V3 con `curl`, V4 en el navegador, V5 con `php artisan roadmap:medir-hoja-de-ruta`
(debe reportar gzip nivel 4 y ~0.44 MB; los ms no cambian).
