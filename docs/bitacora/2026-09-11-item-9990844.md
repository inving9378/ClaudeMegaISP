## 2026-09-11 22:40 — Auditoría READ-ONLY de las ~40 ramas circuito/item-<id>-* huérfanas (item #9990844)

**Alcance de esta vuelta** (fijado por el DES-TRABE al re-aprobar el item el 2026-09-11 16:32 como
`tecnico_seguro`): **solo** producir este inventario detallado, sin mergear, rescatar ni borrar
ninguna rama. El propio item tiene 2 preguntas `requiere_irving` sin resolver:

- **q1** — "¿qué hacer con las ~40 ramas?" → Opción 1 recomendada = "generar reporte detallado y
  presentarlo a Irving para triage manual antes de tocar nada". **Este documento ES esa Opción 1,
  ya ejecutada.**
- **q2** — "¿cómo prevenir que se acumulen?" → sigue sin resolver, es una decisión de proceso
  aparte (tocar el circuito), fuera del alcance read-only de esta vuelta. Queda para que Irving la
  decida por separado.

### Método

1. `php artisan circuito:auditar-ramas-huerfanas` sobre las 1435 ramas locales `circuito/item-*`
   → 201 items con más de una rama.
2. Filtrado exacto con el criterio que el propio item #9990844 define como "huérfana": **(a)** la
   rama NO es la registrada hoy en `item->branch`, **(b)** NO está contenida en `main`
   (`main..rama` > 0 commits), **(c)** tiene ≥1 commit único. Resultado: **41 ramas candidatas en
   39 items** (coincide casi exacto con el "~40" que estimaba la descripción del item).
3. Para cada una: `git log` de sus commits únicos (autor/fecha/mensaje), `git diff --stat` contra
   `main`, y una verificación heurística — ¿los archivos que la rama tocó **ya existen hoy en
   `main`** bajo el mismo path? Si sí, es señal fuerte de que el trabajo se resolvió por otra vía
   (categoría 1 del propio item). Si no, se investigó a mano (grep de la clase/tabla en todo el
   repo, `Schema::hasTable`, búsqueda de renombres) antes de concluir nada.

### Hallazgo #1 (el más importante) — Item #652: feature real, sin mergear, **item TODAVÍA ABIERTO**

Dos ramas huérfanas del mismo item, ambas construyen la misma feature en 2 fases ("bandeja de
Irving en lenguaje natural: resumen legible arriba, lo técnico en un desplegable"):

- `circuito/item-652-bandeja-lenguaje-natural` (2 commits, 2026-08-28)
- `circuito/item-652-preguntas-en-llano` (4 commits, 2026-08-28, incluye los 2 anteriores)

El item #652 en la Hoja de Ruta sigue **`pending` / `aprobado_irving`** (NO completado) y su
`item->branch` está vacío (`-`) — es decir, ni siquiera hay una rama "oficial" registrada hoy;
estas dos son las únicas que existen.

**Verificado que el trabajo NO está en `main`:**
- `app/Modules/Addons/Roadmap/Console/ResumenNaturalCommand.php` — **no existe**.
- `app/Modules/Addons/Roadmap/Services/ResumenNaturalService.php` — **no existe**.
- Las 2 migraciones (`add_resumen_natural_to_roadmap_items`, `add_preguntas_natural_to_roadmap_items`)
  — **no existen como archivo** en `main`.
- `tests/Unit/Modules/Addons/Roadmap/PreguntasNaturalNoCambiaClavesTest.php` — no existe.

**Pero las columnas `resumen_natural`, `resumen_natural_at`, `preguntas_natural`,
`preguntas_natural_at` SÍ EXISTEN hoy en la tabla `roadmap_items`** de la BD compartida de dev
(confirmado con `Schema::getColumnListing`). Es decir: en algún momento se corrió `migrate` estando
parado en una de estas ramas (aplicó el cambio de esquema a la BD compartida), pero el código que
las usa (el servicio que traduce el ticket a lenguaje llano) **nunca llegó a `main`** — quedó fuera
cuando la rama se abandonó. Hoy esas columnas están mudas en la BD (nadie las escribe ni las lee).

`RoadmapController.php`, `ModuleServiceProvider.php` y `TorreControl.vue` sí existen en `main`,
pero eso solo confirma que son archivos compartidos que ya existían antes — no implica que el hunk
específico de esta feature esté aplicado ahí.

**Esto es información nueva para Irving:** el item #652 sigue abierto y hay trabajo real (con
tests) esperando en estas 2 ramas. Antes de decidir "descartar" u "rescatar" (q1), vale la pena
que alguien revise si esta feature sigue siendo deseada — el resumen coloquial de la bandeja de
Irving en lenguaje llano.

### Hallazgo #2 — Item #595: módulo Inversiones con 14 tablas de esquema sin mergear

`circuito/item-595-modulo-inversiones-fase-1-nucleo-ri` (2 commits, 2026-08-20, 634 líneas, 20
archivos) construye el núcleo del módulo Inversiones: scaffold + **14 migraciones** (assets,
portfolios, orders, strategies, backtests, risk_events, etc.) + vista dashboard.

El **id #595 fue reutilizado** por el proceso de reconstrucción `[Reconstruido #1009]` — hoy ese id
en la Hoja de Ruta es un item distinto y ya cerrado ("Items con canal de respuesta correcc[ión]"),
sin ninguna relación con Inversiones. No hay ningún item vivo que reclame esta rama.

**Verificado en `main`:** el módulo Inversiones **sí existe** (`module.json`, `config/inversiones.php`
con 44 líneas, provider, routes, controller de dashboard) pero **sin las 14 migraciones** de esta
rama ni la vista `dashboard.blade.php` de la rama — es decir, lo que hay en `main` es un scaffold
distinto/posterior, y el modelo de datos completo (14 tablas) de esta rama específica **no está
aplicado**. Nota de contexto (ya documentada en `CLAUDE.md`): Inversiones es un proyecto personal
de trading de Irving, sin relación con el circuito — cuánto de su modelo de datos sigue siendo
válido es una decisión suya, no algo que se pueda inferir del código.

### Hallazgo #3 — Item #171: infraestructura de auditoría de tool-calling del Chat IA, huérfana y sin item que la reclame

`circuito/item-171-9-fase-2-chat-ia-ejecuta-acciones-too` (1 commit, 2026-08-08) agrega
`app/Models/IaToolCall.php`, la migración `create_ia_tool_calls_table`, `config/ia.php` (versión de
ese momento) y una entrada en `.env.example` — infraestructura para auditar qué "tool calls"
ejecuta el Chat IA.

El **id #171 también fue reutilizado**: hoy es un item ya cerrado y sin relación
("Guardrail de migraciones falla abierto en la ruta web de Ignition").

**Verificado en `main`:** `app/Models/IaToolCall.php` no existe, la tabla `ia_tool_calls` **no
existe** en el esquema de la BD (`Schema::hasTable` = false), y `config/ia.php` en `main` es un
archivo completamente distinto (mapa de `integraciones_detectables` para el panel de contexto de
IA, sin relación con tool-calling). Ningún grep de `ToolCall`/`tool_call` en todo `app/` da
resultados. **Esta pieza de auditoría de tool-calling nunca se integró y hoy no tiene ningún item
vivo pidiéndola** — queda a discreción de Irving si sigue siendo necesaria.

### El resto (36 ramas) — verificadas, ya resueltas por otra vía

Para cada una se confirmó, con evidencia concreta (no solo "el item dice completado"), que el
contenido ya está cubierto en `main`:

| Item | Rama | Evidencia de que ya está resuelta |
|---|---|---|
| #1020 | ventana-de-reversibilidad-medida-en-fila | `ReleaseReversibilityThreshold.php`/`ReleaseSnapshot.php` **sí existen** en `main` (7/7 archivos) |
| #159 | blocked-negocio-deuda-dos-tablas-de-f | El doc de análisis (`docs/deuda-invoices-vs-client-invoices.md` en la rama) ya está cubierto por `docs/deuda-facturas-invoices-item-632-plan-ejecucion.md` + `docs/deuda-facturas-invoices-item-159-plan.md`, ya en `main` |
| #171* | ver Hallazgo #3 | — |
| #174 | visibilidad-de-modulos-fase-4b-bloque | 3/3 archivos presentes en `main` |
| #196 | whatsapp-fase-3-a-reiniciar-desconectar | 4/4 archivos presentes |
| #212 | whatsappiaservice-reimplementa-su-propio | 2/2 archivos presentes |
| #224 | captura-de-pago-en-mostrador-sin-idempot | 3/3 archivos presentes |
| #232 | clientes-http-propios-a-claude-leyendo-c | 2/2 archivos presentes |
| #271 | get-client-status-expuesto-a-cualqu | 4/4 archivos presentes |
| #282 | flotas-y-gestionred-montan-su-propio-lea | 4/5 archivos presentes |
| #289 | inventario-assigntouser-y-changestore-n | 2/2 archivos presentes |
| #57 | modulo-olt-propio-multi-marca-audit | El doc (`docs/auditoria-modulo-olt-multimarca.md` en la rama) ya está cubierto por `docs/AUDITORIA_OLT_MULTIMARCA_2026-07-15.md`, en `main` |
| #595* | ver Hallazgo #2 | — |
| #652* | ver Hallazgo #1 | — |
| #752 | doc-corporativa-fase-33-mapa-leaflet-wt2 | 3/3 archivos presentes |
| #794 | env-a-config-resto-no-critico-depl | 15/15 archivos presentes |
| #809 | fase3c-tabla-final-v2 | 1/1 (doc de bitácora, ya integrado) |
| #835 | modulemanager-declara-sus-endpoints-en | 1/1 archivo presente |
| #844 | triaje-match-por-substring-sin-distingu | 1/1 (doc de bitácora) |
| #850 | core-documentos-dar-permisos-granulares | 1/1 archivo presente |
| #863 | agregar-item-ventana-de-deshacer-de-15s | 3/3 archivos presentes |
| #865 | fase-b-global-scope-de-alcance-propio | 5/5 archivos presentes |
| #872 | mapa-de-las-seis-listas-de-terminos-del | 1/1 (doc de bitácora) |
| #881 | torre-de-control-fase-3-catalogo-de-a | 2/3 archivos presentes |
| #882 | torre-de-control-fase-5-terminales | 5/6 archivos presentes |
| #920 | flotas-ui-de-documentos-de-conductor-s | 3/4 archivos presentes |
| #923 | catalogo-puestos / expediente-rh-catalogo-de-puestos-prop | `TalentoPuesto.php` y los 11 archivos de ambas ramas (idénticas) están en `main` |
| #935 | diagnostico-item-service-v2 / diagnostico-wt1 | Ambas ramas (mismo commit) — `RoadmapItem.php` con el fix presente |
| #939 | backend-890-orden-real-de-la-cola-en | 1/1 (doc de bitácora) |
| #943 | torre-config-reorganizar-el-panel-por-a | Archivo renombrado por el rename JARVIS (#650): `ThomasService.php` → `JarvisService.php`, que **sí tiene** el método `pendienteReal()` de esta rama |
| #977 | configroute-permissionphp-paths-de-up | 1/1 archivo presente |
| #978 | retirar-el-fallback-legado-config-view-s | 2/2 archivos presentes |
| #984 | ipv6-11-registro-del-modulo-addonsipv | El módulo Ipv6 en `main` es un scaffold más avanzado (incluye `Models/`) que reemplazó a este |
| #989 | obsoleta-pre-refactor-verificarvuelta | El comando se movió a `app/Console/Commands/Circuito/VerificarVueltaCommand.php`, con lógica de revertir/escalar equivalente y más desarrollada |
| #9990337 | mr-06a-5-portar-proyectscontroller-s | `ProyectsController.php` presente en `main` |
| #9990569 | build-atomico-fase-1-parametrizar-mix-**superseded** | El propio nombre de la rama ya lo dice ("superseded"); `MIX_PUBLIC_PATH` no está en `main` — reemplazado por un enfoque distinto |
| #9990647 | talento-completar-documento-formula-2 | 6/6 archivos presentes |
| #9990649 | talento-ui-endpoint-de-firma-por-slot | Archivo presente con contenido ampliado (88 menciones de "firma", muy por encima del commit original) |

\* Ver Hallazgos #1/#2/#3 arriba — estas 3 SÍ requieren una decisión explícita, no son
descartables sin más.

### Recomendación para Irving (no ejecutada aquí — corresponde a q1/q2)

- **36 de las 41 ramas** (todos los items de la tabla sin `*`) están verificadas como ya resueltas
  por otra vía — son borrado seguro cuando Irving lo autorice explícitamente (Opción 1 del propio
  item: el reporte se presenta, la decisión final de borrar sigue siendo suya).
- **3 ramas necesitan su atención antes de tocarlas:**
  - `circuito/item-652-preguntas-en-llano` (la más completa de las 2 de #652) — feature real de
    lenguaje llano en la bandeja, item todavía abierto. Si sigue siendo deseada, es candidata a
    reconstruir/mergear con revisión (las columnas de BD ya están puestas).
  - `circuito/item-595-modulo-inversiones-fase-1-nucleo-ri` — 14 migraciones del modelo de datos de
    Inversiones (proyecto personal), decidir si ese diseño de esquema sigue vigente.
  - `circuito/item-171-9-fase-2-chat-ia-ejecuta-acciones-too` — auditoría de tool-calling del Chat
    IA, sin item vivo que la pida hoy; decidir si aún se quiere.
- **q2** (cómo prevenir la acumulación futura) sigue sin resolverse — es una decisión de proceso
  separada, no se tocó código del circuito en esta vuelta.

Nada se borró, mergeó ni modificó en ninguna rama existente. El único cambio de esta vuelta es este
documento.
