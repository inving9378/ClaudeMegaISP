## 2026-09-14 18:05 — F7c: documentar la regla de publicación en CLAUDE.md y megaisp-conventions

Item #9991144 (sub-item de seguimiento de #9990678, épica #9990668 "Emisión atómica de
versiones"). Tarea puramente documental: dejar escrita la regla textual de Irving
"si se hizo una versión se debe publicar" en los dos puntos de consulta del repo.

**Hecho:**
1. `CLAUDE.md` — sección nueva "### Regla de publicación de versiones (item #9990678, épica
   #9990668)" insertada justo antes de "### Portal Cliente — estado al 2026-06-15": cita
   textual + contexto (V1.34/V1.33/V1.16-V1.19 quedaron en dev sin publicarse) + qué implica
   desde la F3 de la épica (`ReleasePublisherService`, item #9990674 — emisión atómica con
   compensación) + el permiso Spatie separado `releases.emitir` (items #9990678/#9991141) que
   gatea el botón "+ Nueva versión" y la ruta `POST /releases/store`.
2. `.claude/skills/megaisp-conventions/SKILL.md` — línea puntero bajo "## Caché y deploy",
   mismo tono terso que el resto de la sección, remitiendo a CLAUDE.md para el contexto
   completo.

**Nota de precisión:** al momento de escribir esto, F3 (`ReleasePublisherService`, item
#9990674) y F7a (permiso `releases.emitir`, item #9991141) siguen `aprobado_irving` sin
mergear — el texto describe el diseño de la épica y remite al estado real de esas piezas en
sus propios items de la Hoja de Ruta, en vez de afirmar que ya están activas en `main`.

Sin cambio de código de aplicación, solo estos dos archivos de documentación. Un commit
(`fdf846f2`), mergeado a `main` (`f1a241f6`).
