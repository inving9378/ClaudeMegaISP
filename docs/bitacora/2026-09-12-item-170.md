## 2026-09-12 16:35 — Item #170: freno de mano fuera de la BD (RESUELTO — ya implementado, gap de cierre)

Item reclamado por `wt-1`. Investigado: el fix pedido (centinela en archivo +
`isPaused()` fail-closed) ya estaba **implementado y mergeado a `main`** desde el
2026-08-25, commit `b426bc42` ("feat(circuito#170): el freno de mano sale de la base —
centinela en archivo, fail-closed"), 7 minutos después de que el revisor lo autorizara.
Fue un commit directo de Irving a `main` que nunca actualizó el estado del item en el
roadmap — gap de cierre, mismo patrón ya visto en #9990003/#9990353/#9990658.

Verificado punto por punto contra el código real (`FrenoCircuito.php`,
`RoadmapCircuitoService::isPaused()`, `config/circuito.php`, `SchedulerCommand.php`):
centinela por existencia de archivo (no por contenido), escritura atómica tmp+rename,
ruta absoluta (nunca `storage_path()`, por el problema de worktrees independientes),
`isPaused()` archivo-primero + `settings` de respaldo + try/catch fail-closed con log
en archivo, y el chequeo corre DENTRO del lazo del pool continuo, no solo al arrancar.
Todo cubierto. Detalle completo en
`docs/circuito-freno-archivo-item-170-verificacion.md`.

Hallazgo aparte: la rama `circuito/item-170-vendedores-renderizar-saldo-null-como`
traía un commit huérfano (`94454ef6`/`c827fffb`, jul-2026) de una encarnación anterior
de este mismo ID de item (tema distinto: saldos null de vendedores). Se dejó fuera del
cierre por no pertenecer al alcance actual — reseteada la rama a `main` antes de
commitear la documentación; el contenido viejo sigue recuperable si alguien quiere
retomarlo aparte.

**Sin cambio de código.** Cerrado documentando la verificación.
