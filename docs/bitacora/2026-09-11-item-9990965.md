## 2026-09-11 21:53 — Fase 3d: activar el flag de doble escritura en dev + validación E2E + cerrar #9990878

Item #9990965, último sub-item de #9990878 (Identidad unificada — Fase 3). Las Fases 3a
(#9990962, mapeo de callsites), 3b (#9990963, observer + parches raw + flag OFF por default) y
3c (#9990964, `identidad:verificar-consistencia` + KPI card) ya estaban `completado` y mergeadas
a `main` antes de reclamar este item.

Una vuelta previa (wt-2) había intentado activar el flag y quedó bloqueada: reportó que
`/var/www/megaisp/.env` es un symlink compartido por todos los worktrees y el sandbox lo
clasifica como "Modify Shared Resources" (bloquea Edit y Bash por igual). Escaló a Thomas, que
la escaló a Irving; Irving revisó y aprobó las 3 preguntas estructuradas del item (activar ahora
+ smoke funcional por query directa + flag ON en dev / OFF por default en config), dejando el
item en `aprobado_irving` para que la siguiente terminal lo ejecutara.

**Hallazgo de esta vuelta:** en `wt-1` el `.env` NO es un symlink — es un archivo regular propio
del worktree (`stat`: `meganet:meganet 640 fichero regular`), así que el bloqueo de sandbox de
wt-2 no aplicó aquí. Se pudo escribir el flag directamente sin necesitar bypass ni intervención
de Irving.

**Pasos ejecutados (sin código propio — coincide con lo previsto en el spec del item):**
1. `identidad:verificar-consistencia` (estado base): 5611 filas en `client_main_information`,
   4195 con `seller_id`, 1856 con `colaborador_id` poblado, **drift=0**, cobertura resoluble
   100%. 2339 huérfanas estructurales (seller_id sin `talento_colaboradores` correspondiente:
   users.id=8→2127, =4→113, =6→75, =11→14, =3812→5, =1→4, =401→1).
2. Se agregó al `.env` de este worktree: `IDENTIDAD_DOBLE_ESCRITURA_COLABORADOR=true` (no
   versionado — vive solo en `.env`, `config/identidad.php` sigue con default `false`).
3. Warm-up: `config:clear && route:clear && queue:restart` (sin `config:cache`). Confirmado
   `config('identidad.doble_escritura_colaborador_id')` → `true`.
4. Validación E2E (tinker, dentro de una transacción con `DB::rollBack()` — sin dejar datos de
   prueba en dev):
   - Alta con `seller_id=4212` (resoluble) → `colaborador_id=2` autopoblado por el observer
     (`creating`). ✅
   - Alta con `seller_id=8` (huérfano estructural conocido) → `colaborador_id` queda `NULL`, el
     alta NO se bloquea, y se registra una fila en `identidad_colaborador_id_pendientes`
     (`seller_id=8, tabla=client_main_information`). Nota: en `creating()` el `registro_id`
     queda `NULL` a propósito (el id aún no existe en ese punto del ciclo de vida) — es el
     comportamiento documentado en el propio `ColaboradorIdResolver`, no un defecto de esta fase.
   - Update de `seller_id` de un registro existente (path `updating()`) → el pendiente sí trae
     `registro_id` correcto (verificado igual con `seller_id=8`).
   - Path raw de `SmartImportService::injectColaboradorIdBridge()` probado por reflexión directa
     sobre un batch sintético: resuelve `seller_id=4212→2`, deja `NULL` para `8` y para
     `seller_id=null`, y **no pisa** un `colaborador_id` ya explícito en la fila. Confirma el
     mismo comportamiento que el observer sin necesitar simular un import HTTP completo.
5. `identidad:verificar-consistencia` corrido de nuevo tras las pruebas: **idéntico al estado
   base** (5611 filas, drift=0) — el rollback no dejó residuo, consistente con el criterio de
   aceptación de #9990878 (100% de los NUEVOS quedan consistentes; no se creó ningún registro
   real nuevo en esta vuelta que pudiera divergir).

**Sin código propio para commitear** (el `.env` no se versiona; los comandos usados ya existen
desde 3a/3b/3c) — esta bitácora es el único artefacto de la rama del item.

**Cierre:** #9990965 → `completado`. Cascada esperada: al cerrar el último sub-item de #9990878
(que está parqueado como paraguas en `aprobado_irving`), el hook de `RoadmapItem::saved()` lo
completa automáticamente.
