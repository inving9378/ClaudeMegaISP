## 2026-09-16 12:33 — Intento de cierre del paraguas raíz: parqueado por el gate de cierre #1005 (no por el guard "(2b) PARAGUAS")

#9991182 es el paraguas RAÍZ de la auditoría/corrección de responsive de todo el sistema
(disparador: header/topbar global que corta el avatar y no escala SVGs/nombre al resize). El
propio `prompt` dice explícitamente "PARAGUAS — no lo trabajes directo": se descompone en fases
y nadie debe intentar arreglarlo todo en una sola vuelta.

Al reclamarlo, ya tenía sus 2 hijos directos creados por sesiones previas:
- **#9991183** — Fase 1 (fix del header global): `completado`, mergeado (`df7207a4`).
- **#9991184** — Fase 2 (inventario read-only): NO `completado` (sigue `aprobado_irving`), pero
  tiene `archivado_at` puesto (su propia rama se mergeó a main sin código real, por ser
  "solo lectura") y a su vez es paraguas de sus propios 4 nietos (`origen_item_id=9991184`):
  #9991185 (Grupo A, `en_progreso` en `wt-3` — con dueño, no se toca), #9991186 y #9991187
  (`requiere_irving`, sin reclamar) y #9991188 (`aprobado_revisor`, consolidación final,
  depende de los 3 anteriores). Ninguno de los 4 está cerrado.

**Variante respecto al patrón habitual (#738/#830/#816/... ver `CLAUDE.md`):** el log de
#9991182 no tenía ningún intento de cierre previo (a diferencia de la mayoría de esos casos),
así que esta vuelta ejecutó el primer intento. Pero `RoadmapItem::subItemsAbiertos()` filtra por
`whereNull('archivado_at')` — como #9991184 SÍ tiene `archivado_at` (aunque su propio trabajo
siga incompleto en sus nietos), `tieneSubItemsAbiertos()` de #9991182 devolvió `false` y el
guard "(2b) PARAGUAS" **no disparó**. En su lugar, el intento de completar cayó en el gate de
cierre más nuevo (#1005, `jarvis:verificarCierre`): #9991182 nunca tuvo rama/`merge_commit`
propios (es puramente coordinador, sin código directo) ni `cierre_sin_codigo_motivo` ni
`enlace_revision`/`sin_ui_motivo` — eso cuenta como `bloqueante` (cierre hueco, el mismo patrón
que causó el incidente #9990366), así que el gate lo frenó igual: reenrutado a `aprobado_irving`
+ `excluir_pool_automatico=true`, con `worker_sid`/`claimed_at` liberados.

**Resultado neto:** el mecanismo de guard que disparó fue distinto al habitual, pero el efecto es
el mismo que busca la corrección estándar — #9991182 queda fuera del pool/reaper, sin marcarse
`completado` en falso, y solo lo cerrará el hook de cierre en cascada cuando #9991184 (y, en
cascada, sus 4 nietos) cierren de verdad. No se forzó el cierre con datos falsos
(`enlace_revision`/`cierre_sin_codigo_motivo` inventados) porque el trabajo real de Fase 2 sigue
genuinamente incompleto.

**Sin cambio de código de aplicación** — el trabajo técnico real (Fase 2: inventario por grupos +
consolidación, y cualquier Fase 3+ de corrección que salga de ahí) sigue en
#9991185/#9991186/#9991187/#9991188, pendientes de que sus dueños los cierren.
