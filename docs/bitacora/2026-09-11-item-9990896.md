# Item #9990896 — Fase 2a (confirmar con evidencia real si colisión es causa frecuente de terminales ociosas)

## 2026-09-11 18:57 — bucle reap sobre paraguas ya descompuesto (mismo patrón que #738/#745/#830/.../#9990856)

Mismo patrón que la larga familia documentada en `CLAUDE.md` (#738, #745, #830, #816, #818, #848,
#852, #905, #878, #906, #907, #924, #9990012, #917, #910, #936, #9990408, #962, #9990554,
#9990549, #9990624, #9990650, #9990733, #9990740, #9990807, #9990826, #9990836, #9990856): una
vuelta previa (`wt-5`, 2026-09-11 18:55 CST) ya hizo lo correcto — corrió `circuito:cabida` (NO
CABE, `historico_excede_umbral`), confirmó con `date` + grep directo que
`storage/logs/circuito-despacho-*.log` seguía sin existir en ningún worktree ni en el checkout
principal a las 18:54:23 CST, y en vez de fabricar una conclusión sin datos descompuso el reintento
en **#9990916** ("Fase 2a (reintento) — medir colisión... con >=15 min reales acumulados",
bloqueado hasta después de 2026-09-11 19:09 CST). Pero esa vuelta murió (`soltar-claim`,
"muerte del proceso: kill, OOM o freno a media vuelta") antes de intentar **cerrar** al padre — el
pool lo repartió de nuevo (a `wt-4`, esta vuelta) sin trabajo propio que hacer.

Verificado esta vuelta:
- Hora real del sistema al tomar el item: `2026-09-11 18:56:02 CST` — **todavía antes** del umbral
  de 19:09 CST que exige el propio spec. No se podía medir nada real todavía; hacerlo habría sido
  exactamente el "fabricar una conclusión sin datos" que el item prohíbe.
- `php artisan circuito:cabida 9990896 --sid=wt-4` → `CABE [ya_descompuesto]` — según el propio
  código (`JarvisService::caberEnVuelta()`, comentario líneas 1328-1330): "Idempotente: si el item
  YA se descompuso ... no vuelve a evaluar — decir 'cabe' aquí solo significa 'no re-descompongas',
  el guard de paraguas del modelo ya se encarga de que no se complete mientras le queden sub-items
  abiertos." Confirma que la única acción pendiente era el intento de cierre, no más código.
- `#9990916` sigue intacto (`pendiente_revision`, `worker_sid=null`), con el spec completo y
  correcto (los 4 pasos originales + instrucción de repetir el patrón si el tiempo sigue sin
  cumplirse) — la descomposición original de `wt-5` seguía siendo correcta, nadie más la tocó.

**Corrección:** se ejecutó el intento de cierre faltante
(`estado_aprobacion='completado'` vía tinker). El guard de paraguas
(`RoadmapItem.php`, bloque "(2b) PARAGUAS") lo reenrutó a `aprobado_irving` +
`excluir_pool_automatico=true` (log: `paraguas_abierto`, "le quedan 1 sub-item(s) abierto(s)"),
liberando `worker_sid`/`claimed_at` y sacándolo del pool/reaper hasta que el hook de cierre en
cascada (`RoadmapItem.php:459-491`) lo complete solo cuando `#9990916` cierre.

**Sin cambio de código** — la medición real (grep de `circuito-despacho-*.log` cruzado con
`colision_pausada_por`) sigue pendiente en `#9990916`, bloqueada hasta que pasen los >=15 min
reales exigidos (no antes de 2026-09-11 19:09 CST).
