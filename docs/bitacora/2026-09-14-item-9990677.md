## 2026-09-14 12:05 — F6: reconciliar la historia (versiones fantasma)

Item #9990677 (F6, épica #9990668 "Emisión atómica de versiones: una versión existe si y sólo
si está publicada"). Continuación de una vuelta previa que agotó `max_turns` con 1 commit ya en
la rama (`cde57cec`): la migración `2026_09_14_190000_marca_versiones_fantasma_historicas.php`,
que marca 54 versiones SOLO_TABLA (del cruce de F0, `releases:reconciliar` #9990669) como
`historica_no_publicada` con su motivo (47 esquema legado `AAAA.MM.DD.N` pre-tags, 7 intentos de
emisión que no completaron el pipeline).

**Lo que faltaba y se completó esta vuelta:**

1. **Corrí la migración pendiente.** Requirió `--force-uncommitted` porque el guardrail de
   migraciones destructivas (#1018) marca cualquier `->change()` como sospechoso — en este caso
   es un ensanche puro (`estado_publicacion` de `varchar(20)` a `varchar(40)`, ya documentado en
   el propio archivo de migración como no-destructivo: el valor `historica_no_publicada` mide 22
   caracteres, no cabía en 20).
2. **Verifiqué con datos reales.** `releases:reconciliar --json` contra git+GitHub confirma:
   54 SOLO_TABLA (ahora todas con `estado_publicacion=historica_no_publicada`) + 29 PUBLICADA
   (sin tocar, no son fantasma) + 1 TAG_SIN_RELEASE (V1.2.1).
3. **V1.2.1 es la única versión que de verdad exige la excepción del propio item** ("Solo te
   detienes si vas a PUBLICAR RETROACTIVAMENTE algo, que es reescribir historia"): su tag ya es
   público en `origin`, solo falta el objeto GitHub Release. Publicarlo retroactivamente vía API
   es justo el caso que el item ordena escalar, no decidir. Creado el item de respuesta
   **#9991143** (`[RESPUESTA] F6 — V1.2.1: publicar Release retroactivo o marcar histórica`,
   `tipo=respuesta`, `nivel_riesgo=B`, `requiere_irving`) con las 2 opciones y recomendación
   (publicar, porque el tag ya es público — no hay snapshot nuevo que exponer).

**Resultado:** `circuito:integrar` mergeó la rama a `main` (`6da94208`), pero el guard de
paraguas del modelo (`RoadmapItem.php`, bloque "(2b) PARAGUAS") interceptó el intento de marcar
`completado` porque #9991143 seguía abierto — reenrutó el item a `aprobado_irving` +
`excluir_pool_automatico=true` (mismo patrón documentado en CLAUDE.md para #738/#745/#830/etc.).
Es el comportamiento correcto: el item se completará solo, por el hook de cierre en cascada,
cuando Irving resuelva #9991143 y ese hijo cierre. `reporte_coloquial`/`enlace_revision` quedaron
poblados para cuando eso ocurra.

Sin cambio de código de negocio nuevo en esta vuelta (la migración ya la había escrito la vuelta
anterior); el trabajo de esta vuelta fue ejecutarla, verificarla contra datos reales, y separar
correctamente la única decisión que de verdad requería a Irving.
