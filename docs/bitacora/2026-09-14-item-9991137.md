## 2026-09-14 12:05 — #9991137 Medidor mecánico del disparador de #9991129

**Sesión directa con Irving.** Nivel A. Pedido al ratificar #9991092: que el chequeo del
disparador de la deuda #9991129 sea mecánico, no "sentir lenta la Torre".

- `8f044bd6` — `php artisan roadmap:medir-hoja-de-ruta` (`Console/MedirHojaDeRutaCommand.php`,
  registrado en `ModuleServiceProvider`): corre `RoadmapController::index()` en proceso con un
  usuario `super-administrator` (resuelto por rol, nunca por id), y reporta **(a)** universo
  (`COUNT roadmap_items`), **(b)** bytes gzip del payload al `gzip_comp_level` real de nginx
  (lee `/etc/nginx/nginx.conf` + `conf.d/*.conf`; sin directiva = 1) y **(c)** mediana de ms de
  N corridas. Umbrales por opción (`--umbral-items=4000 --umbral-mb=2 --umbral-ms=1500`),
  `--json` para máquinas, **exit 1 si alguno se cumple**. Merge `5e300fa9`.
- Verificado: tabla + exit 0 con los umbrales reales; exit 1 forzando `--umbral-ms=100`;
  `--json` válido. Corrida desde el checkout principal tras el merge: 1,787 items · 0.51 MB gzip
  (nivel 1, aún sin el drop-in de #9991136) · ~550–600 ms → 🟢 ninguno.
- Línea base del 2026-09-14 y el disparador de 3 condiciones quedaron en el cuerpo de #9991129.
