## 2026-09-11 19:11 — Migración aditiva motivo_espera + proyección en COLUMNAS_BANDEJA (bucle reap sobre paraguas ya descompuesto)

Item #9990910 llegó ya con una descomposición correcta hecha por una vuelta previa
(`wt-4`) que murió sin cerrar: investigó y confirmó que la migración de `motivo_espera`
ya existía DOS VECES (hecha por `#9990904`/`#9990914`, ambos mergeados a `main`), sin
índice, y que la proyección en `RoadmapController::COLUMNAS_BANDEJA` (Fase C, diferida
a propósito por el comentario de la propia migración) nunca se hizo. Creó dos sub-items
correctos: `#9990926` (agrega el índice faltante, migración aditiva nueva) y `#9990927`
(proyecta `motivo_espera` en `COLUMNAS_BANDEJA` + verifica `torre()`). Pero nunca
intentó cerrar al padre.

Esta vuelta (`wt-2`) reverificó todo (migración en main, sub-items intactos sin
reclamar, `COLUMNAS_BANDEJA` aún sin el campo) y ejecutó el intento de cierre que
faltaba: `estado_aprobacion='completado'` → el guard de paraguas del modelo lo
reenrutó a `aprobado_irving` + `excluir_pool_automatico=true`, liberando
`worker_sid`/`claimed_at`. El item sale del pool/reaper hasta que `#9990926` y
`#9990927` cierren los dos.

Detalle completo en `docs/roadmap-bucle-reap-item-9990910-verificacion.md`.

**Sin cambio de código de aplicación.** El trabajo real (índice + proyección en la
Torre) sigue en `#9990926`/`#9990927`, pendientes de que una terminal los reclame.
