## 2026-09-16 19:20 — Vendedores solicita artículos — FASE 1 frontend: bloqueada, diferida a sub-item (sin código)

**Item:** #9991191 (sub-item de #9991176, hermano de FASE 2 backend #9991190 y FASE 3
notificaciones #9991192).

**Qué se pidió:** botón "Solicitar artículo" + modal + botón "Cancelar" propia solicitud en la
pestaña Artículos de la ficha del vendedor (`resources/js/components/module/sellers/inventory_items/index.vue`),
consumiendo 2 endpoints nuevos que debía dejar listos la FASE 2 backend (#9991190):
`POST /inventory/inventory_item_stock/request_item_by_vendor` y
`.../cancel_own_request/{id}`.

**Hallazgo al reclamar el item:** el propio sub-item de FASE 2 backend (#9991190) fue
**cancelado por Irving** (`irving:david_marsal`, 2026-09-16 13:13, sin comentario), después de
que la válvula de contexto lo marcara nivel C (`veredicto: accion` — modifica
`config/route_permission.php` agregando rutas nuevas dentro de arrays de permisos existentes).
Verificado con grep directo sobre `app/Modules/` y `routes/`: los 2 endpoints **no existen en
main** — solo existen los ya usados hoy (`accept_item_by_movement`, `reject_item_by_movement`).

**Por qué no se construyó el frontend igual:** el propio texto de #9991191 (Fase 0 ya
investigada) anticipaba este escenario exacto y daba dos caminos sin necesidad de picar código a
ciegas: verificar si el endpoint ya existe en main (no existe) y, si no, usar
`circuito:sub-item` para dejar el trabajo pendiente, o consultar a Thomas si el bloqueo es real.
Construir la UI contra endpoints inexistentes habría dejado un botón/modal no funcional — no es
"duda de implementación" resoluble con la regla de oro, es una dependencia dura ya **decidida y
rechazada por Irving**, no una que un ejecutor deba re-intentar por su cuenta.

**Acción tomada:**
1. `circuito:reportar --tipo=decision` documentando el bloqueo y la decisión de no construir a
   ciegas (reporte #8312).
2. `circuito:sub-item` → creado **#9991193** ("FASE 1 (retomar): frontend, bloqueado hasta que
   FASE 2 backend se resuelva"), con toda la investigación de #9991190 referenciada (columna
   `requested_by_user_id`, patrón `movementable_from_type=System`, endpoints sugeridos, permisos
   ya existentes) para que el próximo ejecutor no la repita, y una instrucción explícita de NO
   reconstruir el backend por su cuenta (fue decisión de Irving) sino verificar primero si ya se
   re-aprobó, y si no, consultar a Thomas antes de repetir el ciclo.
3. Intento de cierre de #9991191 (`estado_aprobacion = completado`) → el guard de paraguas
   (`RoadmapItem.php` bloque "(2b) PARAGUAS") lo reenrutó correctamente a `aprobado_irving` +
   `excluir_pool_automatico=true`, liberando `worker_sid`/`claimed_at` — queda parqueado sin
   loopear en el pool/reaper hasta que #9991193 cierre.

**Sin cambio de código de aplicación** — el trabajo real de FASE 1 (botón/modal/cancelar) sigue
en #9991193, bloqueado hasta que Irving decida sobre FASE 2 backend (#9991190).
