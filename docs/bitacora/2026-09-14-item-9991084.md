## 2026-09-14 11:12 — Item #9991084: bucle reap sobre paraguas ya descompuesto (se completa el cierre-intento faltante)

Mismo patrón que #738/#745/#830/#816/#818/#848/#852/#905/#878/#906/#907/#924/#9990012/#917/#910/
#936/#9990408/#962/#9990554/#9990549/#9990624/#9990650/#9990807/#9990826/#9990836/#9990856/
#9990892/#9990896/#9990886/#9990893/#9990878/#9990870/#9991072, documentado extensamente en
CLAUDE.md.

#9991084 (deuda: `roadmap_items.subtasks` sobrecargada con dos formas — lista de sub-tareas de la
UI vs el objeto `{"descomposicion":{"depende_de":[…]}}` que escribe `circuito:sub-item`) ya había
sido descompuesto correctamente por una vuelta previa (`wt-1`, 2026-09-14 11:08-11:10), justo
después de que Irving aprobara las 2 preguntas estructuradas del item (q1: ejecutar las 3 fases en
ramas separadas; q2: down() con snapshot previo). Esa vuelta corrió `circuito:cabida` (NO CABE, ya
había timeouteado antes por max_turns) y partió el trabajo en:

- **#9991089** — Fase 1: migración `roadmap_items.descomposicion` json nullable + backfill
  idempotente de los 492 items afectados, con down() que copia de vuelta y drop.
- **#9991090** — Fase 2: `SubItemCommand::117` y `DependenciaGate` leen/escriben la columna nueva
  (depende de #9991089).
- **#9991091** — Fase 3: normalizar `RoadmapCircuitoService::1089` (payload roadmap-externo →
  Cowork) y los 5 endpoints crudos restantes de `RoadmapController`, o dejar el blindaje del
  #9991082 si no hay consumidores reales (depende de #9991089 y #9991090).

Pero esa vuelta murió antes de intentar **cerrar** al padre — el log solo registra `soltar-claim`
("La vuelta de wt-1 terminó sin cerrar el item... muerte del proceso: kill, OOM o freno a media
vuelta. Se libera el reclamo y vuelve a la cola como aprobado_revisor"). Irving ya había aprobado
el item minutos antes (11:08:51, `aprobado_irving`), así que el pool lo volvió a repartir a `wt-1`
sin trabajo propio que hacer más allá de completar el cierre que faltaba.

**Verificado esta vuelta:** los 3 hijos (`origen_item_id=9991084`) siguen intactos —
`pendiente_revision`, sin `worker_sid`, sin reclamar — la descomposición original seguía siendo
correcta, nadie más la tocó.

**Corrección:** se ejecutó el intento de cierre faltante (`estado_aprobacion='completado'`); el
guard de paraguas (`RoadmapItem.php` bloque "(2b) PARAGUAS") lo reenrutó a `aprobado_irving` +
`excluir_pool_automatico=true` (evento `paraguas_abierto` en el log: "le quedan 3 sub-item(s)
abierto(s)"), liberando `worker_sid` y sacándolo del pool/reaper hasta que el hook de cierre en
cascada (`RoadmapItem.php:459-491`) lo complete solo cuando #9991089, #9991090 y #9991091 cierren
los tres.

**Sin cambio de código de negocio** — el trabajo técnico real (columna `descomposicion`, backfill
de los 492, SubItemCommand/DependenciaGate, normalización de RoadmapCircuitoService y los 5
endpoints crudos) sigue en #9991089/#9991090/#9991091, pendientes de que una terminal los reclame.
