## 2026-09-14 10:10 — #9991082 Hoja de ruta no pinta: `subs.filter is not a function`

**Sesión directa con Irving** (no vino por el pool; item creado con `en_desarrollo_humano`).

### Causa raíz (una línea)
`roadmap_items.subtasks` carga dos formas incompatibles: la lista de sub-tareas de la UI y, desde
`circuito:sub-item` (`SubItemCommand.php:117` / `DependenciaGate`), el objeto
`{"descomposicion":{"depende_de":[…]}}` — 492 items desde el 2026-09-04; `c96b81c0` (#9990890,
viernes 09-11 19:45) pasó la Hoja de ruta de `?vista=backlog` (~4 items) al universo completo y
`RoadmapTab::lastAdvance` hizo `subs.filter` sobre ese objeto.

### Por qué "apareció el sábado"
No fue un commit del Árbol. `/var/www/megaisp` (lo que sirve `dev.meganett.com.mx`) cambió del
branch VoIP a `main@648ff00c` el 09-11 23:45 y su bundle se recompiló el 09-13 09:51 — ahí entró
`c96b81c0` al JS. Los commits del sábado ni siquiera estaban en disco: el checkout principal
llevaba desde el 09-10 sin sincronizarse (ver #9991086).

### Hipótesis confirmada
**H1** (backend serializa objeto donde la vista espera lista; dos usos peleando por la misma
llave). Variante aplicada: exponer las dos formas por separado, sin tocar DependenciaGate ni el
Árbol.

### Cambios
- **Capa 1 (raíz) `3a5d2815`** — `RoadmapController::normalizarSubtasks()/separarSubtasks()`:
  `subtasks` SIEMPRE lista + `descomposicion` aparte, en `index/show/store/update/start/complete`
  (todo lo que `RoadmapTab.replaceItem` reinyecta en la lista) y en `itemDetallePayload`. Solo en
  memoria (`setAttribute`, sin `save`); columna y DependenciaGate intactos.
- **Capa 2 (blindaje) `1a125f64`** — `RoadmapTab.subtasksDe(item)` (lista tal cual; objeto →
  `Object.values(data ?? obj)`; null → `[]`) usada por `subtasksDone/subtasksPct/lastAdvance` y el
  template; misma regla en `RoadmapItemDetalle.vue`.
- Merge a `main`: `c0d8ce1f` (runner del circuito, `circuito:integrar`).

### Verificación
- tinker sobre `index()`/`show()` autenticado: **1751 items, 0 con `subtasks` no-lista, 492 con
  `descomposicion`, 69 con sub-tareas / 1682 sin**; BD intacta (fila #9990283 sigue con su objeto).
- Node ejecutando el código literal de `RoadmapTab` sobre el payload real: payload viejo + código
  viejo revienta en **492**; Capa 2 sola **0**; Capa 1 sola **0** (ni el código viejo revienta);
  ambas: 0, 69 con barra / 1683 sin (V5 en ambos sentidos).
- `npm run prod` en el checkout principal (semáforo, publicación atómica) + `view:clear`,
  `route:clear`, `view:cache`.
- **Pendiente de Irving:** V1–V4 visuales (Ctrl+F5 en /releases: Hoja de ruta, Árbol, Panorama,
  Integración, Terminales, consola limpia). No hay navegador desde esta sesión.

### Fuera de alcance (registrado)
- **#9991084** (deuda): columna `subtasks` sobrecargada — endpoints crudos sin normalizar
  (`cancel/updateSubtasks/toggleSubtask/addLog/cancelarDisparo`, `RoadmapCircuitoService:1089`
  = roadmap-externo), `PATCH …/subtasks` borraría `depende_de`, fix estructural = columna propia
  + backfill de los 492.
- **#9991086** (urgente): `MergeRunner::syncCheckoutPrincipal()` nunca ha sincronizado (status
  después de `update-ref`: 0 ok / 74 sucio / 190 omitido) y el rebuild post-merge no corre desde
  el 09-09 → dev sirvió 3 días de código viejo. Hoy se sincronizó a mano (2 `git stash`
  recuperables en `/var/www/megaisp`) y se recompiló.
