## 2026-09-12 17:16 — Fase 4 (C3/C4/C5): leer el log de despacho y decidir tope de concurrencia/frenos

Sub-item de seguimiento de #9990863. Precondiciones verificadas: Fase 2 (#9990892) y Fase 3
(#9990893) ambas `completado` con `merge_commit` ancestro de `main`; log real del canal
`circuito_despacho` con ~17.5h de datos (2026-09-11 23:46 → 2026-09-12 17:13, 5,974 líneas), leído
del checkout principal (`/var/www/megaisp/storage/logs/circuito-despacho-*.log`) porque cada
worktree tiene su propio `storage/` sin ese archivo — el scheduler que lo escribe corre en el
checkout principal. Solo LECTURA ahí, sin editar/artisan/git (no viola aislamiento #334).

**Hallazgo principal:** de los 15 códigos que el item pide contar, solo aparecieron 3 en 17.5h
reales: `dependencia_sin_cerrar` (3,027 líneas / 5 items), `bloqueado_por_dependencia` (1,661 /
4 items), `desarrollo_humano` (1,286 / 2 items). **`footprint_desconocido` y `tope_modulo` — los
dos que condicionan tocar la concurrencia (C3/C4) — tuvieron 0 ocurrencias.** Confirmado también
con una muestra directa (2026-09-12 12:00:08): las 6 terminales ociosas simultáneamente, ninguna
por tope de concurrencia.

**Decisión (registrada, sin frontera dura):** NO se toca `circuito_paralelismo` (6) ni
`paralelo_mismo_modulo` (2) — el ocio real es 100% backlog de dependencias entre items + trabajo
humano en curso, no un techo bajo. Subir la perilla sin esa evidencia sería "subir a ciegas",
que el propio item prohíbe.

**Documentado además:**
- Aprobados crudos vs. despachables reales: 194 con `estado_aprobacion IN aprobado_*`, de esos
  139 son paraguas parqueados (`excluir_pool_automatico=true`, nunca despachan) → solo **27**
  pasan `RoadmapItem::scopeDespachable()`, que es el número real que importa para diagnosticar
  "por qué no asigna".
- Desambiguación de "sin modelo": son 3 conceptos sin relación entre sí que comparten texto —
  (1) `digest.sin_modelo` en la Torre = revisor sin respuesta de la API al triar items nuevos
  (falla-segura, #807); (2) `--modelo=` de `vuelta.sh` = qué variante de Claude ejecuta esa
  vuelta; (3) "Sin modelo" de SmartImportExport = tabla sin Eloquent, dominio de import/export.
  Ninguno es un freno del despachador; `pausado=0` y `N sin modelo` conviven sin contradicción
  porque miden capas distintas (reparto de trabajo vs. triaje inicial).

Detalle completo con las tablas de conteo en
`docs/circuito-despacho-fase4-item-9990894-verificacion.md`. Sin cambio de código de negocio —
item de análisis puro, cerrado documentando hallazgos y la decisión de no ajustar ningún
parámetro.
