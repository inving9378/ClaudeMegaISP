## 2026-09-14 10:45 — #9991092 Hoja de ruta: recuperar el scope de backlog (default "Pendientes")

**Sesión directa con Irving.** Nivel A. Referencias: `c96b81c0` (11-sep, #9990890: quitó
`?vista=backlog`, efecto colateral) y `153a8ef7` (13-jul: "Hoja de ruta = solo backlog").

### PASO 0 (read-only)
- **a)** `c96b81c0` toca un solo archivo: `RoadmapTab.vue:626` —
  `axios.get('/api/roadmap/items?vista=backlog')` → `axios.get('/api/roadmap/items')`. Lo demás
  es comentario. **No se revierte.**
- **b)** `scopeBacklog` sigue existiendo (`RoadmapItem.php:2202`) pero el #432 (BLOQUE 0) lo
  reconvirtió en el *intake* (`nivel_riesgo IS NULL`, excluye `aprobado_irving`/`requiere_irving`):
  hoy devuelve **0** items y lo consumen los KPIs `sin_clasificar`/`intake` del Panorama
  (`RoadmapController:1133`). No sirve para la Hoja de ruta (daría 0, no ~203) y cambiarlo
  rompería el Panorama. La semántica de julio (sin `branch`) daría 100. **No se reusa ni se
  escribe un scope nuevo.**
- **c)** Terminales pinta `trabajandoAhora()` → `allLiveSessions()` (`settings` `live:*`) →
  `current_item` por sesión; el conjunto es **`RoadmapCircuitoService::idsEnCurso()`**. Los
  items tomados tienen `status=pending` + `estado_aprobacion=en_progreso` + `worker_sid`
  (`status=in_progress` es otra cosa: 2–3 items manuales). Un reclamo en BD que no es
  `current_item` vivo (p. ej. #9990768 en `wt-4`) NO aparece en Terminales → tampoco se
  excluye de la Hoja de ruta (mismo criterio, sin inventar otro).

### Cambios
- `e69f37a5` backend — `RoadmapController::marcarEnTerminal()`: `index()` y `show()` adjuntan
  `en_terminal` por item calculado con `idsEnCurso()`. El universo se sigue devolviendo completo.
- `f3cb4596` frontend — `RoadmapTab.vue`: `activeFilter` default `'pending'`; `visibleGroups`
  excluye `en_terminal` del grupo Pendientes; "Todos" = universo; `counts` intactos.
- Sin cambios en tarjetas/badges/píldoras, chips, "+ Agregar item", ni otras pestañas.

### Verificación (código literal de la pestaña corrido en Node sobre el payload real)
- V1: default "Pendientes" → **196** visibles (universo 1758; pending 202 − 6 en terminal).
- V2: contadores sobre el universo → 3 / 202 / 1544 / 9 (los números de la mañana eran
  2/203/1540/9: el universo se movió con los items de hoy; la regla es la misma).
- V3: chip Hechos = 1544; chip Todos = 1758.
- V4: Terminales muestra 6 ids; **0** de ellos en la Hoja de ruta por defecto; flag
  `en_terminal` == `idsEnCurso()` (igualdad exacta).
- V6: 0 items con `subtasks` no-lista (la normalización de #9991082 sigue).
- V5 visual (Panorama/Árbol/Terminales/Integración, consola) → Irving, Ctrl+F5.

## 2026-09-14 11:55 — #9991092 Ratificación de Irving + dos condiciones

Irving ratificó la desviación (flag `en_terminal` desde `idsEnCurso()`; la instrucción de reusar
`scopeBacklog` se basaba en la semántica de julio, ya reconvertida por el #432). Respuesta
#9991105 cerrada.

1. **`idsEnCurso()` una vez por request — medido, no supuesto.** `index()` = 5 consultas SQL para
   1771 items; **1 sola** lee sesiones vivas (`select key,value from settings where key like
   'live:%'`, 1.64 ms); el resto es `isset()` sobre `array_flip($ids)` por item, en memoria.
   `show()` = 2 consultas, 1 de sesiones. (`marcarEnTerminal()` recibe la colección completa y
   evalúa el servicio antes del `each`.)
2. **Deuda estructural registrada: #9991129** (nivel B, `requiere_irving`, fuera del pool, sin
   ejecutar): contadores por endpoint de agregados (COUNT por estado, sin filas) + lista filtrada
   server-side y paginada. Disparador: universo > 4,000 items **o** payload > 2 MB. **Línea base
   hoy:** 1,768 items · payload crudo **2.16 MB** (2,263,845 bytes; `done` = 1.71 MB / 79 %) ·
   632 ms · lo pintado por defecto = 196 items / 0.38 MB (18 %). nginx: `gzip on` pero
   `gzip_types` comentado → el JSON viaja sin comprimir (comprimido serían 0.41 MB).
   ⚠️ El disparador de 2 MB **ya se cumple** con el crudo; queda a decisión de Irving en el item
   (recomendación: `gzip_types application/json` primero, reversible; la deuda cuando se acerque
   a 4,000 items o antes si la Torre se siente lenta).
