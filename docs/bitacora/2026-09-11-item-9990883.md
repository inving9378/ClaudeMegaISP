## 2026-09-11 18:46 — CIRC-02a: duplicado de #9990857, cerrado sin código

`#9990883` pedía el mismo diagnóstico de solo lectura que ya resolvió `#9990857` (y sus fases
`#9990872`/`#9990873`, ambas `completado` y mergeadas): cuántas respuestas de Irving en
`requiere_irving` se quedaron sin ejecutar. Es un duplicado exacto — nació como sub-item de
`#9990882`, que es a su vez copia del paraguas `#9990856` (mismo título/prompt, tripleta de
sub-items espejo `#9990883/84/85` ↔ `#9990857/58/59`).

`circuito:cabida` dio NO CABE por umbral histórico, pero no había código que picar: el trabajo ya
existe. En vez de descomponer (que solo recrearía los mismos dos sub-items ya hechos), se verificó
el resultado existente y se hizo un recheck de frescura (universo de 1594 items con log, vs. 1589
de la corrida original hace ~2h): la conclusión sigue igual, **0 respuestas perdidas**. El único
caso nuevo que apareció en el recheck (`#9990864`) resultó ser un near-miss legítimo (la respuesta
de Irving SÍ se ejecutó; un ejecutor encontró una contradicción real de spec y re-escaló con una
pregunta nueva hace ~6 minutos) — no una pérdida, y se documentó de paso un hallazgo colateral: el
mecanismo de `consulta`/escalación de un ejecutor no deja rastro en el `log` estructurado del item,
solo en `comentarios_claude` (punto ciego anotado para quien construya `#9990858`/`#9990884`, el
mecanismo del hilo de respuestas — no se toca código aquí).

Detalle completo en `docs/circuito-diagnostico-duplicado-item-9990883-verificacion.md`. Cerrado
`completado` sin cambio de código de aplicación (solo estos dos documentos).
