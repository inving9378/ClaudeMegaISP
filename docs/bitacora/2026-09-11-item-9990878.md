## 2026-09-11 20:43 — Item #9990878: bucle reap sobre paraguas ya descompuesto (se completa el cierre-intento faltante)

Item paraguas (Identidad unificada — Fase 3: doble escritura de `colaborador_id` junto a
`seller_id`) ya había sido descompuesto correctamente por una vuelta previa de `wt-5`
(2026-09-11 20:16) en 4 sub-items reales: **#9990962** (mapear callsites Eloquent vs raw,
`completado`+mergeado), **#9990963** (implementar doble escritura, `en_progreso` bajo `wt-5`),
**#9990964** (comando de consistencia + KPI, `en_progreso` bajo `wt-5`) y **#9990965** (activar
flag + verificación final + cerrar el padre, `aprobado_revisor` sin reclamar).

Esa vuelta murió (comentario truncado a media oración) antes de intentar **cerrar** al padre —
el reaper lo re-encoló como huérfano (`reap_count=1`) y el pool lo repartió de nuevo a `wt-3`
sin trabajo propio que hacer.

Verificado: la descomposición original sigue correcta, nadie más la tocó; #9990963/#9990964
siguen activamente reclamados por `wt-5` (aislamiento #334, no se tocaron). Corrección:
ejecuté el intento de cierre faltante sobre #9990878 (`estado_aprobacion='completado'`); el
guard de paraguas (`RoadmapItem.php` bloque "(2b) PARAGUAS") lo reenrutó a `aprobado_irving`
+ `excluir_pool_automatico=true`, liberando `worker_sid`/`claimed_at` y sacándolo del
pool/reaper hasta que el hook de cierre en cascada complete el padre cuando #9990963,
#9990964 y #9990965 cierren los tres.

Detalle completo en `docs/roadmap-bucle-reap-item-9990878-verificacion.md`.

**Sin cambio de código de negocio** — el trabajo técnico real de la Fase 3 sigue en
#9990963/#9990964 (reclamados por `wt-5`) y #9990965 (pendiente de reclamo).
