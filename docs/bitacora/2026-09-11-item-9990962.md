## 2026-09-11 20:30 — Fase 3a: mapa completo de callsites de seller_id en client_main_information

Item #9990962 (sub-item de #9990878, precondición de Fase 3b #9990963). Investigación READ-ONLY.

Completé los 3 puntos que quedaban pendientes de la investigación previa:

1. **SmartImportExport** — confirmado que el modo SMART (el real, default) escribe
   `client_main_information` 100% RAW vía `DB::table()->upsert()` en `flushBulkMergeRows()`,
   sin pasar por Eloquent, sin importar el `'model' => ClientMainInformation::class` declarado
   en `TABLE_MODULE_MAP` (esa metadata solo aplica al modo REPLACE, que no es el que corre en la
   práctica para esta tabla identidad-por-PK).

2. **Endpoint de edición de vendedor en la ficha** — ubicado: `InformationClientCrud.vue` postea
   a `/cliente/update/{id}` → `ClientInformationController::update()` →
   `saveSingleRelationIfExist('App\Models\Client', ...)` → `App\Models\ClientMainInformation::find($id)->update($input)`.
   Es Eloquent, pero usa la clase **LEGACY** (`App\Models\ClientMainInformation`), no la modular.

3. **Cuál clase usa cada callsite** — confirmado que el alta nueva (`ClientController::store()`)
   usa la clase **MODULAR** (`App\Modules\Core\Clientes\Models\ClientMainInformation`, vía la
   relación `Client::client_main_information()`), mientras que la edición de ficha usa la
   **LEGACY**. Son dos clases Eloquent DISTINTAS en dos callsites distintos.

**Hallazgo crítico nuevo, verificado empíricamente en tinker (transacción + rollback, sin
persistir nada):** aunque `App\Models\ClientMainInformation` EXTIENDE a la modular, los eventos
de modelo de Eloquent se registran/disparan con `"eloquent.{evento}: " . static::class` usando
late static binding sobre la clase CONCRETA en tiempo de ejecución — no sobre la clase donde se
llamó `observe()`. Probé ambas direcciones: observer en la modular no dispara al guardar vía la
legacy, y viceversa. Esto significa que la Fase 3b (#9990963) NO puede enganchar el observer a
"la" clase Eloquent (singular, como asume su texto) — tiene que enganchar a AMBAS clases
explícitamente, o se le queda sin doble escritura uno de los dos flujos (alta nueva o edición de
ficha) sin ningún error visible.

Documenté todo en `docs/identidad-seller-id-callsites-item-9990962-verificacion.md` (tabla de los
4 callsites + detalle + la prueba empírica) para que Fase 3b no tenga que re-investigar nada.

Sin cambio de código de negocio — es investigación pura, nivel B, autorizada por el revisor como
"auditorías y mapas READ-ONLY". Cierro el item con el documento como entregable.
