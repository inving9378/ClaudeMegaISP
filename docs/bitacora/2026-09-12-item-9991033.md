## 2026-09-12 13:12 — Cobranza WhatsApp fallback — Fase 4: verificación E2E y cierre del padre #9991031

Item #9991033 (sub-item de #9991031). El spec pedía verificar en DEV, sin activar en prod, los 4
casos de `billing:send-pending-notifications` con el respaldo por WhatsApp, y cerrar el padre
#9991031 si todo pasaba.

**Hallazgo previo:** el código de Fase 1+2 que describía el sub-item #9991032 (del cual dependía
formalmente #9991033) YA estaba mergeado a `main` desde antes — commits `9d178194` (flag) +
`173af77c` (fallback real), ambos integrados vía `abc7c74b` bajo la propia rama de #9991031.
#9991032 seguía `en_progreso` (worker_sid wt-1) cuando arranqué, pero la dependencia real (el
código en main) ya estaba satisfecha. Se registró como decisión (`circuito:reportar --tipo=decision`)
para no bloquear la Fase 4 ni duplicar el trabajo de wt-1.

**Verificación E2E** (transacción + rollback completo, sin dejar datos de prueba, cliente 17 —
MARIA DE LOS ANGELES, teléfono real 5566513187):

- Neutralicé temporalmente (dentro de la misma transacción) las 4 notificaciones reales que
  estaban pendientes de envío y el flag `billing_config.envio_pausado` (estaba en `true` en dev,
  lo cual explica por qué la primera corrida de prueba no tocó nada hasta que lo bajé, solo
  dentro de la transacción).
- **(a) Con email** → sigue enviándose por correo igual que hoy: `Mail::to()->send(BillingMail)`,
  status `enviado`, sin cambio aunque el flag esté ON.
- **(b) Sin email + con teléfono, flag ON**:
  - Estado real de DEV (sin ninguna `WhatsAppInstance` con `default_instance=true`): cae
    controladamente a `error` ("Respaldo WhatsApp falló: No query results...") — sin excepción no
    capturada. **Prerequisito de config pendiente**: dar de alta una instancia default para que el
    flag sirva de algo en DEV.
  - Simulando una instancia default (dentro de la misma transacción): resuelve el teléfono de la
    ficha (phone/phone2/phone3), crea una fila `whatsapp_messages` OUT en `pending` vía
    `WhatsAppGateway::sendText`, número normalizado `5215566513187`, mensaje con nombre del
    cliente + referencia MEG. Status del `billing_notification` → `enviado`.
- **(c) Sin email y sin teléfono válido, flag ON** → `error`, "Sin correo ni teléfono registrado
  (sin canal de notificación)", sin excepciones.
- **(d) Flag OFF (default)** → idéntico al comportamiento actual: `error`, "Sin dirección de
  correo".

Post-rollback confirmado: `envio_pausado` restaurado a `true`, backlog real de 4 notificaciones
intacto, teléfono del cliente 17 restaurado, `default_instance` de la instancia restaurado a
`false`, 0 filas de prueba residuales en `billing_notifications`/`whatsapp_messages`.

**Cierre:** #9991033 cerrado `completado` con `cierre_sin_codigo_motivo` (verificación sin bugs,
nada que commitear). Al cerrarlo, el guard de paraguas detectó que #9991032 ya había cerrado en
paralelo (wt-1, merge_commit `4659901b` — llegó a la misma conclusión) y completó automáticamente
al padre **#9991031** (`paraguas_cerrado`). Actualicé `reporte_coloquial`/`enlace_revision` de
#9991031 para reflejar la verificación E2E de Fase 4.

**Sin cambio de código** — el trabajo de implementación ya estaba en `main` desde el commit
original de #9991031; esta vuelta fue puramente de verificación y cierre.
