## 2026-09-14 11:55 — Item #9991122: escalar individualmente las 3 decisiones de permisos (mecanismo elegido por Irving)

**Item:** `[RESPUESTA] permisos — 3 decisiones tras consolidar el documento final (#9990744, urgencia
CRÍTICOS, mecanismo Diana)`. Canal de respuesta de `#9990746` (consolidación del documento final
`docs/auditoria-permisos-2026-09.md`, caso Diana / SUPERVISOR_MOSTRADOR), padre `#9990721`.

**Lo que ya venía decidido:** el item tenía una pregunta estructurada meta (`q1`) con 3 opciones
sobre CÓMO proceder con las 3 decisiones de negocio consolidadas en el cuerpo (Q1/Q2/Q3, sobre
`#9990744`, `#9991116` y el mecanismo de Diana). Irving ya había elegido la **Opción 1** (la
recomendada): *"Escalar a Irving el detalle de las 3 decisiones (una por una) con recomendación
técnica del circuito para cada una, y esperar su OK antes de tocar permisos"* — rechazando
explícitamente la Opción 2 ("Autorizar el circuito a ejecutar las 3 decisiones", marcada como
violación de frontera dura).

**Lo que hice esta vuelta:** ejecutar literalmente ese mecanismo, sin decidir Q1/Q2/Q3 por mi
cuenta:

1. `circuito:sub-item 9991122` ×3, cada uno colgando de `#9991122`, cada uno con el contexto
   completo + las 3 opciones + la recomendación técnica ya redactada en el propio item padre:
   - **#9991133** — ¿archivar `#9990744` (Fase 2/3, H1-H10) como ya-no-necesario, re-especificarlo
     a solo H7/H8, o dejarlo tal cual?
   - **#9991134** — ¿elevar prioridad/urgencia de `#9991116` (12 agujeros CRÍTICOS de rutas: leak
     de password Mikrotik en texto plano + toma de control de cualquier cuenta)?
   - **#9991135** — ¿investigar el mecanismo exacto de por qué el rol de Diana nunca persistió, o
     proceder directo con `#9990797`/`#9991117` sin confirmarlo?
   Los tres nacieron `pendiente_revision` (candado de `RoadmapIntakeService::crear` — quien crea no
   se auto-aprueba); el revisor los triaje como cualquier item y, al tocar permisos/seguridad,
   deberían escalar a `requiere_irving` por su cuenta.
2. `circuito:reportar --tipo=decision` registrando el razonamiento.
3. Intento de cierre del padre (`estado_aprobacion=completado`) → el guard de paraguas
   (`RoadmapItem.php`, bloque "(2b) PARAGUAS") lo reenrutó solo a `aprobado_irving` +
   `excluir_pool_automatico=true` (evento `paraguas_abierto` en el log: "le quedan 3 sub-item(s)
   abierto(s)"). Esto es el comportamiento **correcto y esperado** — el item queda parqueado como
   paraguas y el hook de cierre en cascada (`RoadmapItem.php:459-491`) lo completará solo cuando
   los 3 hijos cierren.
4. `circuito:reportar --tipo=cierre` con el resumen de verificación.

**Por qué no decidí yo mismo Q1/Q2/Q3** (aunque Q1 y Q3 parecían de bajo riesgo por sí solos):
Irving ya evaluó y rechazó explícitamente la opción de que el circuito decida y ejecute — la marcó
como violación de frontera dura. Seguir la "regla de oro" (decidir con la opción recomendada) aquí
habría sido ignorar una instrucción explícita y ya aprobada sobre el PROCESO mismo, no sobre el
contenido de cada decisión.

**Qué queda pendiente:** que el revisor trie los 3 sub-items (deberían llegar a la bandeja de
Irving por ser permisos/seguridad) y que Irving elija una opción en cada uno. Nada de esto bloquea
otro trabajo: `#9990797` (fix del rol de Diana) y los 6 items de arreglo (`#9991116`-`#9991121`)
ya avanzan solos en el pool normal, como decía el propio item.

**Sin cambio de código** — esta vuelta fue puramente de gestión del roadmap (crear 3 sub-items +
cerrar el padre correctamente vía el guard de paraguas ya existente).
