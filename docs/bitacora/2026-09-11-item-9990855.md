# Item #9990855 — Fix del escalado por timeout que pisa el guard de paraguas

## 2026-09-11 18:40 — Cierre de Fases 1-3, decomposición de la Fase 4

Retomo el item tras una reanudación previa (agotó turnos con 1 commit ya hecho en la rama
`circuito/item-9990855-fix-del-escalado-por-timeout-no-escalar`). `circuito:cabida` devuelve
NO CABE (`ya_timeouteo_antes`) — sigo el protocolo de descomposición, pero antes verifico qué
tanto de las 4 fases del prompt ya está resuelto por la vuelta anterior, para no perder ese
trabajo ni duplicarlo.

**Verificado (solo lectura, sin re-hacer nada):**
- **FASE 1 (diagnóstico)** — ya documentada en el propio commit `a4b200ac`: `commits_rama` en
  `ParquearTimeoutCommand.php` ya contaba sobre `items.branch` (correcto), el "0 commits" de
  #9990810 salía de leer ese dato DESPUÉS de que la decisión ya estaba tomada, no de leer la
  rama equivocada.
- **FASE 2 (el fix)** — completa y commiteada (`a4b200ac`, 90 líneas): guarda de estado (si
  `estado_aprobacion !== 'en_progreso'` al llegar, no escala, registra `timeout_no_escalado`) +
  guarda de paraguas (si `tieneSubItemsAbiertos()`, no escala, parquea como
  `aprobado_irving`+`excluir_pool_automatico=true`). `php -l` limpio; los métodos
  `tieneSubItemsAbiertos()`/`subItemsAbiertos()` que usa ya existen en `RoadmapItem.php`
  (líneas 1819/1827). **NO estaba integrada a `main`** — la integro en esta vuelta.
- **FASE 3 (saneamiento)** — ya ejecutada por la vuelta anterior (registrado como decisión en
  el log del item, 18:27). Reverificado contra la BD real:
  - `#9990801` → `completado`, `excluir_pool_automatico=1`, 0 sub-items abiertos. ✅
  - `#9990714` → `completado`, `excluir_pool_automatico=1`, 0 sub-items abiertos. ✅
  - `#9990776` → `aprobado_irving`, `excluir_pool_automatico=1`, 1 sub-item abierto (correcto:
    sigue cerrando por cascada). ✅
  - `#9990810` → `aprobado_irving`, `excluir_pool_automatico=1`, 1 sub-item abierto (correcto). ✅
  Barrido histórico (0 casos vivos adicionales) también ya registrado por la vuelta anterior;
  no lo repito.
- **FASE 4 (candado de regresión)** — NO existe ningún test de `ParquearTimeoutCommand` en el
  repo (`find . -iname "*ParquearTimeout*"` solo devuelve el propio comando). Es lo único que
  falta.

**Acción de esta vuelta:**
1. Creado sub-item **#9990880** con la Fase 4 completa (3 pruebas: sub-items abiertos no
   escala, item normal sí escala, rama con commits reencola en vez de escalar), spec detallado
   con archivo/líneas exactas para que el próximo ejecutor no re-investigue.
2. Integrada la Fase 1-2 (commit `a4b200ac`, ya completo desde la vuelta anterior) — encolado
   el merge vía `circuito:integrar 9990855`, para que el fix real deje de estar huérfano en una
   rama mientras la Fase 4 se resuelve aparte.
3. Ejecuto el intento de cierre del propio #9990855 (ahora paraguas de #9990880): el guard de
   paraguas lo parquea como `aprobado_irving`+`excluir_pool_automatico=true`, liberando
   `worker_sid`/`claimed_at` — evita que este mismo item termine siendo, sin ironía, el próximo
   caso de "bucle reap sobre paraguas ya descompuesto" que él mismo estaba corrigiendo.

**Sin cambio de código de negocio propio de esta vuelta** — el fix (Fases 1-2) ya estaba
escrito por la vuelta anterior; el trabajo real que falta (Fase 4, candado de regresión) sigue
en #9990880, pendiente de que una terminal lo reclame.
