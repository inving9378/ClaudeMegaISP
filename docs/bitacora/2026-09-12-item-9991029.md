## 2026-09-12 12:15 — Ejecución manual: worktree dedicado /var/www/megaisp-scheduler + crontab (item #9991029)

**Contexto:** item de seguimiento de #9991024 (hermano de Q1 #9991026/Q4 #9991028). Pide, en
palabras del propio título, "Ejecución manual": (1) crear el worktree fijo
`/var/www/megaisp-scheduler` (`git worktree add ... main`, corrido desde `/var/www/megaisp`);
(2) confirmar que `deploy/circuito/cron-wrap-scheduler.sh` apunta a esa ruta fija; (3) editar el
crontab real del sistema (`crontab -e`, usuario `meganet`) para que las líneas `circuito:*` usen
`cron-wrap-scheduler.sh` en vez de `cron-wrap.sh`; (4) verificar 1-2 ticks reales; (5) anotar la
hora exacta del corte (nuevo punto de partida de las 6h que exige #9991028).

Ya fue triado (revisor → `requiere_irving`, categoría prod) y Des-Trabe (Opus) antes de llegar a
esta terminal; Irving aprobó el item y respondió `q1`/`q2`/`q3` (opción recomendada en las tres:
crear el worktree + reemplazar la línea del crontab; comentar — no borrar — la línea vieja;
validar ejecutando el wrapper a mano y esperando 1-2 ticks reales antes de cerrar).

**Por qué no se ejecutó aquí:** las dos acciones centrales del item violan mi propia regla dura de
aislamiento (#334: nunca tocar `/var/www/megaisp` ni recursos compartidos fuera de mi worktree
para git/edición/`php artisan`) y son EXACTAMENTE las mismas que el item hermano #9991026 ya
intentó vía `php artisan circuito:provision-worktree --path=/var/www/megaisp-scheduler --base=main`
— rechazadas por el clasificador de auto-mode del harness (motivo: "Modify Shared Resources"). En
esa vuelta se consultó a Thomas, que instruyó: documentar el bloqueo, NO forzar un bypass, dejar
el resto para ejecución manual de Irving (ver `docs/bitacora/2026-09-12-item-9991026.md`).

Se volvió a consultar a Thomas para este item (mismo dilema, item distinto):

> **Pregunta:** el item pide crear el worktree bajo `/var/www` y editar el crontab real; ¿aplica
> la misma instrucción que ya dio para #9991026 (documentar, no ejecutar, dejar manual)?
> **Opción recomendada (reversible):** no ejecutar la parte de worktree/crontab; solo documentar
> el estado actual y dejar el item parqueado para ejecución manual real de Irving.
> **Veredicto:** PROCEDE con la opción recomendada.

**Verificación en seco (solo lectura, sin tocar nada):**
- `/var/www/megaisp-scheduler` **no existe** (confirmado con `ls`).
- `crontab -l` del usuario actual: las líneas `circuito:*` (`circuito:scheduler` incluida) siguen
  apuntando a `deploy/circuito/cron-wrap.sh` desde `/var/www/megaisp` — el corte a
  `cron-wrap-scheduler.sh` **no se ha hecho**.
- `deploy/circuito/cron-wrap-scheduler.sh` (commit `5c571bbe`, ya en `main`) sigue intacto y listo
  para usarse en cuanto el worktree dedicado exista.

**Estado del item:** se parquea (`estado_aprobacion=aprobado_irving`,
`excluir_pool_automatico=true`, `motivo_espera=frontera_produccion`) — NO se marca `completado`
porque el objetivo real (el corte del cron) no se ejecutó. Queda fuera del pool automático hasta
que Irving (u otra sesión con permisos de OS fuera del sandbox del circuito) haga los pasos
manuales.

**Pendiente — ejecución manual de Irving (idéntico a lo documentado en #9991026, sin cambios):**
1. Desde `/var/www/megaisp`: `php artisan circuito:provision-worktree --path=/var/www/megaisp-scheduler --base=main`.
2. Verificación en seco: desde `/var/www/megaisp-scheduler`, `php artisan list | grep circuito:` y
   probar en modo lectura `circuito:scheduler`/`circuito:disparo-check` antes de tocar el cron real.
3. `crontab -e` (usuario `meganet`): comentar (no borrar) la línea vieja de `circuito:scheduler`
   apuntada a `cron-wrap.sh`, y agregar la nueva apuntada a `cron-wrap-scheduler.sh` con el mismo
   horario/argumentos. Decidir aparte si `backup_db:process`/`circuito:compuertas-sonda` migran
   también (hoy atadas a `/var/www/megaisp` a propósito, según sus propios comentarios en el
   crontab).
4. Ventana corta; verificar `storage/logs/cron-wrap-scheduler-errores.log` (o el log que corresponda
   dentro del checkout dedicado) y que no se perdieron ticks.
5. Anotar aquí (o en #9991028) la fecha/hora exacta con TZ en que el cron quedó apuntando al
   checkout dedicado — ese instante es el nuevo punto de partida real de las 6h que pide #9991028.
6. Documentar el checkout nuevo en `CLAUDE.md`.
