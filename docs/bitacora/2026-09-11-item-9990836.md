## 2026-09-11 23:12 — Item #9990836 (bucle reap sobre paraguas ya descompuesto — se completa el cierre-intento faltante)

Mismo patrón documentado repetidamente en CLAUDE.md (#738/#745/#830/#816/#818/#848/#852/#905/#878/
#906/#907/#924/#9990012/#917/#910/#936/#9990408/#962/#9990554/#9990549/#9990624/#9990650/#9990807/
#9990826): #9990836 ("Fase 3 del #9990833: buscador usa serie_equipo_norm para encontrar el SN en
cualquier formato") ya había sido correctamente descompuesto por una vuelta previa (mismo `wt-1`,
2026-09-11 17:10) tras un `circuito:cabida` en NO CABE, en **#9990847** (Fase 3a: backend —
`config/clientes_busqueda.php` + `ClienteSearchService::normalizar()` con el case `sn_canonico`) y
**#9990848** (Fase 3b: UI — listado de Clientes muestra `serie_equipo` + indicador de
`serie_equipo_origen`), tras verificar que la dependencia (Fase 2 #9990835) ya estaba completada y
mergeada y que el backfill ya había corrido en dev (5153/5606 filas con `serie_equipo_norm`
poblado). Pero esa vuelta murió sin intentar cerrar al padre — el log solo registra
`claim_liberado_al_morir_la_vuelta` (muerte del proceso), y el pool volvió a repartir #9990836 (a
la misma `wt-1`, esta vuelta) sin trabajo propio que hacer.

Verificado: #9990847 y #9990848 (`origen_item_id=9990836`) siguen intactos, `pendiente_revision`,
sin reclamar — la descomposición original seguía siendo correcta, nadie más la tocó.

Corrección: se ejecutó el intento de cierre faltante (`estado_aprobacion='completado'`); el guard
de paraguas (`RoadmapItem.php` bloque "(2b) PARAGUAS") lo reenrutó a `aprobado_irving` +
`excluir_pool_automatico=true` (evento `paraguas_abierto` en el log, "le quedan 2 sub-item(s)
abierto(s)"), liberando `worker_sid`/`claimed_at` y sacándolo del pool/reaper hasta que el hook de
cierre en cascada (`RoadmapItem.php:459-491`) lo complete solo cuando #9990847 y #9990848 cierren
los dos.

**Sin cambio de código de negocio** — el trabajo técnico real (columna normalizada en el buscador
de clientes + indicador visual en el listado) sigue en #9990847/#9990848, pendientes de que una
terminal los reclame.
