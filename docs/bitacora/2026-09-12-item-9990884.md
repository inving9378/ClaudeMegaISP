## 2026-09-12 01:19 — Item #9990884 (CIRC-02b, cadena #9990874) — CANCELADO por duplicado exacto

**Item:** CIRC-02b: Mecanismo — hilo de respuestas (`roadmap_item_respuestas`) + re-encolado
automático. Sub-item de `#9990882` (CIRC-02 PARAGUAS, cadena raíz `#9990874`), depende de CIRC-02a
`#9990883`.

### Qué se encontró

`circuito:cabida 9990884 --sid=wt-1` devolvió **NO CABE** (`historico_excede_umbral`, ~51741s).
Antes de descomponer en sub-items PASO 1-4 (como pide el propio spec), investigué si ya existía
trabajo equivalente — y sí: el mismo trabajo CIRC-02a/b/c está duplicado en **3 cadenas paralelas**,
generadas por 3 items raíz distintos que Irving aprobó por separado en una ventana de ~30 minutos
sin que el circuito detectara la duplicación semántica:

| Cadena raíz | CIRC-02a | CIRC-02b | CIRC-02c |
|---|---|---|---|
| `#9990856` (la viva) | `#9990857` completado, merge `2c23bad4` | `#9990858` → descompuesto en `#9990899` (migración+modelo, **YA MERGEADO**, commit `ce471c61`), `#9990900` (disparo, `aprobado_irving`), `#9990901` (inyección en prompt, `aprobado_revisor`), `#9990902` (pruebas, `aprobado_revisor`) | `#9990859` `aprobado_revisor` |
| `#9990854` | `#9990866` duplicado | `#9990867` duplicado | `#9990868` duplicado |
| `#9990874` | `#9990883` completado (re-investigó lo mismo que `#9990857`) | **`#9990884` (este item)** | `#9990885` duplicado |

El hallazgo ya estaba documentado por otra terminal en `#9990903` ("[RESPUESTA] CIRC-02b — 3
cadenas duplicadas"), que Irving **ya aprobó** (2026-09-11 19:01:08, opción recomendada del
DES-TRABE Opus, Opción A): *"Conservar cadena `#9990856` (la que ya avanzó) y cancelar las otras
dos"* — es decir, cancelar `#9990866/867/868` (cadena `#9990854`) y `#9990882/883/884/885` (cadena
`#9990874`), con nota de duplicado y referencia cruzada al item vivo.

### Qué se hizo

Ejecuté esa parte ya aprobada de la decisión para `#9990884` (mi item, "un item = un dueño" — no
toqué `#9990882`, `#9990883` ni `#9990885`, que no son míos):

- `estado_aprobacion = 'cancelado'`, `status = 'cancelled'` (vía tinker, replicando el patrón real
  de `RoadmapController::decidir(accion=cancelar)`: `cierreManualIrving=true` para que el guard de
  "cancelación con rama sin mergear" no interfiera — no aplicaba de todas formas, `branch` era
  `null`).
- `cierre_sin_codigo_motivo` + `sin_ui`/`sin_ui_motivo` poblados (cierre administrativo sin código
  ni pantalla — bookkeeping del propio roadmap).
- `comentarios_claude` con el detalle completo de la investigación y la cadena de autoridad de la
  decisión (Irving vía `#9990903`).
- Entrada de log `evento: cancelado_duplicado` con `decision_de: "irving:admin (via #9990903,
  2026-09-11 19:01:08)"`.
- `circuito:reportar --tipo=decision` (no decompongo, cancelo) + `--tipo=cierre` (detalle técnico
  completo, reporte #7784/#7785).

### Qué NO se hizo (a propósito)

No cancelé `#9990882` (paraguas propio de esta cadena) ni `#9990885` (CIRC-02c hermano) — no son
mi item. Tampoco toqué la cadena `#9990854→866/867/868` ni el bug de raíz (dedupe por
título/similitud al crear items, q2 de `#9990903`). Eso queda para quien retome la limpieza
completa de `#9990903` como su propio item.

### Verificación

`tinker` confirma el estado final: `estado_aprobacion=cancelado`, `status=cancelled`,
`branch=null`, `merge_commit=null`, log con la entrada `cancelado_duplicado`. Sin cambio de código
(no aplica: es cierre administrativo, no una feature).
