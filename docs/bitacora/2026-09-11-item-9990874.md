## 2026-09-11 18:40 — Items de corrección del Circuito CC — para desatorar el flujo (#9990874)

**Qué era el item:** un documento (redactado por Cowork, "v2 — se agrega CIRC-05") con 9 bloques
listos para pegarse como items nuevos en la Torre de Control (CIRC-00 a CIRC-08), cada uno con
título, módulo, nivel, prioridad, descripción y prompt completo para Claude Code. El propio
documento explica por qué nadie los creó ya: la API externa de escritura (`roadmap-externo`) solo
permite tocar `estado_aprobacion`/`nivel_riesgo`/`comentarios_claude`, no crear items — así que el
trabajo de #9990874 no es "arreglar el circuito" directamente, es **materializar el documento como
items reales de la Hoja de Ruta**, usando `circuito:sub-item` (el mecanismo que sí puede crear
items desde una terminal on-box).

### Qué se hizo

**CIRC-00 (no era item nuevo, solo empujón sobre 2 items existentes):**
- **#9990756** ("fix build roto en main — flujoAnimadoConfig redeclarado en mapUtils.js"): estaba
  `aprobado_revisor` con `excluir_pool_automatico=1` desde el 2026-09-10, ya con DOS
  verificaciones previas (wt-1 y wt-2) de que el fix real ya había llegado a `main` vía el merge de
  su item padre #9990740 (commit `f4767645`) — solo le faltaba el cierre formal con
  `cierre_sin_codigo_motivo`. Reverificado de nuevo aquí (`grep` en `mapUtils.js`: una sola
  declaración de `flujoAnimadoConfig`, y el código de la Fase 2a posterior —#9990754, commit
  `bb0fa2c6`— ya construye sobre ella sin romperse) y **cerrado como `completado`** con su
  `cierre_sin_codigo_motivo`, `reporte_coloquial` y `excluir_pool_automatico=false`. No hacía falta
  "mandarlo al pool": ya estaba resuelto, solo mal cerrado.
- **#9990075** ("Hook DNS-01 automatizado para dev.meganett.com.mx"): ya está correctamente
  parqueado (`aprobado_irving` + `excluir_pool_automatico=true`) esperando una credencial/decisión
  de Irving, exactamente como describe el documento. Sin cambios — es acción material suya, no de
  código.

**CIRC-01 a CIRC-08 (creados como sub-items de #9990874, vía `circuito:sub-item`, con el spec
completo tal cual lo trae el documento):**

| # | Título | Padre | Depende de |
|---|---|---|---|
| #9990881 | CIRC-01 — fix del escalado por timeout que pisa al guard de paraguas + saneamiento de 4 atorados | #9990874 | — |
| #9990882 | CIRC-02 (PARAGUAS) — el comentario de Irving es la respuesta | #9990874 | — |
| #9990883 | CIRC-02a — diagnóstico solo lectura de respuestas perdidas | #9990882 | — |
| #9990884 | CIRC-02b — tabla `roadmap_item_respuestas` + re-encolado automático | #9990882 | #9990883 |
| #9990885 | CIRC-02c — hilo en la Torre + watchdog | #9990882 | #9990884 |
| #9990886 | CIRC-03 — bandeja de decisiones real (espera decisión vs. espera insumo) | #9990874 | #9990881 |
| #9990887 | CIRC-04 — re-triaje de la cola (prioridades, basura, 430 sin nivel) | #9990874 | — |
| #9990888 | CIRC-05 — API externa: crear items + hilo de reportes | #9990874 | #9990884 |
| #9990889 | CIRC-06 — el despachador no llena terminales ociosas | #9990874 | — |
| #9990890 | CIRC-07 — pestaña Hoja de ruta muestra 0/0/0/0 | #9990874 | — |
| #9990891 | CIRC-08 (SOLO LECTURA) — inventario de ramas sin integrar | #9990874 | — |

Decisiones tomadas con la regla de oro (registradas también con `circuito:reportar --tipo=decision`):

1. **`--depende-de` (posiciones de hermanos, `subtasks.descomposicion.depende_de`) NO está
   cableado al scheduler vivo** — es puro dato, sin efecto en el despacho (así lo dice el propio
   docblock de `DependenciaGate`, #334/#629: "TODAVÍA NO CABLEADO al scheduler vivo"). Para que las
   dependencias del documento (02b espera a 02a, 02c espera a 02b, 03 espera a 01, 05 espera a 02b)
   **sí se respeten de verdad**, se fijó además el campo `depende_de` de nivel superior (el de
   MR-36, #9990332, que sí está cableado en `RoadmapCircuitoService::estaCerradoParaDependencia()`)
   con los IDs reales: #9990884→[#9990883], #9990885→[#9990884], #9990886→[#9990881],
   #9990888→[#9990884]. Se dejaron ambos mecanismos (el de posiciones queda como estaba, informativo)
   sin quitar nada.
2. **`nivel_riesgo` y `modulo` NO se fijaron a mano** aunque el documento los declara por bloque
   (ej. CIRC-05 "Nivel: C"). `SubItemCommand` es deliberado en esto — su propio docblock dice
   "Tampoco declara nivel de riesgo — clasificar es del revisor, no de quien pide" — así que los 11
   items nacen `pendiente_revision` sin nivel, para que pasen por el triaje/revisor normal como
   cualquier otro item. Se dejó una nota explícita en el spec de CIRC-05 sobre por qué el nivel
   sugerido es C (frontera de credenciales/tokens expuestos), para que el revisor lo tenga a mano
   sin que yo se lo imponga.
3. **`urgente=true`** sí se fijó a mano en #9990881 (CIRC-01) y #9990889 (CIRC-06), porque el
   documento lo declara explícitamente como dato de hecho ("Urgente: sí"), no como juicio de riesgo
   — es equivalente a copiar un campo de prioridad declarado, no a clasificar.
4. **CIRC-02** se creó como paraguas real (con sus 3 hijos 02a/02b/02c colgando de él, no de
   #9990874 directamente) para que su propio ciclo de vida (guard de paraguas + cierre en cascada)
   sea el que ya usa el resto del circuito, en vez de aplanar los 3 sub-pasos como hermanos sueltos
   de #9990874.

### Verificación

- Los 11 items existen, con el `origen_item_id`/`position`/`depende_de` esperados (confirmado por
  consulta directa a `roadmap_items`).
- #9990756 quedó `completado` con `excluir_pool_automatico=false` (verificado tras el `save()`).
- No se tocó ningún archivo de código de la aplicación — el trabajo de este item es 100% datos del
  roadmap (creación de registros + cierre de un item ya resuelto). Este documento de bitácora es el
  único archivo de git que cambia en la rama de #9990874.
- No se reclamó ni se tocó el trabajo de ninguna otra terminal — #9990756/#9990075 no estaban
  reclamados por nadie al momento de tocarlos (`worker_sid=null` en ambos).

### Qué sigue

El trabajo técnico real de "desatorar el flujo del circuito" vive ahora en los 11 items creados,
cada uno con su spec completo copiado del documento origen — no hace falta releer el documento,
basta abrir cada item. #9990874 se cierra como PARAGUAS (se intentará `completado`, el guard de
paraguas lo parqueará en `aprobado_irving` + `excluir_pool_automatico=true` hasta que los 11 hijos
cierren, que es el comportamiento correcto y esperado).
