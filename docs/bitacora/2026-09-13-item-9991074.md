## 2026-09-13 16:55 — Fase B (fix .pa-error/.pa-c-cola/.pa-c-detenido/.pa-decision-aviso en TorreArbolNodo.vue) — RESUELTO, ya corregido por commit directo de Irving

Item #9991074, sub-item de #9991072 (a su vez sub-item de #9991071, "Torre Panorama Árbol
ilegible en modo oscuro"). El diagnóstico y diff exacto ya estaban confirmados en #9991072: 4
selectores de `TorreArbolNodo.vue` con color hardcodeado (`.pa-error`, `.pa-c-cola`,
`.pa-c-detenido`, `.pa-decision-aviso`) que reutilizaban valores de modo claro sobre fondo oscuro,
dependientes de que la Fase A (#9991073) declarara `--pa-warn`/`--pa-danger` en `.pa-wrap` de
`TorrePanoramaArbol.vue`.

**Hallazgo:** el fix completo (Fase A + Fase B, los 5 selectores: `.pa-error-raiz` +
`.pa-error`/`.pa-c-cola`/`.pa-c-detenido`/`.pa-decision-aviso`) ya está en `main` — commit
`2f6b2cf2` ("fix(torre-control): corrige contraste del árbol en modo oscuro"), autoría Irving,
el mismo día en que se creó el sub-item (2026-09-13 10:44), integrado a main vía el merge de
`#9991071` (`d4867cf8`). Es el mismo patrón de carrera de timing ya documentado varias veces en
este repo (#733/#741/#753/#9990003/#9990353/#9990624/#9990658/#9990869): Irving aplicó el fix
directo mientras el circuito seguía descomponiendo/triando el item.

**Verificado línea por línea contra el spec de #9991074:**
- (D) `.pa-error{ color:var(--pa-danger,#dc2626); }` ✅ (línea 339)
- (E) `.pa-c-cola{ color:var(--pa-muted,#64748b); }` ✅ (línea 335, reusa var ya existente)
- (F) `.pa-c-detenido{ color:var(--pa-danger,#dc2626); }` ✅ (línea 336)
- (G) `.pa-decision-aviso{ color:var(--pa-warn,#d97706); }` ✅ (línea 352 — fallback ajustado a
  `#d97706` en vez del `#92400e` original del spec, porque el commit real de Irving eligió un
  ámbar con mejor contraste en vez de solo envolver el valor viejo; el efecto — variable +
  fallback razonable — es el mismo que pedía el item)
- Fase A (dependencia): `--pa-danger`/`--pa-warn` declaradas en `.pa-wrap` (`#dc2626`/`#d97706`)
  y `.pa-wrap.pa-dark` (`#f87171`/`#fbbf24`) de `TorrePanoramaArbol.vue`, más `.pa-error-raiz`
  enganchado a `var(--pa-danger,#dc2626)` — todo confirmado con `git show 2f6b2cf2`.

**Build:** `bash deploy/circuito/npm-build.sh` compila sin errores (warning preexistente de
`__VUE_OPTIONS_API__` no relacionado). Sin badges tocados, sin cambio de estructura/lógica —
consistente con lo que pedía el spec.

**Sin cambio de código propio** — no se creó rama ni commit nuevo para #9991074: el diff ya
existía en `main` antes de reclamar el item. Se cierra documentando la verificación.

Nota: #9991073 (Fase A, item hermano) sigue `en_progreso` en la Hoja de Ruta pese a que su
código ya está satisfecho por el mismo commit — no se tocó (no es mi item, regla de aislamiento
#334). #9991071 y #9991072 ya están parqueados como paraguas (`aprobado_irving` +
`excluir_pool_automatico=true`) con `merge_commit` propio; su hook de cierre en cascada los
completará solos cuando #9991073 también cierre.
