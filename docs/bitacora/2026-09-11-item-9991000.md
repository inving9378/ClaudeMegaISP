## 2026-09-11 20:56 — PASO 4 de #9990861 (mecanizado): propuesta de re-priorización de la cola viva

Item #9991000, sub-item de seguimiento de #9990876 (que a su vez continúa #9990861 PASO 4). El
intento anterior (#9990876) había agotado `max_turns` sin commitear nada por intentar razonar las
259 filas de la tabla a mano; este item traía el trabajo reducido a 100% mecánico: correr un
script determinístico (ya incluido en el `prompt` del item, listo para pegar) que genera el
Markdown solo, sin juicio fila por fila.

**Ejecución:**
- `circuito:cabida 9991000` → CABE (sin señal de riesgo).
- Corrido el script vía `php artisan tinker` (pegado en un archivo temporal en `storage/app/`,
  incluido con `require`, y borrado antes de commitear — no es parte del entregable).
- Snapshot real al momento de ejecutar: **247 items vivos** (36 alta, 207 media, 4 baja) — distinto
  a los 259 que documentaba el item, tal como advertía su propia descripción ("la cola es viva,
  recalcular al ejecutar").
- Entregable: `docs/roadmap/repriorizacion-20260911.md` (277 líneas), con sección de metodología,
  hallazgo de calidad de datos (36/247 sin prioridad, solo 5/247 con `target_version` poblado y
  fuera del esquema real de `releases`) y las 3 tablas alta/media/baja.
- Un solo commit (`43607c3f`), `git add` selectivo del único archivo entregable.
- `circuito:estado-item` sin colisión → `circuito:integrar` → encolado y aplicado por el runner
  on-box (`merge_commit=f02f5ab4`, merge a `main` confirmado con `git log`).
- **Nota operativa:** el primer intento de cerrar a mano (`estado_aprobacion=completado`) antes de
  que el runner procesara la cola fue rechazado por el guard `JarvisService::verificarCierre()`
  (rama con `merge_commit` aún vacío) y reenrutado a `aprobado_irving` + `excluir_pool_automatico`.
  El runner terminó el merge segundos después y sobrescribió el estado a `completado` con su
  `merge_commit` real — comportamiento esperado del flujo (`MergeRunner::markMerged`), no un bug.
  `reporte_coloquial`/`enlace_revision` sí se conservaron pese al rechazo parcial.

**No se tocó ningún registro de `roadmap_items`** — el script es de solo lectura sobre la BD y solo
escribe el archivo `.md`. Sin cambios de código de aplicación.

Cerrado: `completado`, `merge_commit=f02f5ab432c712a60276e618e52fd3f65a8a596b`.
