## 2026-09-14 17:30 — Torre: KPI "Esperan un insumo" junto a "Requiere Irving" (#9990912)

Item sub-item de seguimiento de #9990860, hermano de #9990910 (migración `motivo_espera`,
mergeada) y #9990911 (backfill de los 10 items, completado). Al investigar, resultó que
**#9990906** (CIRC-03 Fase C, familia distinta de items — #9990886→#9990904/#9990905/#9990906)
ya había implementado el 100% del backend y casi todo el frontend que este item pedía:
`RoadmapItem::scopeBandejaDecision()`/`scopeBandejaInsumo()`, `RoadmapController::torre()`
con `cola_espera_insumo` agrupado por `motivo_espera` y `resumenCola.espera_insumo`, y en
`TorreControl.vue` la sección "📦 Esperan un insumo tuyo" con acordeón por tipo.

Lo único que faltaba del spec literal era el KPI dedicado "junto al de 'Requiere Irving'"
(~línea 182 del componente). Se agregó la tarjeta `tc-kpi` "Esperan un insumo" en la fila de
KPIs, usando `resumenCola.espera_insumo` (mismo bloque `resumen_cola` que ya trae el fallback
null-no-cero de #878), con tinte ámbar (`#b45309` claro / `#fbbf24` oscuro) consistente con
`.tc-insumo-grupo-head` para que el número de arriba y la sección de abajo se lean como el
mismo dato.

Verificado con el semáforo `deploy/circuito/npm-build.sh` (compila OK, solo warnings
preexistentes de sass/browserslist ajenos al cambio). Commit `2e01ce78` en
`circuito/item-9990912-torre-bandeja-partida-en-dos-listas-e`, integrado a main vía
`circuito:integrar`. Enlace de revisión: `/releases` → pestaña Panorama (Torre) → fila de
tarjetas KPI, junto a "Requiere Irving".
