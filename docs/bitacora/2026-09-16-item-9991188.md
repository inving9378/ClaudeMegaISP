## 2026-09-16 19:5x — Item #9991188: Inventario responsive — Consolidación final

**Contexto:** sub-item de seguimiento de #9991184 (Fase 2, "Inventario real de problemas de
responsive"), depende de que los 3 inventarios parciales estuvieran cerrados: Grupo A #9991185
(`docs/responsive-inventario-grupo-a.md`), Grupo B #9991186 (`docs/responsive-inventario-grupo-b.md`)
y Grupo C #9991187 (`docs/responsive-inventario-grupo-c.md`). Los 3 ya estaban `completado` y
mergeados a `main` al llegar este item.

**Bloqueo del flujo estándar:** `circuito:cabida 9991188` devolvió `NO CABE`
(`historico_excede_umbral`, ~721s). Al intentar descomponer con `circuito:sub-item`, el comando
rechazó la creación: *"el padre #9991188 ya está a profundidad máxima (3) desde su raíz"*
(cadena #9991182 → #9991184 → #9991188). Como la descomposición prescrita por el flujo está
bloqueada por este guard estructural, y la tarea es acotada y solo-lectura de código (fusionar
453 líneas de markdown en un solo documento, sin tocar código de la app), se decidió ejecutarla
directo en esta misma vuelta. Decisión registrada vía `circuito:reportar --tipo=decision`
(reporte #8317).

**Trabajo realizado:**
- Rama `circuito/item-9991188-inventario-responsive-consolidacion-fi` creada desde `main`.
- Lectura completa de los 3 documentos parciales (141 + 205 + 107 = 453 líneas, 24 hallazgos
  individuales en total: 6 alta / 11 media / 7 baja).
- Redactado `docs/responsive-inventario-item-9991184.md`: fusión aplicando las 2 reglas de fusión
  del padre — (1) orden alta→media→baja; (2) agrupación por causa raíz común entre vistas de
  distintos módulos. Resultado: 24 hallazgos originales → **14 consolidados** (6 alta / 4 media /
  4 baja), de los cuales 5 son fusiones que cubren 15 hallazgos originales:
  - Bootstrap `col-*` sin clase base `col-12`/`col-sm-*` (Tickets `VerTicket.vue` + CRM
    `InformationCrmCrud.vue`, 5 ubicaciones).
  - `flex-wrap` faltante en toolbar/header (Vendedores `VendedorListar.vue` ×2 + CRM
    `CrmDatatable.vue`, 3 ubicaciones).
  - `q-tabs` de Quasar con `:breakpoint="0"` (Clientes `ClientCrud.vue` + Talento Portal
    `app.js`, 2 ubicaciones).
  - Ausencia total de reglas `@media` (Mapa de Red `LeafletMapRed.vue` + Talento Portal
    `portal.css`, 2 ubicaciones).
  - Grid Bootstrap anidado con `class_col` vacío en Inventario (`InventoryItemStockListar.vue` +
    `SelectComponentWithCheckbox.vue`, 3 resoluciones de la misma ubicación).
  Se agregó además una sección de "Deuda de plataforma" (el patrón `class_col` afecta ~29
  componentes compartidos usados por ~60 CRUDs), una sección de vistas ya bien resueltas (para no
  restar tiempo a la fase de corrección) y las limitaciones generales consolidadas de los 3 grupos
  (deduplicadas). Formato: tabla markdown, sin screenshots — igual que los 3 docs origen. Los 3
  docs parciales quedan intactos como evidencia.
- Commit `5554c85a` (solo el .md nuevo, sin tocar código de la app).
- `circuito:estado-item` verificado libre (sin colisión) antes de integrar.
- `circuito:integrar 9991188` — merge encolado para el runner on-box.
- Cierre: `estado_aprobacion=completado`, `reporte_coloquial` y `enlace_revision` fijados,
  reporte de cierre registrado (#8324).
- Anotado en el log de #9991184 (reporte #8325, tipo nota) que su inventario ya está completo y
  listo para las fases de corrección — **sin cerrar #9991184**, el guard de paraguas del sistema
  lo completará solo en cascada cuando este último hijo (#9991188) cierre.

**Verificación:** el cambio es exclusivamente documental (un `.md` nuevo); no aplica `php -l` ni
build de frontend. Se revisó manualmente la estructura de tablas markdown y el conteo de
hallazgos (24 originales = 6+11+7 por grupo, verificado contra las tablas "Resumen de severidad"
de Grupo A y B, y contado directo en Grupo C que no traía resumen propio).

**Pendiente:** ninguno de este item. La cascada de cierre de #9991184 queda automática.
