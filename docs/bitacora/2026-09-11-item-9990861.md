## 2026-09-11 18:30 — Item #9990861: Re-triaje de la cola (PASO 0-2 ejecutados, PASO 3-4 descompuestos)

Item de higiene de metadatos sobre `roadmap_items` (nivel B, re-aprobado por DES-TRABE tras
escalación por falso positivo de "seguridad" en el Revisor). `circuito:cabida` dio CABE, pero al
medir el alcance real del PASO 3 se confirmó que 428 de los 431 items sin `nivel_riesgo` son
metadatos históricos ya cerrados (ver abajo) — trabajo de fondo, no de una sola vuelta. Se
ejecutó lo que sí cabía completo (PASO 0, 1, 2 y la porción viva del PASO 3) y se descompuso el
resto en dos sub-items, siguiendo el propio framing del item ("gradual y con respaldo").

### PASO 0 — Respaldo

`mysqldump` de la tabla `roadmap_items` (recomendado en la propia pregunta estructurada q1,
opción 1, confianza alta, reversible) vía el motor compartido `MysqldumpEngine`:

- `storage/backups/manual/items-20260911-1823.sql` (15 MB, gitignored — `/storage/backups/manual/`
  en `.gitignore:10`) — copia de trabajo de esta terminal.
- `/var/backups/mysql/roadmap_items-manual-20260911-1823.sql` — copia duradera fuera de
  cualquier worktree (sobrevive al ciclo de vida de `wt-3`).
- Verificado: 16 bloques `INSERT INTO`, cabecera/pie de `mysqldump` correctos, 1599 filas en la
  tabla en vivo al momento del dump.
- **Revert:** `mysql --host=localhost --user=<config('database.connections.mysql.username')> -p
  megaisp < /var/backups/mysql/roadmap_items-manual-20260911-1823.sql` (el dump trae
  `DROP TABLE IF EXISTS` + `CREATE TABLE` + datos, así que restaura el estado exacto de las
  18:23 del 2026-09-11 sobre la tabla completa).

### PASO 1 — Coherencia (`status=cancelled` con `estado_aprobacion` vivo)

Query: `RoadmapItem::where('status','cancelled')->whereNotIn('estado_aprobacion',
['cancelado','completado','rechazado'])`. Resultado: **exactamente los 2 casos conocidos**,
ninguno más:

- `#14` — "Sección "Addons" separada en sidebar" — `pendiente_revision` → `cancelado`.
- `#74` — "MegaFamilia: investigar por qué APK no registra fcm_token…" — `pendiente_revision` →
  `cancelado`.

Ejecutado directo (q5, opción 1 recomendada — el propio PASO 1 ya autoriza el cambio, no
justifica una ida-vuelta). Ninguno de los dos tenía `branch` asignado, así que el guard de
"commits sin mergear" del modelo no aplicó. Cada uno quedó con una entrada en su `log` propio
(`evento: coherencia_status_cancelled`) con el motivo.

### PASO 2 — Basura (items sin contenido ejecutable)

**Umbral operativo decidido (q2):** se descartó la lectura literal de la opción 1 ("descripción
vacía" a secas) porque, medida contra la BD real, da **39 falsos positivos** (37 de los 39 items
con `description` vacía tienen un `prompt` sustancial — es sólo que este sistema permite crear
items solo con `prompt`, sin usar la columna `description`). El umbral aplicado en su lugar:
**título < 15 caracteres, sin verbo accionable, Y `prompt` < 100 caracteres (o completamente
vacío)** — conserva la intención de la opción 1 (pocos falsos positivos) sin heredar su
imprecisión sobre este dataset.

Candidatos reales (título<15 chars en TODA la tabla = solo 8 items; de esos, 6 con prompt
trivial):

| ID | Estado | Título | Nota |
|----|--------|--------|------|
| #184 | done/completado | "Más 8tems" | ya cerrado, prompt=86 chars |
| **#185** | **pending/aprobado_irving** | **"Más 8tems"** | **caso conocido del propio item — sigue vivo, `urgente=true`, `priority=alta`** |
| #186 | done/completado | "Más 8tems" | ya cerrado, prompt=86 chars |
| #187 | done/completado | "Más items" | ya cerrado, prompt=86 chars |
| #188 | done/completado | "Más items" | ya cerrado, prompt=86 chars |
| #189 | done/completado | "Más items" | ya cerrado, prompt=86 chars |

Descartados de la lista (mismo patrón de título corto pero con prompt sustancial → contenido
real, no basura): `#190` "Más items" (prompt 4762 chars, vivo) y `#9990721` "permisos" (prompt
7778 chars, vivo).

**Hallazgo secundario (fuera del criterio de PASO 2, pero relacionado):** `#9990058`
("TEST-9990012 padre nivel C") y `#9990059` ("TEST-9990012 hijo") tienen `description` Y
`prompt` **completamente vacíos** (0 caracteres) — parecen residuos de una prueba manual del
comando/servicio de paraguas (#9990012) que quedaron en la BD compartida de dev, ya cerrados
(`done/cancelado` y `done/completado`). No son "basura sin contenido ejecutable" en el sentido
del PASO 2 (nunca tuvieron intención de serlo, son fixtures de prueba) — se reportan aparte para
que Irving decida si vale la pena borrarlos (higiene) o dejarlos como están (ya cerrados, sin
efecto).

**No se borra nada** (instrucción explícita del PASO 2) — lista entregada para que Irving decida
uno por uno, empezando por `#185` que es el único vivo.

### PASO 3 — Nivel de riesgo de los "430" (cola viva resuelta; backlog descompuesto)

Medido contra la BD real: de los **431** items sin `nivel_riesgo`, solo **3** están en la cola
viva (`pendiente_revision`/`aprobado_claude`/`aprobado_revisor`/`requiere_irving`/
`aprobado_irving`/`en_progreso`) — el resto (**428**) son items **ya cerrados**
(422 `completado` + 6 `cancelado`), sin ningún impacto en el pool de despacho.

De los 3 vivos:
- **`#9990733`** ("Mapa de Red — flujo animado de puntos en los enlaces OLT") — **clasificado
  `nivel_riesgo=B`** en esta vuelta. Su propio `prompt` trae una plantilla con
  `NIVEL_RIESGO: B` embebido; se confirmó independientemente con el criterio del circuito
  (feature visual/UX no destructiva, sin tocar dinero/permisos/auth/prod directamente, pero sube
  a producción visible → B, no A).
- **`#185`** — se deja SIN clasificar a propósito: es el mismo item marcado como basura en el
  PASO 2. Asignarle un nivel de riesgo antes de que Irving decida su destino (¿se cancela? ¿se
  corrige el título/prompt?) sería trabajo perdido si se cancela.
- **`#9990853`** ("corrección del Circuito CC — para desatorar el flujo") — se deja SIN
  clasificar: está en `requiere_irving`, ya escalado y a medio resolver por otra vía; clasificar
  su riesgo unilateralmente aquí interferiría con esa decisión pendiente.

Los 428 cerrados se descompusieron en el sub-item **#9990875** (spec con la query exacta, el
primer lote de 50 IDs ya calculado, y el criterio A/B/C a aplicar) — es limpieza de metadatos
histórica sin urgencia, tratable en lotes sucesivos por cualquier terminal futura.

### PASO 4 — Propuesta de re-priorización (descompuesta)

Medido para que el siguiente ejecutor no tenga que re-derivarlo: cola viva = **219** items;
prioridad actual `alta=114 · media=65 · baja=6 · sin prioridad=34` (string vacío, no null — es un
hallazgo de calidad de datos aparte). De los 100 items vivos más recientes, 71 ya están en
`alta` (coherente con el "88/100" que menciona la `description` original del padre; la cifra
exacta se mueve porque la cola es viva). Descompuesto en el sub-item **#9990876** con el
criterio de clasificación (alta = cierre del mes o desbloquea a otros vía `depende_de`; media =
resto vigente; baja = vigente sin fecha) y el formato de entrega
(`docs/roadmap/repriorizacion-YYYYMMDD.md`, tabla Markdown, **sin aplicar nada** — solo
propuesta).

### Resumen de decisiones registradas (`circuito:reportar --tipo=decision`)

Los 5 `q1`-`q5` del brief traían opción recomendada + reversible=true + confianza alta/media, así
que se aplicaron directo (regla de oro) y se registró cada una en el historial del item (reportes
#7656-#7660).

### Sin código de aplicación tocado

Este item es 100% metadatos sobre `roadmap_items` — no toca ninguna vista, controller ni ruta de
la aplicación. El trabajo restante (clasificación retroactiva del backlog cerrado + propuesta de
re-priorización) sigue en #9990875 y #9990876, pendientes de que una terminal los reclame; el
padre #9990861 queda como paraguas hasta que ambos cierren.
