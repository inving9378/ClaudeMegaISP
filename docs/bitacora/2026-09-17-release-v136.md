## 2026-09-17 10:25 — Cerrar el pipeline de releases y emitir V1.36-17.09.2026

**Sesión directa con Irving.** Fases 1–3 = #9991209 (`c49a2ab2`), #9991207 ajuste (`c6fee8e0`),
#9991208 (`b4298ec8`); bitácoras propias por item. Esta entrada cubre la **Fase 4**.

- Paso 0: `2a09ba95`, `d0522c6c` y `25052bf5` tocan PRODUCTO (`TalentoCompensacion.vue`) → emitir
  era obligatorio. #9991207 ya lo había hecho el circuito (`8b1f6909`) marcando `by_design` los
  5 skips, incluido `remote_deploy` → ajustado a `config: opcional|requerida`. `25052bf5` pisó el
  merge `8b1f6909` por el checkout-desync (índice viejo al commitear en el principal 17 s después
  del merge) y `da39fa32` lo revirtió → registrado y ya corregido por el circuito (**#9991211**,
  `8857f9ce`).
- Caches: `view/config/route:clear`, `view:cache`; **`config:cache` NO** (`config:auditar-env`
  exit 1, candado #790). `queue:restart` para que el worker `deploy` cargue el código nuevo.
- Gate obligatorio: `next-version = V1.36-17.09.2026`, **`coverage.desde_tag = V1.35-17.09.2026`**,
  19 commits, sin truncar.
- Bundle `prod` servido 10:15:13 con Fase 2 (`pendiente de configuración`) y Fase 3
  (`release-notes`).
- Emisión: `ReleaseController::store()` como Irving (id 8) con las notas del
  `ReleaseChangelogService` → release 97, deploy **#52**: `success` en **90 s**, preflight
  **VERDE — sin hallazgos**, `git_commit` skipped `by_design`, `github_release` creado
  (390874180), `remote_deploy` skipped `config_pendiente/opcional` → **0 omisiones que tiñen**.
  `releases.97`: `publicada`, `published_at 2026-09-17 10:18:08`, `github_release_id 390874180`.
  Tag `V1.36-17.09.2026` = HEAD (`2a18e52a`), 0 commits sin publicar, tag en origin. Notas
  renderizadas: `<ul><li><strong>Notas de versión con formato mejorado:</strong>…`.
- Changelog acumulado para prod: rango correcto (instalada → V1.36, `v0.3.0` excluido) pero
  `UpdateBanner.vue` lo pinta en `<pre>` → **#9991213** (no se arregló aquí).
- Canal de respuesta: **#9991214** (config:cache omitido, 2 stash reversibles en el principal,
  screenshots a validar). Esta bitácora entra después del tag: son 2 commits docs "sin publicar"
  hasta la próxima versión.
