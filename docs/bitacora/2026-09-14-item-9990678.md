## 2026-09-14 12:02 — F7 (permiso releases.emitir + regla + test) — bucle reap sobre paraguas ya descompuesto (se completa el cierre-intento faltante)

Mismo patrón que #738/#745/#830/#816/#818/#848/#852/#905/#878/#906/#907/#924/#9990012/#917/#910/
#936/#9990408/#962/#9990554/#9990549/#9990624/#9990650/#9990807/#9990826/#9990836/#9990856/
#9990892/#9990896/#9990886/#9990893/#9990878/#9990870/#9991072 (ver CLAUDE.md). #9990678 (F7:
frase de Irving en CLAUDE.md/convenciones + test de regresión + permiso Spatie nuevo
`releases.emitir`, gate en botón/ruta) ya había sido escalado por ANTI-LOOP (Opus) y reaprobado
por Irving con sus 4 preguntas resueltas (verificado: las 4 `opcion_elegida` corresponden a la
opción recomendada de cada una — redacción con contexto en CLAUDE.md, test Feature completo
(permiso+403+botón oculto), registro vía `PermissionSyncService`, gate doble Blade+ruta).

Una vuelta previa (`wt-6`, 2026-09-14 11:57) ya hizo lo correcto: descompuso el trabajo por pieza
en **#9991141** (F7a — registrar el permiso `releases.emitir` y gatear ruta/botón, nivel C),
**#9991142** (F7b — test de regresión permanente, nivel B) y **#9991144** (F7c — documentar la
regla en CLAUDE.md y megaisp-conventions, nivel B). Pero esa vuelta murió (`soltar-claim`, "muerte
del proceso: kill, OOM o freno a media vuelta") **8 segundos después** de crear el último
sub-item, sin intentar cerrar al padre — el ítem volvió a la cola como `aprobado_irving` y el
pool lo repartió de nuevo (a `wt-4`, esta vuelta) sin trabajo propio que hacer.

Verificado esta vuelta: los 3 hijos (`origen_item_id=9990678`) siguen intactos — #9991141 y
#9991142 en `requiere_irving` sin reclamar, #9991144 **activamente reclamado por `wt-3`**
(aislamiento #334, no se tocó) — la descomposición original seguía siendo correcta, nadie más la
tocó. El campo `descomposicion:{"depende_de":[6]}` del item resultó ser residuo irrelevante (item
#6 "Motor de servicios contratables", ya `completado` desde antes, sin relación con F7).

**Corrección:** se ejecutó el intento de cierre faltante (`estado_aprobacion='completado'`); el
guard de paraguas (`RoadmapItem.php` bloque "(2b) PARAGUAS") lo reenrutó a `aprobado_irving` +
`excluir_pool_automatico=true` (evento `paraguas_abierto` en el log: "le quedan 3 sub-item(s)
abierto(s)"), liberando `worker_sid`/`claimed_at` y sacándolo del pool/reaper hasta que el hook de
cierre en cascada (`RoadmapItem.php:459-491`) lo complete solo cuando #9991141, #9991142 y
#9991144 cierren los tres.

**Sin cambio de código de negocio** — el trabajo técnico real de F7 (permiso Spatie, test de
regresión, documentación de la regla) sigue en #9991141/#9991142 (pendientes de que una terminal
los reclame) y #9991144 (a cargo de `wt-3`).
