## 2026-09-14 11:35 — Reintento Q4: bloqueado de nuevo — el corte del crontab al checkout dedicado sigue sin ocurrir (item #9991030)

**Contexto:** item de seguimiento de #9991024, continuación de #9991028 (Q4). El propio spec es
explícito: "no arranca hasta que #9991029 cierre con la fecha/hora exacta (TZ incluida) en que el
crontab quedó apuntando de verdad a `/var/www/megaisp-scheduler`". Sin ese instante como punto de
partida, medir ">=6h reales de `circuito-despacho`" no tiene sentido — sería fabricar una
conclusión sin el dato que la sostiene (mismo criterio ya aplicado en #9990896).

**Verificación (solo lectura, sin tocar nada):**
- `/var/www/megaisp-scheduler` **sigue sin existir** (`ls` → "No existe el fichero o el directorio").
- `crontab -l` (usuario `meganet`): TODAS las líneas `circuito:*` (incluida `circuito:scheduler`)
  siguen apuntando a `/var/www/megaisp/deploy/circuito/cron-wrap.sh` — el checkout **compartido**,
  no al dedicado. Ninguna línea usa `cron-wrap-scheduler.sh`.
- `deploy/circuito/cron-wrap-scheduler.sh` sigue en el repo (mergeado por #9991026), intacto y sin
  usarse.
- Leí el cierre real de #9991029 (no solo el título): **cerró `aprobado_irving` +
  `excluir_pool_automatico=true` + `motivo_espera=frontera_produccion`, NO `completado`**. Su
  propio `reporte_coloquial` dice textualmente: "No se ejecutó el corte del cron... requiere una
  sesión humana fuera del sandbox del circuito". Es decir, la precondición que este item (#9991030)
  necesita para arrancar **nunca se cumplió** — #9991029 documentó el bloqueo (igual que #9991026
  antes) y quedó parqueado para que Irving hiciera el paso manual, pero ese paso manual todavía no
  pasó.

**Consulté a Thomas** (mismo dilema que ya resolvió para #9991026 y #9991029, ahora en la tercera
vuelta de la misma cadena):

> **Pregunta:** el item depende de un cutover real que #9991029 documentó como NO ejecutado (sigue
> pendiente de Irving); ¿confirmo el mismo patrón — documentar sin tocar `/var/www` ni el crontab,
> y parquear — o intento el corte yo mismo?
> **Opción recomendada (reversible):** documentar el estado verificado (sin cutover) y parquear el
> item igual que #9991029, sin tocar `/var/www` ni el crontab real.
> **Veredicto:** PROCEDE con la opción recomendada.

**Por qué no se ejecuta el cutover aquí:** exactamente la misma razón que #9991026/#9991029 — crear
`/var/www/megaisp-scheduler` y editar `crontab -e` del sistema violan mi regla dura de aislamiento
(#334: nunca tocar `/var/www/megaisp` ni recursos compartidos fuera de mi worktree) y están fuera
del alcance de un agente on-box. No hay bypass razonable: forzarlo sería repetir el mismo choque ya
documentado dos veces.

**Estado del item:** se parquea (`estado_aprobacion=aprobado_irving`, `excluir_pool_automatico=true`,
`motivo_espera=frontera_produccion`) — NO se marca `completado` porque no se pudo medir nada (la
ventana de 6h ni siquiera empezó). Q1/Q2 del spec original **siguen sin respuesta** — no se pueden
responder sin el cutover real.

**Pendiente — ejecución manual de Irving (idéntico a lo ya documentado en #9991026 y #9991029, sin
cambios, tercera vez que se repite el mismo pendiente):**
1. Desde `/var/www/megaisp`: `php artisan circuito:provision-worktree --path=/var/www/megaisp-scheduler --base=main`
   (o el mecanismo equivalente ya documentado).
2. Verificación en seco desde el worktree nuevo antes de tocar cron real.
3. `crontab -e` (usuario `meganet`): comentar (no borrar) la línea vieja de `circuito:scheduler`
   apuntada a `cron-wrap.sh`, agregar la nueva apuntada a `cron-wrap-scheduler.sh`.
4. Esperar 1-2 ticks reales, revisar logs de error del wrapper dedicado.
5. Anotar la fecha/hora exacta (TZ incluida) del corte — ese instante es el nuevo punto de partida
   real de las >=6h que este item necesita para poder correr.
6. Solo entonces reactivar este item (o crear uno nuevo) para hacer el grep de
   `storage/logs/circuito-despacho-*.log` en la ruta del checkout dedicado y responder Q1/Q2.

**Recomendación para no repetir esta misma cadena una cuarta vez:** este es ya el tercer item
(#9991026 → #9991029 → #9991030) que topa con el mismo bloqueo idéntico y se cierra documentando
lo mismo sin poder avanzar. Si se vuelve a generar un ítem de seguimiento sobre esta misma cadena
sin que el paso manual (crontab/worktree) haya ocurrido, vale la pena que Irving lo tome
directamente en vez de dejar que el circuito siga midiendo el mismo bloqueo tres, cuatro, cinco
veces.
