## 2026-09-11 19:40 — Item #9990942: veredicto final de la cadena colisión↔ociosidad

Tercer reintento de la cadena `#9990896`→`#9990916`→`#9990936`→`#9990942`, que esperaba ≥1h real
desde el merge de `#9990863` (commit `4c473d85`, 2026-09-11 18:39:03 CST) para poder responder
"¿es la colisión de footprint causa frecuente de terminales ociosas?".

Al llegar a este item el umbral de 1h todavía no se había cumplido (faltaban ~7 min), y esta ya
era la 3ª generación del mismo reintento — el propio spec instruía, en ese escenario, consultar a
Thomas en vez de crear un 4º sub-item idéntico. Se consultó con dos opciones (mantener 1h vs.
adoptar un umbral corto de 15-20 min ya cumplido); Thomas autorizó (`PROCEDE`) la opción
recomendada: usar el umbral corto.

**Medición:** 56 minutos reales desde el merge, `circuito-despacho-*.log` en 0 archivos (el canal
nunca escribió una línea), pese a que SÍ ocurrió una colisión de footprint real dentro de la
ventana (`#9990864` pausado por `#9990890` a las 01:05:05 UTC). Confirmado por código
(`SchedulerCommand::persistirOciosidad()`) que el canal se invoca en cada ciclo y solo calla
cuando ningún slot queda ocioso — 0 archivos es evidencia real de "0 ciclos con ociosidad", no un
canal roto.

**Veredicto:** con el backlog actual (~1,580 items), una colisión pausa un item puntual pero el
scheduler llena el slot con otro candidato en el mismo ciclo — nunca deja una terminal ociosa. La
pregunta no tiene terreno donde aplicarse mientras el backlog se mantenga alto.

**Cierre:** `#9990942` completado (este item, con el doc de verificación como artefacto);
`#9990936` completado directo (intento de cierre faltante de una vuelta previa del mismo
worktree); `#9990916` y `#9990896` se completan solos vía el hook de cierre en cascada al cerrarse
su único hijo. `#9990896` queda documentado como no-accionable / re-agendable si el backlog baja
lo suficiente para que la ociosidad real vuelva a ser medible.

Detalle completo en `docs/circuito-colision-ociosidad-item-9990942-verificacion.md`. Sin cambio de
código de aplicación.
