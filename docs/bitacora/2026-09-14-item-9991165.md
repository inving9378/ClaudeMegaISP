## 2026-09-14 14:05 — #9991165 Adjuntos del roadmap · Fase 3 (amarre con el circuito)

**Sesión directa con Irving.** Nivel B. Sub-item de #9991162. Puntos 12–15: lo que evita repetir
#9990934.

### Commits
- Punto 12 — `circuito:adjuntos-prompt {id}` (`Console/AdjuntosPromptCommand.php`) imprime el
  bloque `## ADJUNTOS DE ESTE ITEM (léelos antes de empezar)` con la **ruta absoluta** y la
  descripción de cada adjunto (`RoadmapAdjuntoService::bloquePrompt()`); `vuelta.sh` lo antepone al
  prompt del item junto al bloque de respuestas de CIRC-02b. El `prompt` en BD no se toca.
- Punto 13 — **fail-closed con dos candados**: `RoadmapCircuitoService::claimNextParalelo()`
  llama `guardAdjuntosEnDisco()` ANTES del claim atómico (el item ni se reclama), y el comando lo
  repite al arrancar (exit 2 → `vuelta.sh` hace `return 1`, no lanza `claude -p`, el trap suelta el
  claim). El guard marca `requiere_irving` + `motivo_espera=adjunto_faltante` (nuevo valor en
  `MOTIVOS_ESPERA_INSUMO` → bandeja "Esperan un insumo tuyo"), escribe las rutas en
  `motivo_bloqueo`, evento `adjunto_faltante_no_arranca` en el log y aviso en `circuito_despacho`.
- Punto 14 — `adjuntos[]` en `itemDetallePayload` + lista en `RoadmapItemDetalle.vue`;
  `adjuntos_n` por nodo en `panoramaJerarquia` (subconsulta aditiva) + `📎 N` en
  `TorreArbolNodo.vue` con enlace a la ficha.
- Punto 15 — `roadmap:adjunto-promover {id} --destino=docs/maquetas`: copia con nombre saneado,
  imprime `git add` + `git commit` sugeridos y **no** los ejecuta.

### Verificación (item y adjunto de prueba propios, borrados al final)
- V7: `circuito:adjuntos-prompt` → exit 0 y el bloque con
  `/var/www/megaisp/storage/app/roadmap/adjuntos/<uuid>.html — maqueta de Panorama en árbol…`.
- V8: se borró el archivo a mano → exit 2, "Vuelta NO ARRANCA", item `requiere_irving` /
  `adjunto_faltante` / `worker_sid=null`, evento en log y línea en `circuito-despacho-*.log`.
  Vuelto a `aprobado_irving` con el archivo ausente, `circuito:claim-next --item` **no reclamó**
  (salida vacía) y volvió a marcarlo. Re-subir el mismo archivo revivió el registro (mismo id) y
  `adjuntos-prompt` volvió a exit 0.
- Punto 15: copia a `docs/maquetas/panorama-arbol.html` + comandos impresos; el archivo quedó
  untracked (sin commit automático); se borró la copia de prueba.
- `bash -n vuelta.sh` OK; `php -l` en todo lo tocado; build del frontend OK.
