## 2026-09-12 03:15 — Item #9991001: seguimiento de la pregunta sin resolver de #9990968 (RESUELTO — ya aplicado antes de crearse)

`#9991001` pedía decidir qué hacer con los permisos Spatie `torre.cola.ver` y
`torre.salud.manage` tras la baja de las pestañas Cola/Reporte/Salud del entorno en
`#9990968`. Irving aprobó la Opción 2 (dejar los permisos huérfanos en BD, solo quitar
UI/rutas — clave `92b8302de589aee6`).

Investigado: es la misma carrera de timing documentada varias veces en `CLAUDE.md`
(#733/#741/#753/#9990003/#9990353/#9990658). El ejecutor de `#9990968` ya había tomado y
aplicado exactamente esa decisión (log `[2026-09-11 20:27 · wt-2 · decision]`: "Opcion
recomendada de q2 ... tambien aplicada"), pero nunca escribió `opcion_elegida` en el
arreglo estructurado de preguntas del propio item → el generador automático de
seguimientos creó `#9991001` sin verlo resuelto.

Verificado contra `main` (merge `a86f7580d7`): sin migraciones de permisos, ambos
permisos siguen existiendo en `permissions` (id=733/735), sin ningún consumidor en
código salvo el catálogo descriptivo `CatalogoPermisosCircuito.php` — exactamente la
Opción 2. Detalle completo en
`docs/permisos-torre-cola-salud-item-9991001-verificacion.md`.

**Sin cambio de código de aplicación** — se cierra documentando la verificación.
