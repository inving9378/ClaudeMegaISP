## 2026-09-12 03:13 — CIRC-04: paraguas ya descompuesto, se completa el intento de cierre faltante

Item #9990870 ("CIRC-04 — Re-triaje de la cola: coherencia cancelados, basura y 430 items sin
nivel de riesgo") llegó a `wt-6` reencolado por `reaper-rapido` (huérfano, `reap_count=1`) tras
un timeout previo por `max_turns` sin commits.

Verificado: el item **ya estaba correctamente descompuesto** por una vuelta anterior en 4
sub-items (`origen_item_id=9990870`):
- `#9991008` PASO 0+1 (backup + coherencia cancelados) — `completado`
- `#9991009` PASO 2 (reportar basura, incluye #185) — `en_progreso`, reclamado activamente por `wt-3`
- `#9991010` PASO 3 (nivel_riesgo de los 430) — `completado`
- `#9991011` PASO 4 (propuesta de re-priorización) — `en_progreso`, reclamado activamente por `wt-4`

Mismo patrón documentado extensamente en `CLAUDE.md` (familia #738/#745/#830/#816/#818/#848/
#852/#905/#878/#906/#907/#924/etc.): la descomposición era correcta, pero ninguna vuelta previa
intentó el cierre del padre — se quedó `en_progreso` colgado y el reaper lo repartió de nuevo sin
trabajo propio que hacer.

**Corrección aplicada:** se ejecutó el intento de cierre (`estado_aprobacion='completado'`) sobre
#9990870. El guard de paraguas (`RoadmapItem.php` bloque "(2b) PARAGUAS") lo detectó con 2
sub-items abiertos (#9991009 y #9991011, ambos con dueño activo — no se tocaron, aislamiento
#334) y lo reenrutó a `aprobado_irving` + `excluir_pool_automatico=true`, liberando
`worker_sid`/`claimed_at`. Log confirmado: evento `paraguas_abierto`, "le quedan 2 sub-item(s)
abierto(s)".

Sin cambio de código de negocio — el trabajo técnico real (reportar basura y proponer
re-priorización) sigue en #9991009/#9991011, a cargo de `wt-3`/`wt-4`. El hook de cierre en
cascada (`RoadmapItem.php:459-491`) completará #9990870 solo cuando ambos cierren.
