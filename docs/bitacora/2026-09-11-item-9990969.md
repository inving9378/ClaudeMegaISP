# Item #9990969 — CIRC-09 Fase 2: endpoint de jerarquía Módulo→Épica→Item→Sub-item con lazy-load

## 2026-09-11 20:35 — Implementado, verificado y mergeado

Sub-item de #9990934 (CIRC-09: Panorama en árbol). Ejecuta la FASE 2 del prompt original del
padre: construir el o los endpoints que arman la jerarquía Módulo → Épica → Item → Sub-item con
contadores por nivel, calculados en SQL.

**Decisiones ya resueltas por Irving en el brief del propio item** (verificadas contra hash
`substr(sha1(texto_opción), 0, 16)` de cada `opcion_elegida`, las 3 coinciden con la opción
recomendada):
- q1 → un solo endpoint parametrizado `GET .../torre/panorama-jerarquia?nivel=X&parent_id=Y`
  que devuelve solo los hijos directos del nodo pedido (nunca el árbol completo).
- q2 → payload mínimo por nodo: `id`, `titulo`, `nivel`, `tiene_hijos`, `contador_hijos`, `estado`.
- q3 → reusar el permiso `roadmap_view` existente (no crear permiso Spatie nuevo).

**Implementación:**
- `RoadmapController::panoramaJerarquia()` (nuevo) + 3 helpers privados (`sqlVivo`, `sqlActivos`,
  `sqlDetenidos`) que arman las condiciones SQL como strings reusables por alias de tabla.
- `nivel=modulo` (sin `parent_id`): agrega directo sobre `roadmap_items` agrupando por la columna
  `modulo` (no es tabla propia), contando solo hijos raíz (`origen_item_id IS NULL`).
- `nivel=epica` (`parent_id`=nombre de módulo) y `nivel=item` (`parent_id`=id de cualquier item,
  RECURSIVO — sirve igual para hijos de una épica que para nietos de cualquier item, caso real
  MR-23 con sub-épicas encadenadas vía `origen_item_id`): self-join
  `roadmap_items AS parent LEFT JOIN roadmap_items AS child ON child.origen_item_id = parent.id`,
  contadores `SUM(CASE WHEN ... THEN 1 ELSE 0 END)` en 3 baldes:
  - `activos` = una terminal lo trabaja ahora (`en_progreso` / `in_progress` / `en_desarrollo_humano`)
  - `detenidos` = frenado (`requiere_irving` / `bloqueado_por_bucle` / colisión en vuelo /
    `excluir_pool_automatico`)
  - `en_cola` = vivo y no cae en los dos anteriores
- Por default solo cuenta lo VIVO (`archivado_at` NULL, `status`/`estado_aprobacion` no cerrados) —
  los completados no viajan salvo que se despliegue ese nodo, tal como pedía el criterio del padre.
- Paginación (`page`/`per_page`, tope 200) dentro de `nivel=epica`/`nivel=item` cuando un módulo
  trae más de 200 hijos.
- Ruta nueva: `GET /api/roadmap/torre/panorama-jerarquia` (bajo el grupo `web`+`auth` existente),
  gate `roadmap_view` vía `$this->authorize()` dentro del controller (mismo patrón que el resto
  de endpoints de la Torre — `torreSemaforo`, `torreCola`, `atorados`, etc.).

**No hecho a propósito (documentado para la Fase 4, instrucción explícita del padre):** la "fase
actual" del pipeline (Triage/Decisión/Rama/Editando/Verificando/Integrando) NO existe como columna
— no se inventó aquí. Se deja anotado en el docblock del método que puede derivarse de
`estado_aprobacion` + presencia de `branch`/`merge_commit`, ya resuelto como accessor PHP en
`RoadmapItem::getEstacionAttribute()`/`getEstadoColaAttribute()`, pero no como columna ni
expresión SQL agregable — eso lo resuelve quien construya la Fase 4 (UI).

**Verificado con tinker (autenticado como admin, simulando `Request` directo al controller):**
- `nivel=modulo`: 21 módulos vivos devueltos; para "Auditoria", suma de los 3 baldes = 4,
  contra `COUNT` directo de épicas vivas del módulo = 4 → **MATCH**.
- `nivel=epica` (parent_id=Auditoria): 4 nodos; para el item #9990721 ("permisos"), suma de
  baldes = 4, contra `COUNT` directo de hijos vivos de ese item = 4 → **MATCH**.
- `nivel=item` (parent_id=9990721, recursivo): 4 nodos devueltos, uno de ellos (#9990745) con
  `tiene_hijos=true` y su propio balde `detenidos=1` — confirma la recursión ítem→ítem sin
  necesitar saber si el padre era épica o no.
- Payload de `nivel=modulo` confirmado chico: solo 6 claves por nodo, sin épicas/items anidados.
- Paginación: `per_page=2` sobre 4 épicas de "Auditoria" devuelve 2+2 páginas correctas, `total=4`
  estable en ambas.
- Validaciones: `nivel=epica` sin `parent_id` → 422 con mensaje claro; `parent_id` inexistente en
  `nivel=item` → `ok:true, nodos:[], total:0` (no error, simplemente vacío); `nivel` fuera del
  enum → `ValidationException`.
- `php -l` limpio en los 2 archivos tocados + `php artisan --version` bootea + `route:list`
  confirma la ruta registrada.

**Commit:** `621fc0f1` en la rama `circuito/item-9990969-circ-09-fase-2-endpoint-de-jerarquia-m`,
encolado para auto-merge a `main` vía `circuito:integrar`.

**Archivos tocados:**
- `app/Modules/Addons/Roadmap/Controllers/RoadmapController.php` (+157 líneas: método nuevo +
  3 helpers + import de `DB`)
- `app/Modules/Addons/Roadmap/routes.php` (+4 líneas: ruta nueva)

Sin UI propia en esta fase (endpoint JSON puro) — la Fase 4 (visualización en árbol de la Torre)
es la que consume esto, y queda pendiente como otro item del roadmap.

## 2026-09-11 20:43 — Cierre de bookkeeping faltante (mismo item, vuelta separada en wt-2)

El código de la entrada anterior ya estaba completo y mergeado a `main` (commits `621fc0f1` +
`fd168b82`, integrados vía `214cca97` + `b49cabf5`) — pero el item en la Hoja de Ruta se quedó
`en_progreso`, sin `estado_aprobacion=completado` ni `reporte_coloquial`/`enlace_revision`: la
vuelta anterior murió justo en el último paso (cerrar el item), antes de escribirlo. El pool
volvió a reclamarlo (mismo `worker_sid=wt-2`) sobre la MISMA rama, que `circuito:rama` encontró
"ya existía" y solo rebasó sobre el `main` más reciente (diff vacío — nada nuevo que picar).

Esta vuelta: (1) casi repite el error por no leer el código primero — llegó a escribir una
ruta+método `circuito/jerarquia`/`circuitoJerarquia` duplicados antes de notar que
`panoramaJerarquia`/`torre/panorama-jerarquia` ya existían en `main`; revertido sin commitear.
(2) Re-verificó de punta a punta contra el código real ya en `main` (autenticado como user 3841,
rol DESARROLLADOR, `can('roadmap_view')=true`):
- `nivel=modulo`: 21 módulos, payload 3.3 KB, sin anidados.
- Contadores de "Auditoria" (activos=0/en_cola=1/detenidos=3) == `COUNT` directo por los mismos
  3 baldes → **MATCH**.
- Recursión de 3 niveles reales: `#936` (MR-00) → `#944` (MR-08, con hijos propios) → `#9990504`/
  `#9990505` (MR-08 Fase 2/3, cada uno con `tiene_hijos=true` propio) — confirma que `nivel=item`
  sirve en cualquier profundidad sin distinguir épica de item.
- `nivel=epica` sin `parent_id` → 422 con mensaje claro; `nivel` fuera del enum →
  `ValidationException`. `php -l` limpio, ruta viva en `route:list`.
(3) Cerró el item: `estado_aprobacion=completado`, `reporte_coloquial`/`enlace_revision` poblados,
`merge_commit` apuntando al commit real de código (`621fc0f1`). Sin commits de código nuevos —
el trabajo de fondo ya estaba en `main` desde la vuelta anterior.
