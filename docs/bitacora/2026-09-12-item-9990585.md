## 2026-09-12 13:30 — Fase 1b Modelo DocumentTemplate fillable status+updated_by (RESUELTO — ya aplicado antes de reclamarse)

Item pedía agregar `status`/`updated_by` al `$fillable` de `app/Models/DocumentTemplate.php` y
verificar si `BaseModel` auto-llena `updated_by`. Verificado: el `$fillable` ya tenía ambos campos
desde el commit `3f667c19` (2026-09-07 19:02, 6 min después de crearse este item) — ya ancestro de
`main`. `BaseModel` NO auto-llena esas columnas (solo usa `LogsActivity`); el auto-stamp real viene
de una asignación explícita en `DocumentTemplateController::update()`
(`$template->updated_by = auth()->user()?->id;`) que ya funciona correctamente, verificado en
tinker con transacción+rollback. Detalle completo en
`docs/document-template-fillable-item-9990585-verificacion.md`. Sin cambio de código de
aplicación — solo documentación.
