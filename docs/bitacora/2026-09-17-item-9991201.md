## 2026-09-17 08:xx — Dropdown de prospecto CRM en "Nueva Orden de Trabajo" + estilos + guard

Sesión directa con Irving (David en la sesión de prueba). Cierra el paraguas #9991201
completo: Fase A (#9991202 → #9991204 endpoint, #9991205/#9991206 ya mergeados de
sesiones previas) y Fase B (#9991203, frontend), más dos pedidos adicionales del mismo
hilo (estilos del select "Tipo de orden" y verificación de campos requeridos).

**1. Botones Ver/Iniciar (pedido previo del mismo hilo):** ambos usaban `tc-btn-seg`
(outline azul) — se veían idénticos pese a ser acciones de peso distinto. Nuevas
variantes rellenas locales: `tc-btn-info` (azul `--tc-info`, para "Ver") y
`tc-btn-primary` (índigo `#4f46e5`/`#6366f1` oscuro, para "Iniciar"/"Completar").
Commit `925605ea`.

**2. Fix `--tc-surface` oscuro (pedido previo):** el card de Talento — Órdenes de
Trabajo se veía azul-navy (`#151d2e`) en oscuro, distinto al look "transparente" ya
establecido en Vendedores. Causa: Vendedores envuelve su card en `<q-card
class="tc-card">`, que cae bajo el reset global `[data-layout-mode="dark"] .q-card
{ background:#1A1A1A !important }` — Talento usa `<div class="tc-card">` (sin
q-card) y sí mostraba el navy crudo del token. Se unificó `--tc-surface` (oscuro) a
`#1a1a1a` en `_torre-theme.scss` (Vendedores no cambia, Talento ahora combina).
Commit `ea805167`.

**3. Dropdown de prospecto CRM (#9991201/#9991204/#9991203):**
- Backend: `GET /talento/api/prospectos-crm` (`OrdenTrabajoUnifiedService::
  searchProspectosCrm`) — 2 grupos: "propios" del colaborador seleccionado
  (`crm_techical_user_id = colaborador.user_id`, decisión q3 de Irving) y
  "generales" (resto). Filtro "activo" = `Nuevo/Contactado/Interesado/Instalacion`,
  excluye `Ganado`/`Perdido` (decisión q1). Verificado con datos reales de dev
  (colaborador_id=8/user_id=12 → 7 prospectos propios).
- Frontend: campo "Prospecto (CRM)" opcional en el modal, autocomplete con los 2
  grupos visibles al enfocar (antes de escribir) + búsqueda en vivo, botón × para
  quitar, aviso de exclusión mutua con `client_id` (defensivo — el modal no tiene
  selector de cliente hoy). `saveOrder()` manda `crm_lead_id` en el POST.
- Fix de paso: el catch de `saveOrder()` solo leía `e.response.data.message`, pero
  los 422 del backend devuelven `{"error": "..."}` → los mensajes de validación
  (incluidos los nuevos) nunca se mostraban. Corregido a leer `.error` primero.

**4. Estilos del select "Tipo de orden":** chevron SVG propio (claro/oscuro) +
anillo de foco con `--tc-accent`, en vez del look nativo del navegador.

**5. Campos requeridos / registro sin colaborador:** Irving señaló que la OT #1684
aparece con la columna Colaborador vacía. Investigado a fondo:
- `store()` YA valida `colaborador_id` como `required|exists:...` — la fila de
  `talento_colaboradores` SIEMPRE existe.
- Pero la asignación real ocurre en un paso aparte (`task->users()->sync([$colaborador
  ->user_id])`, sólo `if ($colaborador->user_id)`) — si esa columna fuera nula, la
  orden se crearía igual, sin colaborador visible, en silencio. Se agregó un guard
  explícito (422 antes de crear nada) para ese caso.
- Verificado que el escenario real NO es reproducible hoy: `talento_colaboradores
  .user_id` es `NOT NULL` a nivel de esquema y hay 0 filas con FK huérfana en dev.
  La OT #1684 (única con este problema, de 5 OTs totales) es `[Migrado OT #3]`,
  creada `2026-06-07` — un registro histórico de import, de antes de que este flujo
  existiera, no algo que el `store()` actual pueda producir. El guard queda como
  defensa hacia adelante, no como fix de un bug vivo.

**Nota de proceso — checkout desincronizado:** al llegar a este trabajo, `git diff`
mostró `TalentoWorkOrderController.php`/`OrdenTrabajoUnifiedService.php` con cambios
sin commitear que en realidad ELIMINABAN el trabajo de #9991205/#9991206 (ya
mergeados). Verificado byte a byte: el working tree estaba en el commit justo
ANTERIOR a esos merges — mismo bug ya documentado (#9991086, `syncCheckoutPrincipal`
nunca funcionó). Se resolvió con `git stash push` (reversible, no `checkout --`) en
vez de asumir que era trabajo real de otra terminal.

**Commits:** `925605ea` (botones), `ea805167` (tc-surface), `13df31dd` (prospecto +
estilos select + guard). Todo verificado con Playwright + login real (usuario
`david_marsal`), en claro y oscuro. Roadmap: #9991204/#9991203 marcados
`completado` con `merge_commit=13df31dd`; cascada cerró #9991202 y #9991201
(paraguas completo).
