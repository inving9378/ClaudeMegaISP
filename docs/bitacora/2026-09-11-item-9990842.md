## 2026-09-11 16:47 — Fase 4a-ii: repaso de los 9 criterios de aceptación del buscador v2 de #9990803

Item de solo-verificación (Opción 1 aprobada por Irving: "checklist automatizado, tabla PASA/FALLA
dual"). Sin código, sin rama. Se activó `CLIENTES_BUSQUEDA_V2=true` temporalmente en el `.env` del
worktree (nunca commiteado) + `config:clear`, se corrieron los 9 criterios vía tinker llamando
directo a `ClientDatatableHelper::fetch_datatable_data()` (mismo camino que usa el datatable real),
y se restauró el flag a `false` al terminar.

**Resultado: 9/9 PASA.**

1. Nombre parcial "AARON" — v2 y v1 devuelven exactamente los mismos 10 clientes (390, 782, 1111,
   1158, 2710, 3501, 4098, 5008, 6634, 7326). PASA.
2. SN completo `HWTC5939269F` con columna SN visible — 1 resultado, client_id 6632. Nota: solo
   funciona si la columna visible es `sn_ont_smartolt` (mapea a `olt_onus.sn`, donde vive el SN en
   formato corto). `gpon_ont`/`modem_sn` NO sirven para este término exacto porque guardan el SN en
   formato hex canónico (`485754435939269F`) — `ClienteSearchService::normalizarSn()` existe pero no
   está conectado al pipeline de match (`normalizar()` solo hace mayúsculas+trim para tipo `sn`).
   PASA (tal como el propio item anticipaba con "resuélvelo vía sn_ont_smartolt").
3. Substring del SN `939269F` con columna visible — 2 resultados (989, 6632); verificado que ambos
   comparten literalmente el mismo `modem_sn` en BD (dato real, no bug del buscador). PASA.
4. SN completo con columna SN ausente — 0 resultados. Se revisó el JSON de respuesta
   (`draw,recordsTotal,recordsFiltered,data,color_datatable`): no existe ningún flag "sin
   coincidencias en columnas visibles" — confirma que esa pieza (D3.3, botón "Buscar en todos los
   campos") sigue sin construirse en el backend, tal como el propio item ya anticipaba. No se
   implementa aquí (fuera de alcance de un item de solo-revisión). PASA.
5. `sn:HWTC5939269F` con columna oculta — 1 resultado, 6632 (el prefijo explícito ignora columnas
   visibles). PASA.
6. Teléfono con formato `"1 722-849-8401"` — 5 resultados (6793, 6794, 6797, 6798, 7087), los 5
   verificados con `phone=17228498401` real en BD. PASA (7087 no estaba en el ejemplo del item pero
   es un match legítimo, mismo teléfono compartido).
7. Filtro "solo activos" + búsqueda "AARON" — 0 clientes inactivos en el resultado (1979
   resultados). PASA el criterio literal, pero con un hallazgo importante: la razón NO es el
   encapsulado correcto de v2 — es que `ScopeClient::scopeFilters()` **ignora `$search` por
   completo** cuando hay un `$filter` presente (rama `elseif` línea 52-86, caso
   `client_main_information.estado` nunca usa `$search`). Confirmado idéntico en v1 y v2
   (`v2_habilitado=false` da el mismo 1979) → es un **bug preexistente, anterior a #9990803**, no
   una regresión de este trabajo. Documentado con detalle exacto en sub-item **#9990846** para
   triaje aparte, sin bloquear el cierre de este item.
8. Término de 2 caracteres "ab" — 5633 resultados = total de clientes con `deleted_at` NULL (sin
   filtrar, `min_caracteres=3` respetado). PASA.
9. Columna fuera de whitelist (`users.password`) — 10 resultados, idéntico a sin esa columna, sin
   excepción 500. PASA.

**Cierre:** `estado_aprobacion='completado'` + `cierre_sin_codigo_motivo` + `reporte_coloquial` +
`enlace_revision` puestos en el item, pero el guard de paraguas del modelo (`RoadmapItem.php`, "(2b)
PARAGUAS") lo reenrutó a `aprobado_irving` + `excluir_pool_automatico=true` al detectar el sub-item
`#9990846` abierto — comportamiento esperado del sistema (parqueo correcto, `worker_sid`/`claimed_at`
liberados), se completará solo vía el hook de cierre en cascada cuando `#9990846` cierre.

Sin cambio de código de negocio en este item — trabajo de solo-verificación. El hallazgo de
búsqueda+filtro (bug real, preexistente, con impacto de UX en el listado de Clientes) queda en
`#9990846` pendiente de triaje.
