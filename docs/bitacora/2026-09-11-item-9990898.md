## 2026-09-11 20:20 — Fase 2c: simulación histórica de la exención de docs en el detector de colisiones

Sub-item de #9990892. Premisa original (que la Fase 2b #9990897 ya tenía el filtro implementado)
resultó falsa: 2b cerró sin código porque la Fase 2a (#9990942) midió que la colisión de footprint
no es causa frecuente de terminales ociosas hoy. Aun así, se corrió la simulación pedida —
independiente de esa medición— sobre los 64 eventos `colision_pausada` reales del histórico de
`roadmap_items.log`: **6/64 (9.4%)** se habrían evitado con la regla de exención de docs
(`CLAUDE.md`, `docs/**`, `CHANGELOG*`, `*.md`), 0 con overlap de frontera dura. Detalle completo
en `docs/circuito-simulacion-exencion-docs-item-9990898-verificacion.md`.

Resultado >0 → se implementó la regla per el propio spec del item: `esRutaExentaDeColision()`
(nuevo método público en `RoadmapCircuitoService`) + wiring en `decidirColisiones()` (descarta
archivos exentos de los comunes antes de decidir colisión; las tablas de migración nunca son
exentas). 8 tests nuevos en `ColisionPorTablaMigracionTest.php` (25/25 pasan, incluidas las 17
preexistentes intactas). Cambio puro, aditivo, reversible, sin flag — no toca BD, producción ni
configuración.
