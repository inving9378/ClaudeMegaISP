## 2026-09-11 20:46 — CIRC-05 pieza B: bucle reap sobre paraguas ya descompuesto

Item #9990947 (CIRC-05 pieza B — endurecimiento de la creación externa) reclamado en `wt-6`. Al
leerlo, el `comentarios_claude` mostraba que una vuelta previa (`wt-1`, 2026-09-11 20:15) ya había
hecho el trabajo correcto: corrió `circuito:cabida` (NO CABE) y descompuso las 4 piezas del
endurecimiento en sub-items concretos con archivo/línea exactos (#9990958 límite de profundidad,
#9990959 auditoría con scope, #9990960 canal de respuesta, #9990961 deprecación del manual). Pero
esa vuelta nunca intentó cerrar al padre — quedó `en_progreso` colgado, el reaper lo re-encoló, y
el pool lo repartió de nuevo sin trabajo propio que hacer. Mismo patrón documentado ya ~20 veces
en `CLAUDE.md` (familia #738/#745/#830/.../#936/#962/#9990624/etc).

Verifiqué el estado real de los 4 hijos: #9990958 y #9990959 ya `completado`; #9990960
(`aprobado_irving`) y #9990961 (`aprobado_revisor`) siguen abiertos sin reclamar — la
descomposición original seguía siendo correcta, nadie más la tocó. Ejecuté el intento de cierre
faltante sobre #9990947 (`estado_aprobacion='completado'`); el guard de paraguas del modelo
(`RoadmapItem.php` bloque "(2b) PARAGUAS") lo reenrutó a `aprobado_irving` +
`excluir_pool_automatico=true`, sacándolo del pool/reaper hasta que el hook de cierre en cascada
complete el padre solo cuando #9990960 y #9990961 cierren.

Documenté el hallazgo en `docs/roadmap-bucle-reap-item-9990947-verificacion.md`, commiteado en
`circuito/item-9990947-circ-05-pieza-b-endurecimiento-de-la-c` e integrado a `main`
(`merge_commit=9b976ef3`). Reporté cierre + `reporte_coloquial`/`enlace_revision` en el item.

**Sin cambio de código de negocio.** El trabajo técnico real del endurecimiento sigue en
#9990960/#9990961, pendientes de que una terminal los reclame.
