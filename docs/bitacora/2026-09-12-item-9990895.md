## 2026-09-12 02:20 — Fase 5 (tablero "terminales ociosas x minutos" + "elegibles reales" en la Torre) — descompuesto, sin código propio

El item ya venía con `estado_aprobacion=aprobado_irving` y las 4 preguntas estructuradas
resueltas por Irving (verificado por hash sha1: las 4 respuestas corresponden a la Opción 1
recomendada de cada pregunta — q1: panel permanente fijo arriba de la Torre; q2: "ociosa" = sin
item asignado >3 min, "elegible real" = `RoadmapItem::scopeDespachable()->count()`; q3: leer del
mismo polling ya existente, sin endpoint nuevo; q4: solo alerta visual, sin acción automática).
Una vuelta anterior (`wt-1`, 19:49) había agotado sus turnos sin dejar commits
(`timeout_escalado`, causa `max_turns`), así que `circuito:cabida 9990895` devolvió **NO CABE**
(`ya_timeouteo_antes`) — tocaba descomponer, no picar código directo.

Investigado el código real antes de descomponer: `SupervisorService::estado()`
(`app/Modules/Addons/Roadmap/Services/SupervisorService.php:39`) es donde vive el "escritorio del
supervisor" (recién resueltos / listos para terminal, #475) y ya llega al front vía el mismo poll
de ~3s que consume `TorreTerminales.vue:502-504`; `persistirOciosidad()`
(`SchedulerCommand.php:370`) ya escribe una línea `wt-K ociosa: ...` por ciclo de cron (1/min) en
el canal `circuito_despacho` (`config/logging.php:91`, driver daily); `RoadmapItem::scopeDespachable()`
(`RoadmapItem.php:1100`) es el scope real del despachador, distinto a `autoEjecutable()` (usado
por `listosParaTerminalTotal()`) — deben quedar como 2 números separados a propósito.

Descompuesto en 2 fases, sin tocar código de negocio:
- **#9990977** (backend) — agregar `ociosas_minutos_hoy`, `terminales_ociosas_ahora`,
  `elegibles_reales` y `alerta_contradictoria` a `SupervisorService::estado()`, parseando el log
  diario de `circuito_despacho` (cacheado 30s) + el scope real del despachador. Spec con
  archivo/línea exactos para no repetir la investigación.
- **#9990978** (frontend, `--depende-de=1` de #9990977) — panel fijo arriba en
  `TorreTerminales.vue` que consume esas 4 claves, badge de alerta sin botones de acción, sin
  endpoint nuevo.

Dado el patrón recurrente ya documentado varias veces en este repositorio (items #738, #745,
#830, #816, #818, #848, #852, #905, #878, #906, #907, #924, #9990012, #917, #910, #936, #9990408,
#962, #9990554, #9990549, #9990624, #9990650, #9990807, #9990826, #9990836, #9990856, #9990892,
#9990886, #9990893 — descomponer sin luego intentar cerrar deja el paraguas `en_progreso`
colgado hasta que el reaper lo re-encola sin trabajo propio), esta vuelta ejecutó ella misma el
intento de cierre (`estado_aprobacion='completado'`) inmediatamente después de crear los 2
sub-items. El guard de paraguas del modelo (`RoadmapItem.php`, bloque "(2b) PARAGUAS") lo
reenrutó correctamente a `aprobado_irving` + `excluir_pool_automatico=true` ("le quedan 2
sub-item(s) abierto(s)"), liberando `worker_sid`/`claimed_at`. El item sale del pool/reaper hasta
que #9990977 y #9990978 cierren los dos.

**Sin cambio de código de aplicación.** El trabajo real de la Fase 5 (métricas de ociosidad +
elegibles reales + panel en la Torre) sigue en #9990977/#9990978, pendientes de que una terminal
los reclame.
