## 2026-09-12 — Item #9990998 — Cerrados #9990867 y #9990868 como duplicados de la cadena viva

**Item:** "Cerrar #9990867 y #9990868 como duplicados de la cadena viva (ejecuta el resto de q1
de #9990903)". Sub-item de seguimiento de `#9990903`.

### Contexto

Los últimos 2 items abiertos de la cadena duplicada `#9990854` (CIRC-02b = `#9990867`
`aprobado_irving`, CIRC-02c = `#9990868` `aprobado_revisor`) seguían sin cerrar. La cadena viva
`#9990856` ya cubre ese trabajo real (`#9990858`/`#9990859`, con `#9990899` ya mergeado).

### Qué se hizo

Se cerró cada item con su propio ciclo completo (rama → bitácora dedicada → `circuito:integrar`
→ merge), exactamente igual que el patrón ya verificado en `#9990884`/`#9990885`:

- **#9990867** (CIRC-02b): rama `circuito/item-9990867-...`, bitácora
  `docs/bitacora/2026-09-12-item-9990867.md`, merge `0e4c0cb4`. `estado_aprobacion=completado`,
  `sin_ui=true`, `cierre_sin_codigo_motivo` poblado.
- **#9990868** (CIRC-02c): rama `circuito/item-9990868-...`, bitácora
  `docs/bitacora/2026-09-12-item-9990868.md`, merge `10d0d66e`. `estado_aprobacion=completado`,
  `sin_ui=true`, `cierre_sin_codigo_motivo` poblado.

Ambos verificados en BD tras el merge: `estado_aprobacion=completado`, `status=done`,
`merge_commit` poblado.

### Hallazgo — el padre #9990854 NO cierra en cascada (correcto, fuera de alcance)

La premisa del propio item ("tras cerrar ambos, el padre cierra solo vía el hook de cascada")
resultó incompleta: `#9990854` tiene **2 sub-items más**, ajenos a la duplicación CIRC-02 y no
mencionados en el spec de este item — `#9990870` (CIRC-04: re-triaje de la cola) y `#9990871`
(CIRC-05: API externa roadmap-externo) — ambos siguen `aprobado_irving`/`pending`. El hook de
cascada de `RoadmapItem.php` (`tieneSubItemsAbiertos()`) correctamente NO cierra el padre
mientras esos dos sigan abiertos. Esto es esperado y correcto: `#9990854` es un paraguas de
varias iniciativas (CIRC-02/04/05), no solo de la duplicación CIRC-02 que este item debía
resolver. No se tocó `#9990854`, `#9990870` ni `#9990871` — fuera de alcance de `#9990998`.

### Qué NO se hizo (a propósito)

No se tocó `#9990866` (ya `completado` con trabajo real propio) ni ningún item de la cadena
viva `#9990856`/`#9990858`/`#9990859` (el trabajo real de CIRC-02b/c sigue vivo ahí).

### Verificación

`tinker` confirma el estado final de los 3 items (`#9990867`, `#9990868` completados con sus
merge_commit; `#9990854` sigue `aprobado_irving` con 2 sub-items abiertos legítimos). Sin
cambio de código de negocio — cierre administrativo del roadmap.
