# Item #9990881 — CIRC-01: fix del escalado por timeout (duplicado de #9990855)

## 2026-09-11 18:42 — Cerrado como duplicado, sin cambio de código

`circuito:cabida 9990881` devuelve NO CABE (`historico_excede_umbral`). Antes de descomponer el
prompt de 4 fases en sub-items, reviso si el bug ya tiene trabajo en curso — el título es
sospechosamente idéntico al de otro item reciente.

**Hallazgo:** `#9990855` (creado 20 minutos antes, mismo título, mismo bug, mismo prompt de 4
fases) ya resolvió Fases 1-3 completas y está mergeado a `main` (commit `a4b200ac`, merge
`ff72b2a3`). Verificado línea por línea en `ParquearTimeoutCommand.php`: guarda de estado +
guarda de paraguas + medición de `commits_rama` sobre `items.branch`, exactamente como pedía el
prompt. Reverificado en BD los 4 items atorados del ejemplo (#9990801/#9990714 `completado`,
#9990776/#9990810 `aprobado_irving`+`excluir_pool_automatico` correctamente parqueados) — ninguno
sigue en `requiere_irving`. La única pieza que falta (Fase 4, candado de regresión) ya es el
sub-item `#9990880`, `en_progreso` en `wt-2` — no se toca.

Ambos items (`#9990855` y `#9990881`) nacieron del mismo documento origen (`#9990874`, "CIRC-00")
y el generador los duplicó — mismo patrón de carrera ya visto varias veces en este circuito
(#733/#741/#753/#9990003/#9990353).

**Acción:** cierro `#9990881` directo como duplicado resuelto, sin crear sub-items nuevos (habría
sido triplicar el trabajo de `#9990855`/`#9990880`). Detalle completo en
`docs/circuito-timeout-escalado-duplicado-item-9990881-verificacion.md`.

**Sin cambio de código de negocio** — el fix ya estaba en `main` antes de que este item se
reclamara.
