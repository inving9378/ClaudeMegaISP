## 2026-09-14 06:21 — Restyle Vendedores (pestaña principal) con look de la Torre

Primera pestaña del alcance iterativo (decisión de Irving: "una pestaña a la vez"). Restyle
visual puro de `resources/js/components/module/vendors/VendedorListar.vue` (`/vendedores`,
el listado literal "de vendedores" del enfoque del item), no de `SellerListar.vue`
(`/sellers/seller`, que delega la tabla al componente compartido `Datatable.vue` usado por
~60 CRUDs — alto blast radius, se dejó fuera y quedó registrado como sub-item #9991077).

Hallazgo del explorador: la Torre de Control (`resources/js/components/module/releases/`) NO
usa Quasar — es Bootstrap 5 + Bootstrap Icons + tokens CSS locales por componente (patrón
`--tc-*` + card `.tc-card` con borde/radio/sombra sutil + badges "pill"). El look se replicó
LOCALMENTE en `VendedorListar.vue` (tokens `--vnd-*`, clases `.vnd-card`/`.vnd-title`, header
icono+`h4 fw-bold` como `ReleasesIndex.vue`) sin tocar ningún archivo de `torre-control/` (evita
fragmentar/duplicar el theming compartido de forma peligrosa — decisión q2 recomendada) y sin
pelear el override global `!important` de `resources/sass/base/dark_mode/dark_mode.scss` que ya
gobierna el modo oscuro de `.badge-*`/`.q-card` en TODA la app: solo se tocó forma/tipografía
(radio pill, tamaño, peso), que ese override no toca — el color/fondo en oscuro sigue el
convenio ya establecido site-wide.

Verificado con `bash deploy/circuito/npm-build.sh` (compiló limpio tras corregir un `*/` que
cerraba de más un comentario CSS dentro del `<style scoped>`). Sin cambios de lógica, endpoints
ni estructura de datos.

Mergeado a `main` (commit `27bc996694173adf9e366349194cdce1e76a5cb6`). Registrados 3 sub-items
de seguimiento para el resto del alcance: #9991077 (listado `/sellers/seller`), #9991078 (ficha
individual del vendedor, 7 sub-pestañas de `Menu.vue`), #9991079 (Comisiones/Tipos/Estados,
pantallas de Configuración — a confirmar si entran en el alcance de "Vendedores").
