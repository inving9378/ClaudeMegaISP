## 2026-09-17 10:15 — #9991209 Releases: estado de publicación persistido + preflight sin tags ajenos

**Sesión directa con Irving** (Fase 1 de "cerrar el pipeline de releases y emitir V1.36").

- `15a04171` — el paso `github_release` escribe en `releases`: `github_release_id`, `published_at`
  (el real de la API, a zona local) y `estado_publicacion=publicada`; si la API falla,
  `no_publicada` + motivo (solo si no había estado). Try/catch: registrar nunca tumba el release.
- `a5b7dd78` — `releases:backfill-publicacion {--dry-run} {--solo=}`: por tag contra GitHub
  (reusa la lectura paginada de `releases:reconciliar`, ahora `githubReleasesPorTag()` público),
  solo filas con los 3 campos en NULL. Dry-run mostrado a Irving: las **31** filas tenían Release
  en GitHub → aplicado: 90/91/94 con `publicada` + id + `published_at` real (V1.35 =
  2026-09-17 09:14:48). Segunda corrida: "Nada que rellenar" (idempotente).
- (este commit) — `findPreviousTag()` filtra por la convención `V\d+.\d+-dd.mm.yyyy` (`v0.3.0`
  queda fuera sin borrarse; también corrige el rango del changelog) + chequeo informativo
  `trabajo_sin_publicar`.

**Validación:** `release:preflight V1.36-17.09.2026` → `version_previa_publicada` OK "V1.35 está
PUBLICADA"; `trabajo_sin_publicar` OK "9 commits desde V1.35: entran en V1.36". Sobre V1.35 (ya
tag): WARN "hay trabajo en dev sin publicar: 9 commits". `coverage("V1.36").desde_tag` =
`V1.35-17.09.2026` (antes habría sido `v0.3.0`).
