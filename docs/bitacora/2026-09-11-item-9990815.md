# Item #9990815 — Fase 4: medición p50/p95, criterios de aceptación y encendido del flag en dev

## 2026-09-11 21:43 — wt-3 — NO CABE, integración de lo ya hecho + descomposición

Al reclamar el item, `circuito:cabida` devolvió NO CABE (`ya_timeouteo_antes`): dos vueltas
previas ya habían agotado `max_turns` (2 reanudaciones, tope=2) dejando avance real en la rama
`circuito/item-9990815-fase-4-medicion-p50p95-criterios-de`:

- `22a298e3` — instrumenta duración end-to-end de búsquedas del listado de Clientes: canal de
  log `clientes_busqueda` (`config/logging.php`) + `ClientDatatableHelper::registrarDuracionBusqueda()`
  (longitud del término, NUNCA el término; duracion_ms; v2_habilitado; ampliada).
- `825133a5` — fix real: `olt_onus.client_id` (agregada en `2026_04_17_032635`) nunca tuvo
  índice. El buscador v2 correlaciona `whereExists(olt_onus.client_id = clients.id)` para
  búsquedas por SN → sin índice, MySQL hacía table scan completo (`DEPENDENT SUBQUERY type=ALL`)
  y el p95 de búsqueda por SN medía ~20-27s, muy por encima del criterio 10 del padre #9990803
  (diferencia de p95 SN vs. nombre ≤100ms). Migración aditiva con `down()` que revierte.

Siguiendo el mismo patrón documentado ya varias veces en `CLAUDE.md` (familia de items
#738/#745/#830/.../#9990807): en vez de dejar esos 2 commits huérfanos en una rama sin mergear,
se integraron a main primero (`circuito:integrar 9990815`, por ser reusable y de bajo riesgo,
mismo criterio que #9990807), y DESPUÉS se descompuso el trabajo restante en 2 sub-items:

- **#9990839** — Fase 4a: correr 10 búsquedas por nombre + 10 por SN con el flag encendido,
  calcular p50/p95 real desde el log, verificar el criterio 10 ahora que el índice ya está en
  main, y repasar los 10 criterios de aceptación del padre #9990803 con resultado real.
- **#9990840** — Fase 4b (depende de 4a): encender `CLIENTES_BUSQUEDA_V2` en dev de forma
  persistente si los 10 criterios pasan, `config:auditar-env` antes de `config:cache`, y cierre
  (cascada automática del padre #9990803 si #9990811/#9990812 también están mergeadas).

Se ejecutó el intento de cierre del propio #9990815 (`estado_aprobacion=completado` vía tinker)
para forzar SÍNCRONAMENTE el guard de paraguas (`RoadmapItem.php` bloque "(2b) PARAGUAS") en vez
de dejarlo pendiente del merge asíncrono: quedó parqueado correctamente en `aprobado_irving` +
`excluir_pool_automatico=true` + `worker_sid` liberado — fuera del pool/reaper hasta que #9990839
y #9990840 cierren.

**Sin cambio de código propio de esta vuelta** — el código real (instrumentación + índice) ya
estaba commiteado por vueltas anteriores; esta vuelta solo lo integró a main y descompuso el
resto del trabajo pendiente.
