## 2026-09-11 18:40 — Item #9990865 (CIRC-01): cerrado como duplicado de #9990855, ya resuelto

`#9990865` pedía el mismo fix (3 guardas en `ParquearTimeoutCommand.php`) y el mismo saneamiento
de 4 items atorados que `#9990855` — creados casi al mismo tiempo por dos vías distintas. La
colisión ya había sido detectada por el propio circuito (log: `colision_pausada` con
`ganador=9990855` sobre `ParquearTimeoutCommand.php`, 18:31→18:35).

Verificado en `main` (tip `6c16f40f`): `#9990855` ya completó las 4 fases —

- Fix (guarda de estado + guarda de paraguas + medición de avance) mergeado vía `a4b200ac`
  (merge `ff72b2a3`). Leído el archivo real: las 3 guardas están tal cual pedía el spec.
- Saneamiento verificado en BD: `#9990801`/`#9990714` → `completado`; `#9990776`/`#9990810` →
  `aprobado_irving`+`excluir_pool_automatico=true` (paraguas propios, cierre por cascada).
  Ninguno en `requiere_irving`.
- Fase 4 (candado de regresión, 3 pruebas) descompuesta en `#9990880`, `pendiente_revision`,
  sin reclamar — no se duplicó.

La rama de `#9990865` traía un commit previo (`729512d5`) con una segunda implementación
independiente de las mismas guardas, escrita en paralelo antes de que la colisión se resolviera.
Se descartó (`git reset --hard main` sobre la rama del item) por ser redundante y conflictuar
línea a línea con lo ya mergeado; el SHA queda accesible si hiciera falta revisarlo.

Documentado en `docs/circuito-fix-timeout-paraguas-item-9990865-verificacion.md`, commiteado en
la rama `circuito/item-9990865-circ-01-fix-del-escalado-por-timeout-q` e integrado.

Sin cambio de código de negocio nuevo — el trabajo real ya está en `main` vía `#9990855`, y lo
único pendiente (candado de regresión) sigue en `#9990880`.
