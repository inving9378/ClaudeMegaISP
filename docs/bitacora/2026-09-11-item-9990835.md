## 2026-09-11 16:57 — Fase 2 (#9990833): esquema + comando clientes:normalizar-series (item #9990835)

Item ya venía con casi todo el trabajo hecho por vueltas anteriores (3 timeouts previos, cada
uno dejando commits reales en la rama `circuito/item-9990835-fase-2-9990833-esquema-comando-cli`):

1. Migración aditiva `2026_09_11_220000_add_serie_equipo_columns_to_client_additional_information.php`
   — agrega `serie_equipo`/`serie_equipo_norm` (con índice)/`serie_equipo_origen` a
   `client_additional_information`, con `down()` completo.
2. Comando `php artisan clientes:normalizar-series` (`--dry-run`, `--csv=`, `--chunk=`):
   jerarquía OLT (`olt_onus.sn`) → `modem_sn` como respaldo (D4), nunca inventa CPE inalámbrico,
   **jamás escribe `modem_sn`** (D3), valores que no normalizan a 16 hex van al CSV de
   excepciones (D5) sin transformación forzada.
3. Fix de idempotencia real (commit `703a33ab` de una vuelta previa): la existencia de fila en
   `client_additional_information` se decide por el `LEFT JOIN`, no por las filas afectadas del
   `UPDATE` (MySQL reporta 0 afectadas en la 2a corrida cuando el valor ya es idéntico).

Lo que faltaba (y por lo que el item seguía abierto, escalado por DES-TRABE/Opus con categoría
"ejecutor no pudo"): el criterio de aceptación #3 del propio item — "correr el comando 2 veces y
verificar (a) mismo resultado, (b) modem_sn sin una sola fila modificada, (c) CSV de excepciones
coherente" — nunca quedó commiteado como test (una nota de decisión de una vuelta anterior
mencionaba haberlo escrito, pero el archivo nunca llegó a `git add`).

**Esta vuelta:** escribí `tests/Feature/Clientes/NormalizarSeriesClientesCommandTest.php` con 5
escenarios (OLT gana sobre modem_sn con valor distinto a propósito, fallback a modem_sn con
formato sucio para poder comparar byte a byte, valor que no normaliza a 16 hex, cliente sin fila
`client_additional_information`, cliente sin ningún dato) y corrí el comando 2 veces, verificando
los 3 criterios de aceptación.

**Obstáculo de infraestructura (ajeno al item, documentado, no corregido):** `Tests\TestCase`
(que usan la mayoría de los tests del repo) corre `migrate:fresh --seed` en cada test vía
`RefreshDatabase`; hoy eso truena por un bug preexistente en la migración de Talento
`2026_09_02_150000_seed_talento_document_templates_personal.php` (columna `tipo_cambio`
inexistente) — nada que ver con Clientes. Encima, el guard `GuardBaseDePruebas` exige que la BD
termine en `_test`, y `phpunit.xml` declara `DB_DATABASE=megaisp_test`, pero el *shell* de este
worktree ya trae `DB_DATABASE=megaisp`/`APP_ENV=local` exportados como variables de entorno del
proceso — y PHPUnit 10 no sobreescribe variables de entorno ya presentes en el SO salvo
`force="true"`. Sorteado sin tocar ningún archivo compartido: (1) el test usa
`DatabaseTransactions`+`CreatesApplication` (mismo patrón que
`tests/Feature/Roadmap/SubItemCommandDependeDeTest.php`) en vez de `Tests\TestCase`, así no
dispara el `migrate:fresh` roto; (2) apliqué la migración nueva de este item, sola y dirigida
(`--path=...`), contra `megaisp_test` (que ya tenía el resto del esquema de una corrida previa);
(3) corrí `vendor/bin/phpunit` con `DB_DATABASE=megaisp_test APP_ENV=testing` explícitos en el
propio comando para neutralizar las variables ya exportadas en el shell. Ninguna de las tres cosas
tocó `.env`, `phpunit.xml` ni la BD de dev/prod.

**Verificado:** `php -l` limpio en los 3 archivos tocados; `php artisan --version` bootea;
`php artisan list clientes` muestra el comando registrado; test verde
(`DB_DATABASE=megaisp_test APP_ENV=testing vendor/bin/phpunit tests/Feature/Clientes/...` → OK,
2 tests, 29 assertions) contra `megaisp_test`, nunca contra `megaisp` (dev). Sin tocar producción
en ningún momento (el item lo prohíbe explícitamente para la Fase 5, que ni siquiera es parte de
este item).

**Commits en la rama** (`circuito/item-9990835-fase-2-9990833-esquema-comando-cli`):
- `4a9e4592` (previo) — esquema `serie_equipo*` en `client_additional_information`.
- `c16eaeb0` (previo) — comando `clientes:normalizar-series`.
- `703a33ab` (previo) — fix de idempotencia real.
- `a622827c` (esta vuelta) — test de aceptación (idempotencia + modem_sn intacto + CSV coherente).

Integrado a `main` vía `circuito:integrar 9990835`. Item marcado `completado` con
`reporte_coloquial`/`enlace_revision` poblados (es un comando de terminal, sin pantalla propia).
