# Item #9991089 — Fase 1: columna roadmap_items.descomposicion + backfill idempotente de los 492

## 2026-09-14 11:15 — Implementación y cierre (wt-1)

Sub-item de seguimiento de #9991084 (a su vez originado en #9991082, el bug `subs.filter is not
a function` de la Hoja de Ruta). Fase 1 de la deuda estructural: separar de raíz la metadata de
descomposición (`subtasks.descomposicion.depende_de`, escrita por `circuito:sub-item`/
`DependenciaGate`) de la lista real de sub-tareas de la UI, que hoy comparten la misma columna
JSON `subtasks`.

**Implementado:**

- Migración aditiva `database/migrations/2026_09_14_120000_add_descomposicion_to_roadmap_items.php`:
  - `up()`: guard `Schema::hasColumn` (idempotente si se re-ejecuta) → snapshot de seguridad de
    **solo** `roadmap_items` vía `MysqldumpEngine::dumpToFile()` (mismo motor compartido que
    `backup_db:process`, credenciales desde `config()`, nunca hardcodeadas/logueadas) a
    `storage/app/circuito/backup-roadmap_items-pre-descomposicion-<fecha>.sql` → añade columna
    `descomposicion` JSON NULLABLE `after('subtasks')` → backfill idempotente.
  - Backfill: recorre por `chunkById(200)` las filas con `subtasks` no nulo; SOLO toca las que
    son objeto (no lista) con llave `descomposicion` array — mismo criterio de detección que
    `RoadmapController::separarSubtasks()` (`Controllers/RoadmapController.php:3657-3669`,
    reusado por referencia, no reinventado). Mueve `subtasks.descomposicion` a la columna nueva y
    deja `subtasks='[]'`.
  - `down()`: reconstruye `{"descomposicion": {...}}` en `subtasks` para las filas con la columna
    poblada, antes de dropearla.
  - Nombre de columna `'descomposicion'` documentado como el mismo valor de
    `DependenciaGate::META_KEY` (sin importar la clase de dominio dentro de la migración, para no
    acoplarla — se referencia por comentario).

**Verificado en dev (BD compartida `megaisp`):**
- Antes: `SELECT COUNT(*) FROM roadmap_items WHERE JSON_TYPE(subtasks)='OBJECT'` = 496.
- `php artisan migrate --path=database/migrations/2026_09_14_120000_add_descomposicion_to_roadmap_items.php` → `DONE` (3.1s).
- Después: `JSON_TYPE(subtasks)='OBJECT'` → **0**; `descomposicion IS NOT NULL` → **496** (coincide exacto).
- Snapshot generado: `storage/app/circuito/backup-roadmap_items-pre-descomposicion-20260914_111841.sql` (17 MB).
- Caso real propio (#9991089 mismo): `subtasks='[]'`, `descomposicion='{"depende_de": []}'` — correcto.
- `down()` probado en una transacción aislada (`DB::beginTransaction()` + `rollBack()` sobre 1
  fila real) para confirmar que la reconstrucción produce exactamente el formato viejo, sin dejar
  rastro en la BD real.
- `php -l` limpio, `php artisan --version` bootea, `migrate:status` muestra la migración `Ran`.

**Fuera de alcance (a propósito, es Fase 2 — sub-item hermano, `--depende-de=1`):** NO se
cablearon lectores (`RoadmapController::normalizarSubtasks()`, `DependenciaGate`) a la columna
nueva. Mientras eso no pase, `normalizarSubtasks()` sigue derivando `descomposicion` en memoria
desde `subtasks` (que ya quedó `'[]'` para las 496 filas backfillleadas) — el atributo en memoria
para esas filas en respuestas de API saldrá `null` hasta que la Fase 2 lo repunte a leer la
columna real; no afecta la BD (nunca hace `save()`) ni al `DependenciaGate` (que hoy no está
cableado al scheduler vivo, según su propio docblock).

**Sin cambio de código fuera de la migración.** Commit: `feat(roadmap#9991089): agrega columna
descomposicion + backfill idempotente de los 492` en la rama
`circuito/item-9991089-fase-1-columna-roadmap-itemsdescomposi`, encolado con
`circuito:integrar`.
