## 2026-09-12 01:15 — Fase 2 (C1) exención de docs del detector de colisiones — bucle reap sobre paraguas ya descompuesto

Item #9990892 llegó ya con una descomposición correcta hecha por una vuelta previa (`wt-6`,
18:46) que murió por timeout sin cerrar: corrió `circuito:cabida` (NO CABE, `histórico`),
confirmó que el canal `circuito_despacho` recién mergeado por `#9990863` Fase 1 aún no tenía
ningún archivo de log (precondición de `>=15 min` reales sin cumplirse) y descompuso el trabajo
en 3 sub-items correctos: `#9990896` (Fase 2a — confirmar evidencia real), `#9990897` (Fase 2b —
implementar la exención de docs en `footprintDeRama()`/`footprintEnVivo()`) y `#9990898` (Fase
2c — simulación histórica antes de activar). El reaper lo devolvió a `aprobado_revisor`
(`huerfano_reencolado`, reap_count=1) sin que nadie ejecutara el intento de cierre.

Esta vuelta reverificó todo (los 3 sub-items intactos, sin reclamar — `#9990896` ya está a su
vez parqueado por otra vuelta como paraguas de su propio hijo `#9990916`, `#9990897` en
`aprobado_revisor` listo para tomarse, `#9990898` en `aprobado_irving`) y ejecutó el intento de
cierre que faltaba: `estado_aprobacion='completado'` → el guard de paraguas del modelo lo
reenrutó a `aprobado_irving` + `excluir_pool_automatico=true` ("le quedan 2 sub-item(s)
abierto(s)"), liberando `worker_sid`/`claimed_at`. El item sale del pool/reaper hasta que
`#9990897` y `#9990898` cierren (y, en cascada, `#9990896`→`#9990916`).

Detalle completo en `docs/roadmap-bucle-reap-item-9990892-verificacion.md`.

**Sin cambio de código de aplicación.** El trabajo real (exención de docs en el detector de
colisiones + simulación histórica antes de activarla) sigue en `#9990897`/`#9990898`, y la
precondición de evidencia real sigue en `#9990896`→`#9990916`.
