## 2026-09-11 20:11 — Item #9990938: se completa el cierre-intento faltante (paraguas ya descompuesto)

`#9990938` (sub-item de `#9990923`, Fase 3a-i: filtrar "Listos para terminal" con el criterio
MR-36 de `depende_de`) llegó a `wt-4` ya descompuesto por una vuelta previa (`wt-5`,
2026-09-11 19:25) en dos fases secuenciales: `#9990943` (Fase A, método público
`filtrarConDependenciasCerradas()` en `RoadmapCircuitoService`) y `#9990944` (Fase B, wire en
`SupervisorService::listosParaTerminal()`/`::listosParaTerminalTotal()`, `depende_de=[9990943]`).

Verificado: `#9990943` ya está `completado`. `#9990944` está `en_progreso`, reclamado por `wt-2`
— con dueño, no se toca (regla "un item = un dueño"). Entre la descomposición original y esta
vuelta, el padre pasó por una racha larga de eventos `limite_cuenta_detectado` (~90 reintentos) y
tres `soltar-claim` por muerte de proceso, sin que ninguno intentara cerrar el padre.

Corrección: se ejecutó el intento de cierre faltante (`estado_aprobacion='completado'`). El guard
de paraguas del modelo lo reenrutó a `aprobado_irving` + `excluir_pool_automatico=true`,
liberando el claim y sacándolo del pool/reaper hasta que `#9990944` cierre (su única dependencia,
`#9990943`, ya está mergeada).

**Sin cambio de código de negocio.** Detalle completo en
`docs/roadmap-bucle-reap-item-9990938-verificacion.md`.
