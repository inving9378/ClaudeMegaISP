## 2026-09-11 19:10 — CIRC-04 PASO 2: auditoría de "basura" en la cola viva — resultado: 0 candidatos

**Item:** #9990907 (sub-item de seguimiento de #9990887, "CIRC-04: Re-triaje de la cola").
**Alcance aprobado por Irving** (respuestas a las 4 preguntas del brief, todas la opción
recomendada — verificado por hash contra `preguntas[].opciones`):

- **q1 — criterio de "basura":** Opción 1 (estricto) — item sin descripción/plan **Y** sin
  archivos/rutas/comandos referenciados en título ni cuerpo.
- **q2 — alcance de "cola viva":** Opción 1 — solo items en estado **pendiente/en bandeja**
  (no ejecutados, no descartados) = `estado_aprobacion IN (pendiente_revision, aprobado_revisor,
  aprobado_irving)`. Quedan fuera `en_progreso` (se está trabajando, no es "bandeja") y
  `requiere_irving` (ya escalado, es una categoría aparte según la propia Opción 2 del brief).
- **q3 — formato del reporte:** Opción 1 — tabla markdown en la bitácora del circuito (este
  archivo).
- **q4 — qué hacer con lo detectado:** Opción 1 — solo reportar; Irving decide caso por caso.
  Sin borrado, sin cambio de estado. Este item no toca ningún otro registro más que anexar una
  nota informativa en el padre `#9990887`.

### Metodología

Sobre el corte de la cola viva al momento de correr (`estado_aprobacion IN (pendiente_revision,
aprobado_revisor, aprobado_irving)`), heurístico estricto (q1, Opción 1):

```
description vacía  AND  (prompt vacío O prompt < 15 caracteres)  AND
sin match de archivo/ruta/comando en título+description+prompt
  (patrones: rutas app|resources|routes|config|database, "php artisan",
   extensiones .php/.vue/.js/.blade.php/.json/.md/.sql, o Nombres tipo XController/XService/XModel)
```

### Resultado

| Total en alcance (q2) | Candidatos por heurístico estricto (q1) |
|---:|---:|
| 249 | **0** |

**Ningún item de la cola viva cumple el criterio estricto de "basura" aprobado.** El único
subconjunto que se acerca — items con **`description` vacía** — tiene 9 filas, y **las 9 tienen
contenido real y ejecutable en el campo `prompt`** (de 87 a 51,063 caracteres), que es donde vive
el plan real cuando el item se generó por flujo automático en vez de llenar `description`. Esto
confirma, con el universo completo (no solo la muestra citada en el item padre), la advertencia
que ya traía el propio `#9990907`: un heurístico que solo mira `description` da falsos positivos.

| id | módulo | título | por qué NO es basura |
|---:|---|---|---|
| 185 | Roadmap / Circuito CC | Más 8tems | `prompt` (87c): instrucción real y accionable ("busca trabajo atorado y crea ítems") — ya confirmado en la investigación previa citada por el item padre |
| 190 | Auditoria | Más items | `prompt` (4,860c): spec completo de "Barrido de trabajo atorado" |
| 191 | Talento | Expediente digital del personal: plantillas por puesto... | `prompt` (5,864c): metadatos + decisiones ya tomadas + plan de 5 hijos (A–E) |
| 842 | ModuleManager | Item 2 — Catálogo completo de permisos por módulo (nivel B) | `prompt` (1,514c): objetivo + pasos concretos, depende del Item 1 |
| 9990721 | Auditoria | permisos | `prompt` (7,913c): gate de confirmación + spec de auditoría de solo lectura — ya confirmado en la investigación previa citada por el item padre |
| 9990803 | Auditoria | Clientes: el buscador del listado debe buscar únicamente... | `prompt` (10,656c): módulo/nivel/prioridad + reporte coloquial completo |
| 9990810 | Core / Permisos | Cerrar VoIP: de dev hasta que suene el teléfono en producción | `prompt` (7,272c): continuación explícita de #9990718, con ruta de rama y contexto |
| 9990854 | Auditoria | corrección del Circuito CC — para desatorar el flujo | `prompt` (35,199c): documento de contexto/plan fechado y extenso |
| 9990874 | Auditoria | Items de corrección del Circuito CC — para desatorar el flujo | `prompt` (51,063c): documento de contexto/plan fechado y extenso |

**Distribución por módulo del universo auditado (249 items, para trazabilidad):** Roadmap /
Circuito CC 54 · Auditoria 44 · Mapa de Red 31 · Talento 25 · Ventas / Talento 14 · Deploy /
Releases 11 · Finanzas 9 · MegaFamilia 8 · Infra 6 · Flotas 5 · VoIP 5 · GestionRed 4 ·
Permisos/Roles 4 · Payments 4 · Mapas 3 · Core/Permisos 3 · PortalCliente 3 ·
VoIP/Infraestructura 3 · Auth 2 · Roadmap/Circuito 2 · Embajadores 1 · Vendedores 1 ·
Facturación 1 · VozMayorista 1 · Deploy/Releases 1 · Usuarios 1 · ModuleManager 1 ·
DocumentacionCorporativa 1 · Clientes 1.

### Conclusión y siguiente paso (q4)

No hay lista de "basura" que entregarle a Irving para decidir caso por caso — el hallazgo en sí
**es** la respuesta: bajo el criterio que él mismo aprobó, la cola viva de hoy no tiene items sin
contenido ejecutable. No se tocó ningún `estado_aprobacion` ni se borró nada (cumple q4 al pie de
la letra: nada que decidir porque no hay candidatos). Nota equivalente anexada a
`comentarios_claude` de `#9990887` (el padre) para que quien revise la Fase de re-triaje no
repita esta auditoría.

**Nota operativa:** esto es un corte puntual, no una garantía permanente — la cola crece varias
veces por hora vía `circuito:sub-item`/generadores automáticos; si se quiere una auditoría
recurrente, sería un item aparte (fuera de alcance de este PASO 2, que era un barrido puntual).
