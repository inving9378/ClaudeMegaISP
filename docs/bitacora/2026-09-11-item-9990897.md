## 2026-09-11 19:45 — Fase 2b (exención de docs en footprintDeRama/footprintEnVivo) cerrada SIN CÓDIGO

Item #9990897, sub-item de #9990892 (Fase 2 de la exención de docs del detector de colisiones,
#9990863 Fase 1). Su propia spec condicionaba el trabajo a lo que concluyera la Fase 2a
(#9990896): si la evidencia real mostraba que la colisión de footprint SÍ es causa frecuente de
terminales ociosas, implementar el filtro (`esRutaExentaDeColision()` en
`RoadmapCircuitoService.php`, ~líneas 2929/2966); si concluía que NO, cerrar sin código — decisión
ya tomada por Irving en `q3` del item padre #9990892.

**Verificado el veredicto de la cadena** #9990896 → #9990916 → #9990936 → **#9990942** (doc
`docs/circuito-colision-ociosidad-item-9990942-verificacion.md`): con 56 minutos reales de
operación tras el merge de #9990863, y con una colisión real confirmada dentro de esa ventana
(#9990864 pausado por #9990890), `storage/logs/circuito-despacho-*.log` no registró ni un solo
ciclo con slot ocioso. Con backlog alto (~1580 items pendientes) el scheduler llena el slot
liberado por una colisión con otro candidato en el mismo ciclo — la colisión retrasa un item
puntual, pero no deja ninguna terminal ociosa. Veredicto: no-accionable hoy, re-agendable solo si
el backlog algún día baja lo suficiente para que exista ociosidad real que investigar.

**Cierre:** `estado_aprobacion=completado`, sin rama ni commits de código (nada que implementar
sin evidencia de beneficio). Poblados `cierre_sin_codigo_motivo`, `sin_ui`+`sin_ui_motivo` y
`reporte_coloquial` citando el doc de veredicto. Sin cambio de código de aplicación — el punto de
inserción ya localizado (`footprintDeRama()`/`footprintEnVivo()`) queda documentado en el propio
item por si el backlog baja algún día y vale la pena retomarlo.
