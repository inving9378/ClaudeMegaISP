## 2026-09-11 18:17 — CIRC-02 (paraguas) — se completa el intento de cierre faltante

Item #9990856 es un PARAGUAS explícito (título y `prompt` lo dicen: "no lo trabajes directo",
agrupa CIRC-02a/02b/02c, cierra por cascada). Mismo patrón documentado repetidas veces en
CLAUDE.md (familia #738/#745/#830/#816/.../#9990836): el revisor lo escaló a `requiere_irving`
por ser decisión de arquitectura del propio circuito, Irving aprobó la opción recomendada del
único hueco (q1: "marcarlo como PARAGUAS no-ejecutable, dejar que cierre por cascada"), y el
autopilot lo dejó `aprobado_revisor` — pero nadie había ejecutado el intento de cierre que
dispara el guard de paraguas.

Verificado: los 3 sub-items ya existen (`origen_item_id=9990856`), creados por una sesión previa:
- **#9990857** CIRC-02a (diagnóstico solo-lectura) — `en_progreso`, reclamado por `wt-3` (no se
  toca, aislamiento #334).
- **#9990858** CIRC-02b (mecanismo: `roadmap_item_respuestas` + re-encolado) — `requiere_irving`,
  sin reclamar.
- **#9990859** CIRC-02c (visibilidad en Torre + watchdog) — `pendiente_revision`, sin reclamar.

La descomposición ya era correcta y completa; solo faltaba el paso de bookkeeping. Se ejecutó
`estado_aprobacion = 'completado'` sobre #9990856 vía tinker: el guard `RoadmapItem.php` bloque
"(2b) PARAGUAS" lo detectó (3 sub-items abiertos) y lo reenrutó automáticamente a
`aprobado_irving` + `excluir_pool_automatico=true`, liberando `worker_sid`/`claimed_at`. Log
resultante: evento `flags` con `excluir_pool_automatico: false→true`.

Queda fuera del pool/reaper hasta que el hook de cierre en cascada (`RoadmapItem.php:459-491`) lo
complete solo cuando #9990857, #9990858 y #9990859 cierren los tres.

**Sin cambio de código** — el trabajo técnico real del mecanismo de respuestas de Irving sigue en
los 3 sub-items CIRC-02a/02b/02c.
