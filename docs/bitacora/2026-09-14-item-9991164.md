## 2026-09-14 13:40 — #9991164 Adjuntos del roadmap · Fase 2 (UI en Panorama)

**Sesión directa con Irving.** Nivel B. Sub-item de #9991162. Puntos 6–11 del prompt.

### Commit
- `TorreAdjuntos.vue` (nuevo) montado en `TorreControl.vue` bajo la franja de KPIs, gateado por
  `roadmap.adjuntos.view` (helper `Permission` + `allViewHasPermission()`, igual que
  `ReleasesIndex`). Colapsado por defecto con conteo en la cabecera; al abrir: drag&drop + botón,
  descripción opcional, amarre en el mismo acto (buscador por id/título con sugerencias), barra
  de progreso (`onUploadProgress`), error que repite el motivo del servidor (extensión / tamaño /
  tipo real) y rechazos baratos en cliente antes de mandar 25 MB; lista con miniatura para
  imágenes (`?inline=1`) e ícono por tipo, nombre/tipo/tamaño/quién/fecha relativa, chips de
  items (✕ desamarra), acciones descargar/amarrar/borrar según `puede.*`; "ver borrados".
- Solo tokens `--tc-*` heredados (claro/oscuro): los 11 pares medidos dan ≥ 4.62:1; para el
  acento en claro se usa tinta `#0f766e` (5.47) porque `--tc-accent` como texto sobre blanco da
  3.6. Una columna y acciones en fila bajo 720 px; `prefers-reduced-motion`.

### Verificación por HTTPS con sesión real (usuarios temporales, borrados al final)
- Login real (`POST /login`, campo `email` = `login_user`); V1 html/png/pdf → 201 con MIME real;
  V2 `.php`→`.png` → 422 "tipo real text/x-php"; V3 30 MB → 422; V6 → `duplicado=true`,
  `veces_subido=2`, un registro; V4 html → `application/octet-stream` + `attachment` + `nosniff`
  (aun con `?inline=1`), png/pdf inline solo con `?inline=1`; V9 sesión Mostrador → 403 en
  listar/descargar/subir; amarrar/desamarrar/buscar/borrar-con-item-abierto correctos.
- Gotcha operativo: tras correr la migración de permisos desde el worktree, el checkout
  principal (`CACHE_DRIVER=file`, caché por checkout) devolvía 403 al super-admin hasta
  `php artisan permission:cache-reset` en `/var/www/megaisp`. Anotado en la respuesta del padre.
- Pendiente: V10 (claro/oscuro/teléfono) y V1 desde la UI → Irving (primer uso: subir la maqueta
  y amarrarla al #9990934).
