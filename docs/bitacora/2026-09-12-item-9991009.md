## 2026-09-12 03:15 — CIRC-04 PASO 2: reporte de items sin contenido ejecutable (basura)

Item #9991009, sub-item de seguimiento de #9990870. Tarea de solo lectura: barrer
`roadmap_items` buscando patrones similares al caso de referencia #185 ("Más 8tems",
título sin sentido) y producir un listado para que Irving decida uno por uno, sin
tocar ninguna fila.

**Método:** combiné título corto/sin sentido, `description` NULL/vacía, `prompt`
NULL/vacío, títulos duplicados exactos (agrupando por `title`) y títulos con
artefactos de copy/paste (fragmentos markdown pegados literalmente).

**Hallazgo de método:** en este roadmap, `description` NULL no es señal de basura por
sí sola — la mayoría de los items reales guardan su contenido en `prompt`, no en
`description`. Verifiqué que 0 items abiertos tienen ambos campos vacíos a la vez.

**Resultado:** 5 grupos, documentados completos en `docs/circuito-basura-cola-2026-09-12.md`:
- **Grupo A** — duplicados por creación accidental (lote 2026-08-24, mismo prompt con
  typos): #184/#185/#186 ("Más 8tems") y #187/#188/#189/#190 ("Más items"). Solo #185
  y #190 siguen abiertos; #185 es duplicado puro ya ejecutado en #184/#186 (candidato a
  cancelar), #190 tiene contenido real completo (no es basura, solo título genérico).
- **Grupo B** — #9990058/#9990059 ("TEST-9990012..."), ambos con description Y prompt
  NULL — artefactos de prueba del propio mecanismo padre/hijo nivel C, ya cerrados.
- **Grupo C** — #921, #9990833, #9990929: contenido real y completo, pero el título
  quedó con un fragmento de markdown pegado literalmente (`Título:`, `**Título:**`).
  Los tres ya están cerrados, defecto puramente cosmético.
- **Grupo D** — #9990721, título de una sola palabra ("permisos") pero con un spec de
  auditoría completo detrás. Abierto, no es basura, solo invisible en listados.
- **Grupo E** — #9990853 y #9990854, título EXACTO duplicado ("corrección del Circuito
  CC — para desatorar el flujo"), ambos documentos-semilla del propio circuito, ambos
  abiertos. #9990854 es una v2 que agrega CIRC-05. Nota: ya existen items CIRC-01..09
  en la cola en paralelo — queda a criterio de Irving si alguno de los dos sigue
  aportando algo nuevo.

**Entregable:** `docs/circuito-basura-cola-2026-09-12.md`, con tabla resumen de
prioridad de revisión al final. Ninguna fila de `roadmap_items` fue modificada ni
borrada — tal como exige el item.

**Verificación:** `php -l` no aplica (solo se creó un `.md`); no se tocó código PHP ni
Vue, así que no hay build que romper.
