## 2026-09-12 16:35 — Q1: checkout dedicado del scheduler en main + actualizar cron (item #9991026)

**Contexto:** item aprobado por Irving (Q1/Q2/Q3, hashes `aa5c0d7a1a04fc77`/`0c87ef96092a3dc8`/
`5dbf67aebcccaff4`): crear un checkout FIJO separado en `/var/www/megaisp-scheduler` (worktree
git, `--base=main`) y mover ahí las líneas del crontab que invocan `circuito:*`, para que el
scheduler del circuito nunca corra desde `/var/www/megaisp` mientras ese checkout compartido
tenga una rama de trabajo checada a mano (carrera independiente del fix de #9990640/#9990644,
que solo aisló al merge-runner, no al cron).

**Qué se hizo:**
- Se creó `deploy/circuito/cron-wrap-scheduler.sh` (mismo patrón que `cron-wrap.sh`): resync a
  `main` vía `git checkout --detach -f main` antes de cada tick, mismo candado
  `guard-bd-pruebas.sh` y mismo registro de errores a
  `storage/logs`→`/home/meganet/circuito/logs/cron-wrap-scheduler-errores.log`.
- Commit `5c571bbe` en la rama `circuito/item-9991026-q1-checkout-dedicado-del-scheduler-en-m`,
  integrado a `main` vía `circuito:integrar` (encolado al merge-runner on-box).

**Qué quedó bloqueado (no se pudo hacer en automático):**
- `php artisan circuito:provision-worktree --path=/var/www/megaisp-scheduler --base=main` fue
  **rechazado por el clasificador de auto-mode del harness** (razón: "Modify Shared Resources") —
  cualquier acción que cree/edite algo directamente bajo `/var/www/` fuera del worktree del
  ejecutor se bloquea a este nivel de permisos.
- Se consultó a Thomas (`circuito:consultar`), que instruyó: documentar el bloqueo, NO intentar
  un bypass, y dejar el resto para ejecución manual de Irving.
- No se pudo abrir un sub-item de seguimiento para las 4 acciones manuales restantes: el item ya
  está a profundidad máxima (3) desde su raíz (`#9991024` → `#9991026`). Quedan documentadas aquí
  y en `comentarios_claude` del propio item.

**Pendiente — ejecución manual de Irving (o de una sesión con otro nivel de permisos):**
1. Desde `/var/www/megaisp`: `php artisan circuito:provision-worktree --path=/var/www/megaisp-scheduler --base=main`
   (idempotente; copia `vendor`, symlinkea `.env`/`node_modules` del principal — cumple Q2).
2. Verificación en seco (Q3): desde `/var/www/megaisp-scheduler`, `php artisan list | grep circuito:`
   y probar en modo lectura `circuito:scheduler`/`circuito:disparo-check` antes de tocar el cron real.
3. `crontab -e` (usuario `meganet`): cambiar el target de las líneas `circuito:*` de
   `.../deploy/circuito/cron-wrap.sh` a `.../deploy/circuito/cron-wrap-scheduler.sh` (mismos
   argumentos/horario). Decidir aparte si `backup_db:process` y `circuito:compuertas-sonda`
   también migran (hoy documentadas como atadas a `/var/www/megaisp` a propósito en sus propios
   comentarios del crontab).
4. Cortar en una ventana corta; verificar `storage/logs/cron-wrap-scheduler-errores.log` y los
   logs normales del circuito para confirmar que no se perdieron ticks.
5. Documentar el checkout nuevo en `CLAUDE.md`.

**Estado del item:** parqueado — `estado_aprobacion=aprobado_irving`, `excluir_pool_automatico=true`,
`motivo_espera=frontera_produccion`. Fuera del pool automático hasta que Irving haga los pasos de
arriba; no se marcó `completado` porque el objetivo real (cutover del cron) no se ejecutó.
