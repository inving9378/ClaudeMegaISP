## 2026-09-14 12:55 — #9991163 Adjuntos del roadmap · Fase 1 (backend)

**Sesión directa con Irving.** Nivel B. Sub-item de #9991162 (origen: #9990934 construyó a
ciegas por una maqueta que nunca existió en el repo). Decisiones D1–D7 aplicadas tal cual.

### Commits
- `626f56b2` migración aditiva `roadmap_adjuntos` + pivote `roadmap_adjunto_item`; disco
  `roadmap_adjuntos` con ruta ABSOLUTA del checkout principal (`storage/app/roadmap/adjuntos`,
  fuera de `public/`, env `CIRCUITO_ADJUNTOS_DIR`); modelo `RoadmapAdjunto` (allowlist
  extensión→MIME real, `NUNCA_INLINE`, relación N:M) + `RoadmapItem::adjuntos()`.
- `6c241e21` `RoadmapAdjuntoService`: `subir()` valida ext (D3) → tamaño 25 MB (D4) → MIME real
  con finfo (D4) → sha256 → dedupe (D6) → escribe `<uuid>.<ext>` (D7) → registra; si falla antes
  no toca disco, si falla el registro después retira el archivo. `amarrar/desamarrar`, `borrar`
  (lógico, rechaza si hay item abierto), `purgarVencidos()`, `faltantesEnDisco()` (Fase 3).
- `7a257058` `RoadmapAdjuntoController` + rutas `/api/roadmap/adjuntos[...]` + `/items/buscar`;
  permisos `roadmap.adjuntos.view/upload/delete` (migración; super-administrator +
  DESARROLLADOR; auto-grant `.view` apagado). D5: HTML/SVG siempre `attachment` +
  `application/octet-stream` + `nosniff` (también con `?inline=1`); imagen/PDF inline solo con
  `?inline=1`.
- `0a98c28a` `roadmap:adjuntos-purgar` (Kernel 02:40 diario): a los 30 días del borrado lógico
  el archivo sale de disco, el registro queda con `disco_purgado_at`.

### Verificación (tinker + curl, datos de prueba luego limpiados)
- V1 html/png/pdf → 3 registros, MIME real `text/html`/`image/png`/`application/pdf`, 3 archivos.
- V2 `.php` renombrado a `.png` → rechazado: "dice .png pero el tipo real es text/x-php".
- V3 30 MB → rechazado: "Demasiado grande: 30 MB (límite 25 MB)". `.exe` → extensión no permitida.
- V6 mismo contenido dos veces → **un** registro (`veces_subido=2`), amarrado a 2 items, 1 archivo.
- Disco: 3 subidas válidas = +3 archivos; los rechazos y el duplicado no escriben.
- Punto 5: borrar amarrado a item abierto → rechazado con los ids; suelto → lógico, archivo
  30 días; purga → archivo fuera, registro conservado.
- V4 descarga: html → `application/octet-stream; attachment` (aun con `?inline=1`); png/pdf
  `?inline=1` → su MIME + inline; sin inline → attachment. `nosniff` siempre.
- V9 usuario Mostrador/Vendedor sin permiso → 403 en listar/descargar/subir.
- V5 URL directa (`/storage/app/roadmap/adjuntos/<uuid>.html` y variantes) → 404.
- Pendientes de la Fase 2/3: V1 desde la UI, V7, V8, V10.
