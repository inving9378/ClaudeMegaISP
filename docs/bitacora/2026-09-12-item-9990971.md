## 2026-09-12 03:20 — CIRC-09 Fase 4: paneles desplegables "Trabajando" y "Decidido sin ti"

Sub-item de #9990934 (CIRC-09 · Panorama en árbol), depende de la Fase 3 (#9990970, sibling
posición 3, ya `completado`+mergeada). Ejecuta la Fase 4 del prompt original: dos paneles de
lectura sobre el árbol Módulo → Épica → Item → Sub-item.

**Estado al reclamar el item:** ya existía una rama con 1 commit de una vuelta anterior
(`0d993f2c`, autoría de Irving + Claude): los 2 endpoints backend ya construidos —
`GET /api/roadmap/torre/panorama-jerarquia/{id}/trabajando` y
`GET /api/roadmap/torre/panorama-jerarquia/{id}/decidido-sin-ti` — pero sin ningún consumidor en
el frontend (el árbol de la Fase 3 no los llamaba todavía). El item había quedado colgado
`en_progreso` sin cerrarse (2 `soltar-claim` por muerte de proceso + 1 `timeout:reanudado` por
max-turns) y el pool lo repartió de nuevo.

**Trabajo de esta vuelta (solo frontend, el backend ya estaba listo):**
- Rebase de la rama sobre `main` (el `merge-base` se había quedado 8 commits atrás; sin
  conflictos, `main` había avanzado con la Fase 5 ya mergeada — #9990973).
- `TorrePanoramaArbol.vue`: 8 campos nuevos de estado por nodo (`panelTrabajandoAbierto/
  Cargando/Error/Data`, `panelDecididoAbierto/Cargando/Error/Data`) + 2 funciones
  `paToggleTrabajando`/`paToggleDecidido` (mismo patrón cacheado que `paToggleDecision` de la
  Fase 5: un segundo clic no repite el fetch) + `provide()` de ambas.
- `TorreArbolNodo.vue`: inject de las 2 funciones, computeds `esTrabajando`/`esDecididoSinTi`
  sobre el badge ya calculado por `paEstadoVisual` (Fase 3), `onClick` las dispara igual que ya
  hacía con "Tu decisión", y 2 bloques de template nuevos:
  - **Trabajando:** stepper de 6 fases (usa `fases[]` del endpoint tal cual — 3 con `alcanzada`
    real, 3 con `alcanzada: null` pintadas visualmente distinto y la nota `fase_actual_nota`
    debajo, honesta sobre por qué no hay dato), chip de reloj (`fmtClock`) + rama + terminal,
    y últimas líneas del log (`log_tail`).
  - **Decidido sin ti:** qué decidió / por qué (texto), chips de confianza / reversible (sí·no)
    / terminal / cuándo (`fmtHora`). Si `decidido:false` (sin entrada automática en el log),
    mensaje honesto en vez de un panel vacío.
- CSS nuevo (`.pa-panel`, `.pa-stepper`, `.pa-step*`, `.pa-log-*`), sin tocar las clases
  existentes de la Fase 5 (`.pa-decision*`) — mismo look & feel, bloque aparte.

**Verificación:** `php -l` limpio en el controller/rutas (sin cambio en esta vuelta, ya estaban
commiteados); `php artisan route:list` confirma las 2 rutas registradas; build completo con el
semáforo (`deploy/circuito/npm-build.sh`) compiló OK (`Compiled Successfully`, sin errores).
No hay smoke visual en navegador en esta vuelta (fuera del alcance de un ejecutor on-box sin
browser) — el criterio de aceptación es observable en `/releases` → Torre de Control → Panorama
en árbol → desplegar una fila "Trabajando" o "Decidido sin ti".

**Commit:** `d317f549` — integrado a `main` vía `circuito:integrar 9990971`.

Sin decisiones de criterio nuevas en esta vuelta (las 4 preguntas del item ya venían resueltas
por el autopilot desde el triaje inicial); el único llamado propio fue de implementación directa
(reusar el patrón exacto del panel de Fase 5 en vez de inventar uno nuevo).
