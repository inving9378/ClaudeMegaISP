## 2026-09-13 16:40 — Item #9990411: conflicto de merge resuelto — era trabajo duplicado, ya en main

Item: "FASE 1+2: detectar causa=limite_cuenta en vuelta.sh y no castigar el item" (sub-item de
#9990410). Llegó con `estado_aprobacion=en_progreso`, rama propia
(`circuito/item-9990411-fase-12-detectar-causalimite-cuenta-e`) y DOS escalaciones de merge por
conflicto (2026-09-06 en `vuelta.sh`, 2026-09-13 en `ParquearTimeoutCommand.php`). Irving forzó un
"desparque-destrabe" pidiendo que una terminal resolviera el conflicto.

Investigado (`git diff main <rama>` en ambos archivos): el conflicto no era una edición simultánea
de líneas vecinas — era trabajo **íntegramente duplicado**. Mientras la rama de #9990411 esperaba
merge, los items **#9990426** (vuelta.sh, commit `ad3a8d16`) y **#9990416**
(ParquearTimeoutCommand.php, commit `593e7e9f`) ya implementaron exactamente lo mismo y sí llegaron
a `main`. Peor: `main` siguió avanzando encima con guards posteriores (#9990855 "guarda de estado" +
"guarda de paraguas", #9990901 inyección de respuesta de Irving en el prompt) que la rama vieja de
#9990411 NO tiene — mergearla tal cual habría regresionado esas dos piezas.

Verificado línea por línea que `main` ya cumple el 100% del DoD del item: detecta `'session limit'`
en `vuelta.sh` con extracción best-effort de la hora de reset, y `ParquearTimeoutCommand.php` tiene
el tercer camino que regresa el item intacto a `estadoAprobadoPrevio()` sin tocar
`veces_timeouteo`/`reanudaciones_timeout`/`escalaciones_fingerprint`, con el evento
`limite_cuenta_detectado` en el log.

Decisión (registrada vía `circuito:reportar --tipo=decision`): NO mergear la rama vieja — cerrar el
item con `sin_merge_esperado=true` (el resultado ya existe en `main` por otra vía). Sin cambio de
código. Detalle completo en
`docs/circuito-limite-cuenta-duplicado-item-9990411-verificacion.md`.

Cerrado: `estado_aprobacion=completado`.
