## 2026-09-14 16:40 — Item #9991086: MergeRunner no sincronizaba el checkout principal (Fase 1+2 integradas, Fase 3 descompuesta)

**Contexto:** hallazgo del #9991082 (mismo día): `MergeRunner::syncCheckoutPrincipal()` comparaba
`git status --porcelain` contra un `HEAD` que `update-ref` ya había avanzado al commit nuevo del
merge, así que TODO archivo tocado por el merge se leía como "sucio" → 0 syncs reales desde
2026-09-09 (`fdcf6a7a`). Efecto medido: `/var/www/megaisp` (dev.meganett.com.mx + cron del
circuito) sirvió código de 3 días atrás sin que nadie lo notara.

**Estado al llegar a esta vuelta (wt-2):** el item ya llevaba 2 reanudaciones por timeout
(`max_turns`) con 2 commits en su rama (`circuito/item-9991086-mergerunner-el-checkout-principal-nunca`):
el fix real (comparar contra `$shaAnterior`, capturado ANTES del `update-ref`, con `git diff --quiet`
+ `git ls-files --others --exclude-standard` para trackeados+untracked) y su test de regresión
(`MergeRunnerSyncCheckoutPrincipalTest`, 2 casos: checkout limpio se sincroniza / checkout con
cambio a mano no se toca). Un comentario de la vuelta anterior ya había confirmado que la Fase 2
(rebuild) NO era un bug independiente: `necesitaRebuild = revision_ui && sincronizado`
(`MergeRunner.php:335`) — con `sincronizado` siempre `false` por el bug de la Fase 1, el rebuild
nunca se disparaba; con el fix, vuelve a dispararse solo.

**Esta vuelta:**
1. `circuito:cabida 9991086` → NO CABE (`ya_timeouteo_antes`, ya llevaba 2 reanudaciones).
2. Verifiqué el trabajo YA COMMITEADO en la rama (no piqué código nuevo): `php -l` limpio en
   `MergeRunner.php` y el test; `DB_DATABASE=megaisp_test APP_ENV=testing php artisan test
   tests/Feature/Roadmap/MergeRunnerSyncCheckoutPrincipalTest.php` → **2/2 PASS** (32 assertions).
   (Nota aparte, no de este item: el shell de este worktree exporta `DB_DATABASE=megaisp`/
   `APP_ENV=local` como variables de entorno reales, que pisan a `.env.testing`/`phpunit.xml` —
   hay que forzarlas explícito en el comando para correr tests aquí.)
3. `circuito:integrar 9991086` → encoló el merge de la Fase 1+2 (fix + test) a `main` (lo aplica
   el runner on-box, serializado).
4. Descompuse la Fase 3 (pendiente, sin empezar) en **#9991088**: exponer en `/releases` (Torre)
   el sha del checkout principal vs `main` + fecha del bundle, con spec detallado (archivos
   exactos a tocar: `EnvironmentHealthService.php` junto a `certificado()`,
   `RoadmapController::saludEntorno()` ya expone el JSON sin tocar ruta, `ReleasesIndex.vue`
   junto al aviso de `certAviso` ~línea 13-24 — mismo patrón que el banner de TLS del #891 §3).
5. Intenté cerrar el padre (`estado_aprobacion = 'completado'`) — el guard de paraguas
   (`RoadmapItem.php` bloque "(2b) PARAGUAS") lo interceptó porque #9991088 sigue abierto: lo
   reenrutó a `aprobado_irving` + `excluir_pool_automatico=true`, liberando `worker_sid`/
   `claimed_at`. Comportamiento esperado — cierra solo cuando #9991088 cierre.

**Resultado:** Fase 1 (el bug real del título) y Fase 2 (confirmado no-bug) quedan resueltas e
integradas. Fase 3 (observabilidad en la Torre) sigue en #9991088, pendiente de que el revisor
la tríe y una terminal la reclame. El item #9991086 queda parqueado como paraguas — no vuelve al
pool hasta que #9991088 cierre.
