## 2026-09-16 13:50 — Vendedores solicita artículos — FASE 3 notificaciones: bloqueada, diferida a sub-item (sin código)

**Item:** #9991192 (sub-item de #9991176, hermano de FASE 2 backend #9991190 y FASE 1 frontend
#9991191).

**Qué se pidió:** al crearse una solicitud de artículo del vendedor, notificar (in-app) a los
roles Mostrador+admin, enganchando `->notify()` dentro del método que crea el `InventoryMovement`
(ese método lo debía dejar listo la FASE 2 backend, #9991190), y opcionalmente notificar al
vendedor cuando se le entregue.

**Hallazgo al reclamar el item (mismo root que el hermano #9991191→#9991193):** el sub-item de
FASE 2 backend (#9991190) fue **cancelado por Irving** (`irving:david_marsal`, 2026-09-16 13:13,
sin comentario), tras que la válvula de contexto lo marcara nivel C (`veredicto: accion` —
modifica `config/route_permission.php` agregando rutas nuevas dentro de arrays de permisos
existentes). Verificado con grep directo sobre `app/` y `routes/` en esta vuelta: los endpoints
`POST /inventory/inventory_item_stock/request_item_by_vendor` y `.../cancel_own_request/{id}`, y
la columna `requested_by_user_id`, **no existen en main**. Sin el método real que crea el
movimiento, no hay dónde enganchar el `->notify()`.

**Por qué no se construyó igual el backend:** es una dependencia dura ya **decidida y rechazada
por Irving**, no una duda de implementación resoluble con la regla de oro — mismo criterio que ya
aplicó el hermano de FASE 1 (#9991191) sobre el mismo bloqueo, documentado en
`docs/bitacora/2026-09-16-item-9991191.md`. Reconstruir el backend por cuenta propia sería
deshacer la decisión de Irving, no resolver una ambigüedad.

**Decisiones estructuradas de Irving para esta FASE 3 (ya resueltas, preservadas para el
sub-item):** canal SOLO in-app (Laravel `Notification`, canal `database`, nada de correo/WhatsApp);
SÍ incluir la notificación opcional al vendedor al entregarse, en la misma fase, detrás de un
toggle por-vendedor (default ON); destinatarios Mostrador+admin identificados por **permiso
Spatie** (ej. `vendedores.solicitudes.recibir_notificacion`, vía `PermissionSyncService`), NO por
nombre de rol hardcodeado.

**Acción tomada:**
1. `circuito:reportar --tipo=decision` documentando el bloqueo (reporte #8321).
2. `circuito:sub-item` → creado **#9991195** ("FASE 3 (retomar): notificar Mostrador+admin
   (+opcional vendedor), bloqueado hasta que FASE 2 backend exista"), con las 3 decisiones
   estructuradas de Irving + el patrón de notificación (`TicketOpen.php`,
   `UnreadNotificationService`/`NotificationController`) + instrucción explícita de NO
   reconstruir el backend por cuenta propia, verificar primero si #9991190/#9991193 lo resolvió,
   y solo consultar a Thomas si el bloqueo sigue vigente.
3. Intento de cierre de #9991192 (`estado_aprobacion = completado`) → el guard de paraguas
   (`RoadmapItem.php` bloque "(2b) PARAGUAS") lo reenrutó correctamente a `aprobado_irving` +
   `excluir_pool_automatico=true`, liberando `worker_sid`/`claimed_at` — queda parqueado sin
   loopear en el pool/reaper hasta que #9991195 cierre.

**Sin cambio de código de aplicación** — el trabajo real de FASE 3 (clase de notificación +
dispatch) sigue en #9991195, bloqueado hasta que Irving decida sobre FASE 2 backend (#9991190),
igual que su hermano #9991193.
