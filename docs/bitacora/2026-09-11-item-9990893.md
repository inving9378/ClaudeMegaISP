## 2026-09-11 19:42 — Item #9990893 resuelto: bucle reap sobre paraguas ya descompuesto

`#9990893` (Fase 3 (C2) — "Listos para terminal" + contador de vueltas quemadas + cablear
#9990798) llegó reclamado `en_progreso` en `wt-6`, pero era en realidad un paraguas ya
descompuesto por una vuelta previa (`wt-3`, 2026-09-11 19:02): 3 sub-items acotados
(#9990923 Fase 3a SupervisorService, #9990924 Fase 3b heurística de texto, #9990925 Fase 3c
contador en la Torre), con `circuito:cabida` en NO CABE antes de descomponer. Esa vuelta murió
a media escritura del comentario sin intentar cerrar al padre; el reaper lo re-encoló
(`reap_count=1`) y el pool lo repartió de nuevo sin trabajo propio pendiente — mismo patrón
recurrente ya documentado en `CLAUDE.md` para ~30 items previos.

Verificado: #9990923 sigue reclamado por `wt-3` (con dueño, no se toca), #9990924 y #9990925
siguen `aprobado_revisor` sin reclamar. La descomposición original seguía siendo correcta.

Corrección: ejecuté el intento de cierre faltante (`estado_aprobacion='completado'`). El guard
de paraguas (`RoadmapItem.php` bloque "(2b) PARAGUAS") lo reenrutó a `aprobado_irving` +
`excluir_pool_automatico=true`, liberó `worker_sid`/`claimed_at`, y registró el evento
`paraguas_abierto` ("le quedan 3 sub-item(s) abierto(s)"). Sale del pool/reaper hasta que el
hook de cierre en cascada lo complete solo cuando los 3 hijos cierren.

Sin cambio de código de negocio — el trabajo real sigue en #9990923/#9990924/#9990925. Detalle
completo en `docs/roadmap-bucle-reap-item-9990893-verificacion.md`.
