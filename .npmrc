; Conflicto de peer deps conocido: vue3-apexcharts@1.10 pide apexcharts>=4 pero el
; proyecto usa apexcharts@3.x. Históricamente se instala con --legacy-peer-deps; lo
; fijamos aquí para que `npm ci` del deploy (y `npm install` en dev) resuelvan igual.
legacy-peer-deps=true
